<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.RemoteConfigEditWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Width="540"
                 MinWidth="480"
                 SizeToContent="Height"
                 MinHeight="300"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 WindowStyle="SingleBorderWindow"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 mc:Ignorable="d">

    <Grid Margin="20,48,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部警示横幅 -->
        <Border Grid.Row="0"
                Padding="12"
                Background="#22ED6A0E"
                BorderBrush="#ED6A0E"
                BorderThickness="1"
                CornerRadius="6">
            <StackPanel>
                <ui:TextBlock x:Name="BannerText"
                              FontWeight="SemiBold"
                              TextWrapping="Wrap" />
                <ui:TextBlock x:Name="BannerExtraText"
                              Margin="0,6,0,0"
                              FontSize="12"
                              Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                              TextWrapping="Wrap"
                              Visibility="Collapsed" />
            </StackPanel>
        </Border>

        <!-- 编辑入口 -->
        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <ui:Button x:Name="OpenGroupSettingsButton"
                       Margin="0,0,0,8"
                       HorizontalAlignment="Stretch"
                       HorizontalContentAlignment="Center"
                       Content="配置组设置"
                       Click="OpenGroupSettingsButton_Click" />
            <ui:Button x:Name="OpenHoeingSettingsButton"
                       HorizontalAlignment="Stretch"
                       HorizontalContentAlignment="Center"
                       Content="锄地一条龙（联机）设置"
                       Click="OpenHoeingSettingsButton_Click" />
        </StackPanel>

        <Grid Grid.Row="2" MinHeight="24" />

        <!-- 底部操作 -->
        <StackPanel Grid.Row="3"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <ui:Button x:Name="SaveButton"
                       Margin="0,0,8,0"
                       Appearance="Accent"
                       Content="保存并回传"
                       IsEnabled="False"
                       Click="SaveButton_Click" />
            <ui:Button x:Name="CloseButton"
                       Content="关闭"
                       Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>

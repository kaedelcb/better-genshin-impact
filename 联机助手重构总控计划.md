# 联机助手重构 · 总控计划（Master Plan）

> **本文档是整个重构工程的唯一进度与信息枢纽。**
> 任何新会话/新模型接手时：**先完整读本文档 → 按 §6 提示词模板开工 → 完成后回到本文档更新 §5 进度日志**。
> 维护者：项目 owner + 各执行会话共同维护。创建：2026-09-04。

---

## 0. 给接手者的使用说明（AI/人都适用）

1. **不要跳过本文档直接改代码**。本文档 + 两份设计文档是全部上下文；代码细节到现场再读。
2. 每次开工只做**一个切片**（§4），做完验收、更新 §5 日志、再考虑下一片。禁止跨切片批量推进。
3. 设计文档与代码冲突时，**以代码为准**，并回来修正设计文档。
4. 每个切片结束必须满足其"验收点"才算完成；编译通过只是最低门槛（项目约定：能编译即算成功，无需实际运行，但联机切片除外——标注"需联机实测"的必须实测）。

---

## 1. 三十秒背景

Better Genshin Impact（BGI）的"联机锄地"功能横跨三个进程：**BGI 主进程**（游戏自动化）、**联机助手**（独立 WPF 控制面板）、**协调服务器**（SignalR）。两条通信链路：

- **链路 A（"路"的一部分）**：BGI ↔ 助手，NamedPipe IPC。现状：单向轮询、无事件推送；BGI 监听循环崩溃缺陷已止血修复（P0-A），IPC 已带跨会话 ping 握手校验（ext.hello 的雏形）。
- **链路 B**：助手/BGI ↔ 服务器，SignalR。现状：Hub 公开方法已增至 65 个（原 57 + 日志三件套 8 个）、无协议版本；断线自愈已实现（P1-F Closed 后 30s 自愈），但仍只重连不同步。

业务侧（"货"）：大量权威判定错位在 Hub 传输层（69 处 `lock(room)` + 4 个静态缓存），计数类状态客户端/服务端"双脑各记一半账"，Room 是 40+ 字段的上帝对象，无统一生命周期。

重构 = **路**（通信协议标准化）+ **货**（业务逻辑权威化/状态机化），分 7 个切片渐进推进，底线：**单机零感知、联机行为不变**。

---

## 2. 文档与代码地图

### 2.1 设计文档（仓库根目录，必读）

| 文档 | 内容 | 何时读 |
|---|---|---|
| `联机助手重构总控计划.md`（本文） | 进度枢纽、切片定义、纪律、提示词 | 每次开工前 |
| `联机助手架构审核与重构方案.md`（v2） | **货**：问题诊断（带行号证据）、权威边界、三阶段业务重构、迁移清单 | 做切片 2/3/4/5 前 |
| `联机助手通信架构标准化方案.md` | **路**：模块一 BgiExternalInterface（BGI↔助手 IPC）、模块二 ServerGateway（SignalR 协议）、线程/CT/IDisposable 纪律、验收矩阵 A1-A10 | 做切片 0/1/2/4 前 |
| `联机锄地上线链路稳定性分析报告.md` | 上线链路状态机专项：R1-R10 风险清单（minGen 残留武装、幂等号非单调、threshold 不封顶等），切片 3 必读 | 做切片 2/3 前 |
| `联机助手止血修复评估与提示词.md` | 8 项止血（P0/P1/P2）评估与落实记录 | 了解止血基线时 |
| `Docs/嘟嘟可日志系统设计方案.md` | 嘟嘟可日志系统 P1-P5 + 三件套（实时汇聚/订阅/下载）设计 | 改日志/订阅相关代码前 |
| `Docs/远程配置组编辑-实施方案.md` | 远程配置组编辑契约（4 个新 IPC 操作 + remote_config.* 命令族） | 改配置编辑链路前 |

### 2.2 关键代码位置（到现场再读，此处只索引）

| 区域 | 位置 |
|---|---|
| BGI IPC 服务端 | `BetterGenshinImpact/Service/Instance/`（InstanceService.cs、InstanceConnection.cs、InstanceIpcProtocol.cs、MessageHandlers/InstanceRequestHandler.cs、RemoteEditSession.cs 远程编辑会话） |
| BGI 任务控制 | `BetterGenshinImpact/Core/Script/CancellationContext.cs`、`GameTask/Common/TaskControl.cs`（TaskSemaphore）、`GameTask/TaskRunner.cs` |
| BGI 联机业务 | `GameTask/AutoHoeing/`（AutoHoeingTask.cs 5000 行、Multiplayer/MultiplayerCoordinator.cs、Multiplayer/CoordinatorClient.cs、Multiplayer/KazuhaCollect*.cs 万叶聚物） |
| BGI 上线任务 | `GameTask/AutoOnline/NotifyOnlineTask.cs`（generation 已持久化 User/online_generation.txt） |
| 服务器 | `BgiCoordinatorServer/`（Hubs/CoordinatorHub.cs 3179 行/65 个公开方法、Services/RoomManager.cs 含 AllReady 状态机+日志订阅表、Models/Room.cs） |
| 助手 | `MultiplayerHoeingAssistant/`（ViewModels/MainViewModel.cs 5398 行、Services/IpcClient.cs 含跨会话校验、Services/CommandExecutor.cs 含会话守卫、Services/SignalRClient.cs 含 Closed 自愈） |
| 助手嘟嘟可日志 | `Services/BgiLogTailService.cs`（tail 引擎）、`MemberLogRelayService.cs`（实时汇聚）、`MemberLogShareService.cs`（文件下载）、`KeywordWatchService.cs`、`HoeingStatsService.cs`、`LogFileBrowser.cs`、`ViewModels/DodocoViewModel.cs`、`Views/DodocoPage.xaml` |
| 助手远程配置编辑 | `Services/RemoteConfigEditService.cs`、`Views/RemoteConfigGroupSelectWindow.xaml`；BGI 侧 `View/Windows/RemoteConfigEditWindow.xaml` |

### 2.3 领域知识库（踩坑记录，改对应区域前必读）

- `.agents/knowledge/domains/ipc-signalr.md`——帧格式坑、单任务锁等锁姿势、URL 拼接规则
- `.agents/knowledge/domains/multiplayer-hoeing.md`——先订阅后动作、三信号掉线、轮换双标志、路线索引语义（CurrentRouteIndex vs CurrentJsonRouteIndex）
- `.agents/memory/project-experience.md`——五轮血泪修复（双启动、日志风暴、IPC 崩溃等）
- `.agents/rules/bgi-implementation-patterns-v2.md`——§8 联机代码分布地图、§10/11 公版合并姿势、§12 分支约定

---

## 3. 硬性纪律（每个会话都必须遵守）

### 3.1 分支

- 动手前 `git branch --show-current` 确认在**序号最大的 `main-OldTeaBag-*`** 分支（茶包版主干）。公版对齐基准是 `origin-lcb/main`。
- 每个切片独立 commit 序列，**不跨切片混合提交**；高回归切片（3、5）要求每个子步骤可独立回滚。

### 3.2 编译验证

```
dotnet build BetterGenshinImpact.sln -c Debug
```

三个独立项目（`BetterGenshinImpact` / `BgiCoordinatorServer` / `MultiplayerHoeingAssistant`）改动谁编谁，都 0 error 才算过。程序占用导致无法编译时放弃编译验证即可，但必须在进度日志中注明"未编译验证"。

### 3.3 不可破坏行为（红线）

1. 单机（不连助手、不开联机）行为逐字节不变。
2. IPC 帧格式 `[4字节length][1字节type][JSON]`、length 不含 type 字节——**任何人不得改**。
3. 现有 22 个 IPC 操作（`task.*`/`config.*`/`action.*`，含远程配置编辑新增的 `config.pull_group`/`config.open_remote_editor`/`config.remote_editor_result`/`config.apply_group`）行为不变；新增一律 `ext.` 前缀。
4. `TaskControl.TaskSemaphore` 只读轮询 `CurrentCount` 等锁，**严禁 `WaitAsync` 抢锁**。
5. 触碰 UI/ViewModel 必须 `Dispatcher.InvokeAsync`（异步，非同步 Invoke）。
6. SignalR 断线时封装层门控 + 不 throw；`Closed` 事件必须观察异常参数。
7. 回执型流程先订阅后动作（TaskCompletionSource + finally 退订 + 绑定 ct + 有限超时）。
8. 联机锄地战斗代码在 `GameTask/AutoFight/`（茶包引擎），**不是** `AutoFightOfficial/`。
9. 助手血泪降级逻辑（`CommandExecutor.cs` 两阶段停止、`_hasRestartedThisBatch`、`WaitForBgiIpcReadyAsync`）迁移时逐条保留，不许"顺手简化"。

### 3.4 代码风格

WPF-UI + CommunityToolkit.Mvvm（`[ObservableProperty]`/`[RelayCommand]`）+ Microsoft.Xaml.Behaviors；JSON 优先 Newtonsoft.Json（已被 System.Text.Json 序列化过的模型除外）；对话框用 ThemedMessageBox。

---

## 4. 切片总表

> **路线调整（2026-09-04 下午，用户明确决策）**：**货冻结、路先行**。真实锄地执行逻辑目前够用，切片 3/5（货）与 R1-R9 状态机收口暂停排期；优先顺序调整为 **切片 1 → 切片 4 → 切片 2 的路半边（GatewayHub 骨架 + 兼容层，不做 Room 字段分组）→ 切片 6**。货相关切片何时解冻由用户决定。

| 切片 | 名称 | 归属 | 前置 | 需联机实测 | 状态 |
|---|---|---|---|---|---|
| 0 | AcceptLoop 韧性修复 | 路 | 无 | 否 | ✅ 完成（P0-A 止血 2026-09-04；阶梯退避尾巴已在切片 4 收口：1s/2s/5s/10s/20s 封顶 30s，a39560f0） |
| 1 | 模块一 BgiExternalInterface + 助手 SDK | 路 | 0 | 否（单机+助手即可） | ✅ 完成（2026-09-04，编译 0 error + A1~A6 静态自检 + 独立复审并修复 3 处 bug；事件通道端到端同机实测列为遗留） |
| 2 | RoomPhase 观测 + Room 字段分组 + GatewayHub 骨架 + 65 方法瘦身 | 路+货 | 1 | 是 | ⚠️ 路半边完成（2026-09-04，13 commit + 两轮审查（Kimi Code 自审 4 处 + 会话侧独立复审修复 1 处）；sln 编译 0 error + 服务器测试 42/42 复跑通过；实机验收①②列遗留，见 §5 日志）。货半边（Room 分组）仍冻结 |
| 3 | RoomActor 化 + 经验计数上迁 + 上线链路 R1-R9 收口 | 货 | 2 | 是（多轮） | ⏸️ 冻结（货） |
| 4 | 增量状态同步 + 断线重连恢复 | 路 | 1（原 3，路先行后前置放宽） | 是（拔网线场景） | ✅ 完成（路先行版，2026-09-04，8 commit + 独立复审修复 1 处；编译 0 error + 静态自证；实机验收①②③列为遗留，见 §5 日志） |
| 5 | Phase 门控 + 双端瘦身 | 货 | 3 | 是（完整回归） | ⏸️ 冻结（货） |
| 6 | 旧协议退役 | 路+货 | 5 + 一个版本观察期 | 否 | ⬜ 未开始 |

状态标记：⬜ 未开始 / 🔶 进行中 / ✅ 完成 / ⚠️ 完成但有遗留（日志注明） / ⏸️ 冻结。

### 切片 0：AcceptLoop 韧性修复（0.5~1 天）

> **状态（2026-09-04）**：止血版已落地（P0-A）——catch 已移入循环内、单次失败固定 300ms 退避、失败管道实例丢弃后下轮重建，"单连接异常不退出循环"的核心验收语义已满足。与本文设计的差异仅为退避策略（固定 300ms vs 阶梯 1s/2s/5s 封顶 30s），剩余部分可在切片 1 顺手并入。

- **目标**：BGI 命名管道监听循环不再因单连接异常整体退出。
- **依据**：《通信方案》§3.9；现状 `InstanceService.cs:337-370` 外层 catch 只记日志不重启循环。
- **改动**：`InstanceService.AcceptLoopAsync`——单连接异常 catch 移入循环内，连续失败退避（1s/2s/5s，封顶 30s）重建 server。
- **验收**：模拟客户端"连上立刻断"100 次后监听循环仍存活；正常 IPC 命令不受影响；BGI 项目编译 0 error。

### 切片 1：模块一 + 助手 SDK（1.5~2 周）

- **目标**：《通信方案》§3 全量落地——`Service/ExternalInterface/` 新目录（hello/幂等窗口/三平面/ext.event 推送），助手侧 `BgiExternalClient` SDK 替换 `IpcClient`+`CommandExecutor` 裸调用。
- **依据**：《通信方案》§3（分层架构、操作清单、错误码、桥接映射、验收 A1-A6）。
- **红线**：挂载点 ≤3 处单行；助手降级逻辑逐条保留（§3.3-9）；事件观察器只在有订阅者时启动。
- **验收**：A1~A6（《通信方案》§7）；新助手+老 BGI 自动降级无报错；三项目编译 0 error。

### 切片 2：RoomPhase 观测 + Room 分组 + GatewayHub 骨架（2~3 周）

- **目标**：《审核方案》§7.1.1（Phase 推导+日志，不驱动）+ §7.1.2（字段分组，每组一个 commit）+ §7.1.3（65 方法三段式瘦身，与《通信方案》§4.7 兼容层同批完成；覆盖日志三件套新增的 8 个方法）。
- **依据**：《审核方案》§三问题 4 字段组表；《通信方案》§4.2 信封、§4.3 消息名映射（已含 65 方法版）、§4.4 握手。
- **验收**：行为零变化（部署后跑一次完整联机验证）；Phase 日志与真实流程吻合；GatewayHub 与旧 Hub 双轨并存。

### 切片 3：RoomActor 化 + 经验计数上迁 + 上线链路状态机收口（2~3 周，高回归）

- **目标**：《审核方案》§7.2——Channel 邮箱 + Effect 外置 + 静态缓存/Timer 搬家 + HeartbeatMonitor 改投递；经验计数服务端化（客户端删 4 字段）；**上线链路状态机收口——逐条修复《联机锄地上线链路稳定性分析报告.md》R1-R9**（minGen 残留武装、轮次 id 非单调 vs 客户端幂等号、threshold 不对在线人数封顶、断线抹事件不补报、确认期被新上报打断、幻影上线、双路径 generation 不同源、exhausted 无手动重置/自愈）。
- **依据**：《审核方案》§7.2.1/7.2.2、§8.5（Timer 必须改延迟消息）；《通信方案》§4.4 capability `exp.serverSideCount`；《稳定性分析报告》③ 风险清单。
- **顺序**：先搬 SyncPoint 域验证 Actor 模式，再推广；经验计数是计数上迁第一刀；AllReady 状态机随 RoomActor 一并收编（RoomAllReadyState 并入 Actor 状态）。
- **验收**：多轮四方联机无回归；新旧房间按协议锁定各走各路；69 处 `lock(room)` 清零；R1-R9 逐条有修复或明确设计豁免说明。

### 切片 4：增量同步 + 断线恢复（1~1.5 周）

> **路先行范围界定（2026-09-04）**：本切片的终态目标是《通信方案》§4.5/§4.6（模块二 Room 状态 revision+delta + 断线补差），但其依赖 GatewayHub（切片 2 路半边，排在本切片之后）与 RoomActor（切片 3，货冻结）。因此**本次只做 BGI↔助手 IPC 通道（模块一）的增量同步与断线恢复闭环**，模块二部分顺延到切片 2 路半边完成后接续。

- **本次范围（模块一通道）**：
  1. 助手侧剩余 v2 轮询全量迁移到 `BgiExternalClient` SDK（切片 1 只接管了 online.triggered；task.status 等 10s 轮询改走 ext.task.status + ext.event 事件驱动，轮询降级为兜底）——切片 1 遗留③。
  2. `stateRevision` 同源闭环：ext.task.status 快照与 ext.event 事件帧共用同一单调 revision（《通信方案》§3.5 事件面），SDK 发现跳号 → 自动拉快照补齐（LSP 文档同步模型）。
  3. 断线重连恢复闭环：SDK 重连后 hello 重新协商 → 带 lastKnownRevision 恢复订阅 → 服务端可续则补发缺失事件、不可续则客户端主动拉快照（《通信方案》§4.6 流程在模块一通道的对应实现）；连接状态机 Connecting→Handshaking→Ready→Degraded→Closed 落地到 SDK 公开属性。
  4. ext.* 操作清单补全：远程配置编辑三件套与 config.pull_group 纳入 ext.* 兼容映射（见 §5 日志 2026-09-04 远程配置行遗留）。
  5. 顺手收尾：AcceptLoop 固定 300ms 退避升级为阶梯 1s/2s/5s 封顶 30s（切片 0 尾巴，《通信方案》§3.9）。
- **明确不做**：模块二 §4.5 RoomStateDelta / §4.6 房间事件日志重放（依赖切片 2/3）；任何 Room 业务字段改动（货冻结）。
- **验收**：①同机实测：助手与 BGI 长连状态下杀掉助手进程重启 → 重连后状态经快照/补差恢复一致、事件不丢不重；②拔 NamedPipe（结束 BGI 进程）模拟断线 → SDK 状态机 Degraded→重连→Ready 全流程日志正确；③revision 跳号场景（可注入测试事件）自动拉快照；④三项目编译 0 error；⑤单机零感知（不连助手时 BGI 行为逐字节不变）。

### 切片 5：Phase 门控 + 双端瘦身（2~3 周，高回归）

- **目标**：《审核方案》§7.3——Phase 从观测升门控、RoundTransitionCoordinator、CoordinatorClient→BgiRoomClient、AutoHoeingTask 抽 MultiplayerSession、MainViewModel 三层拆分。
- **验收**：完整联机回归（建房→验证→开锄→多世界轮换→经验上限停止全链路）；单机零感知。

### 切片 6：旧协议退役（2~3 天 + 观察期）

- **目标**：旧 Hub 方法标 `[Obsolete]` → 一个版本周期 → 删除；`VersionCompatibilityDecisions` 字符串比较退役。
- **验收**：三项目编译 0 error；全量客户端新协议运行一个版本无兼容工单。

---

## 5. 进度日志（每完成/推进一个切片必须更新）

| 日期 | 会话/模型 | 切片 | 动作 | 结果 | 遗留问题 |
|---|---|---|---|---|---|
| 2026-09-04 | Kimi | — | 产出两份设计文档 + 本总控计划 | ✅ 完成 | 无 |
| 2026-09-04 | Kimi | 止血修复（重构前置） | 按《联机助手止血修复评估与提示词.md》完成 8 项最小止血：P0-A AcceptLoop 单点崩溃（InstanceService.cs）、P0-B onlineGeneration 跨重启单调递增（BGI 侧持久化 User/online_generation.txt + 助手侧 _lastOnlineGeneration 复位/同步）、P1-C suspend 后固定等待改轮询 task.status、P1-D resume group 分支 Dispatcher.Invoke→InvokeAsync、P1-E BgiProcessMonitor 崩溃事件边沿检测（含 MainViewModel Interlocked 重入守卫）、P1-F SignalR Closed 后 30s 自愈重连、P2-G 确认耗尽分支初版做了降级开锄，后按用户明确取舍推翻（宁可不锄/漏锄也不能缺人开锄），最终仅保留明确日志（标注未确认成员）+ MarkExhausted，耗尽即整轮放弃不开锄、P2-H resume no_context 明确提示 | ✅ 编译 0 error（sln 全量 Debug 构建，含嵌套 MHA 构建）；7 文件，未提交 commit 待用户审阅 | ① 纯外部手动重启 BGI（不经助手 RestartBgi）场景会漏一次上线事件，靠 P0-B 双保险兜底；② P1-C 每轮 IPC 连接超时 1s，极端场景总等待可能超过标称 6s；③ 确认耗尽即整轮放弃，重构期由 RoomActor/Phase 门控根治确认可靠性 |
| 2026-09-04 | Kimi | 止血修复第二批（定时上线链路） | 全链路梳理（产出《联机锄地上线链路稳定性分析报告.md》）后完成 S1-S5：S1 启动时按"是否配置了定时上线时间"初始化 _manuallyClearedOnline 武装状态（修"助手重启后定时永久失效"）、S2 到点上报前检查 SignalR 连接，断线则不打"已上报"、不标已触发、30s 后自动重试（MarkOnlineAsync 改返回 Task<bool>）、S3 _localOnlineGeneration 持久化到 %APPDATA%/NexusBGI/assistant-online-generation.txt（复刻 P0-B 姿势）、S4 定时器回调加 try-catch + 已触发标记提前防 30s 重入双触发、S5 服务端 CheckAndTransition 凑不齐人时打日志标明"就绪 x/y，等待成员：显示名(uid)"（纯日志不改行为） | ✅ MHA/服务器 C# 编译 0 error；MHA 部署拷贝步骤因助手正在运行（PID 30956/33424 文件锁）按 AGENTS.md 约定放弃，需关闭助手后再做一次完整编译 | ① IsConnected 检查到 Invoke 之间有毫秒级竞态窗口，根治需服务端送达确认（协议改动，重构期）；② 双端 generation 共用一个服务端槽位的固有竞争需协议层区分来源（重构期）；③ "缺人不开锄"在客户端层面无法兑现（收 AllReadyConfirm 即开锄），需确认/开锄信号分离（重构期）；④ 服务端 ConsumeOnlineReady 只消费 minGen 成员导致残留武装 30 分钟可重复触发（重构期）；⑤ syncToAll 对离线成员不缓存无回执（重构期） |
| 2026-09-04 | Kimi | 功能开发（重构外） | 远程配置组编辑（3953a859）：助手卡片⚙→拉取对方配置组→本机 BGI 原样设置页编辑→回传合并落盘；新增 4 个 IPC 操作（config.pull_group/open_remote_editor/remote_editor_result/apply_group）+ remote_config.* 定向命令族（复用 SendRemoteCommand 离线缓存，服务器零改动）+ BGI 侧 RemoteEditSession 单会话管理（15 分钟僵尸回收） | ✅ 两端编译 0 error；契约文档 Docs/远程配置组编辑-实施方案.md | open_remote_editor 用了同步 Dispatcher.Invoke（为同步回报窗口创建失败的有意取舍，操作有界）；切片 1 落地时需纳入 ext.* 操作清单 |
| 2026-09-04 | Kimi | 功能开发（重构外） | 万叶聚物三项修复（78e7edf5）：残留 CD 尾轮询、快照第 1 层捕获、syncKey 重跑隔离（AutoHoeing/Multiplayer/KazuhaCollect*） | ✅ 编译通过 | 属 BGI 侧战斗逻辑，与通信重构正交 |
| 2026-09-04 | Kimi | 功能开发（重构外） | 嘟嘟可日志系统 P1-P5（7fcbbafb）：BgiLogTailService 实时 tail（专用后台线程+Watcher/轮询双保险+逐订阅者隔离分发）、异常监控、锄地数据、桌面监控、远程成员截图汇聚 | ✅ 编译 0 error | 设计文档 Docs/嘟嘟可日志系统设计方案.md |
| 2026-09-04 | Kimi | 功能修复（重构外） | IPC 跨会话检测（42b81a04）：IpcClient 连接后 ping 握手比对 windowsSessionId，跨会话/无法确认一律不可信；CommandExecutor 控制指令会话守卫（阻断且不进入杀进程回退）；标题栏⚠跨会话徽章 | ✅ 编译通过 | ping 握手即《通信方案》§3.4 ext.hello 的雏形，切片 1 在其上演进；每次 ConnectAsync 多一次 ping 往返（10s 轮询路径可接受） |
| 2026-09-04 | Kimi | 功能开发（重构外） | 嘟嘟可日志三件套（dd5cf141）：房间实时日志汇聚（观众驱动按需订阅+500ms 合批+300 环形补发）、订阅表（服务端 RoomManager，上限 20/目标）、远程成员完整日志下载（gzip+base64 分块 192KB/160ms，双端文件名白名单防目录穿越，服务端纯转发+单播+双限流）；服务端 Hub +8 公开方法（57→65）；BgiCoordinatorServer.Tests 18/18 通过 | ✅ 两端编译 0 error；测试 18/18 | Hub 方法数再度膨胀，切片 2 兼容层映射表已同步更新（《通信方案》§4.3） |
| 2026-09-04 | Kimi | 全局复审（本文档更新） | 对止血 8 项 + S1-S5 + 上述新功能做全量代码复审：逐条核实修复落实与质量（结论：全部落实，工程质量高）；确认 P2-G 按用户决策反转为"缺人不开锄"；核实 P1-C 依赖的 task.status running/hasSuspendedTaskContext 字段真实存在；ping 响应确含 windowsSessionId；更新三份文档（本计划 + 两份设计文档 v2.1） | ✅ 只读复审，无代码改动 | 新发现问题极少：①RemoteEditSession/日志服务均良好；②切片 3 需收口 R1-R9（已写入切片定义）；③稳定性分析报告 R7 幻影上线（助手重启即误触发）仍未修，列入切片 3 |
| 2026-09-04 | 用户决策 + Kimi 记录 | 路线调整 | **货冻结、路先行**：真实锄地执行逻辑够用，切片 3/5（货）与 R1-R9 状态机收口暂停排期；优先顺序改为 切片 1 → 切片 4 → 切片 2 路半边 → 切片 6。核实 BGI 侧 CoordinatorClient 断线自愈已存在（内置 42s + 2 轮×4 次指数退避 ≈ 77s，超限走 OnDegraded 既有语义），路的断线地基已齐：助手侧 P1-F、BGI 侧自愈、IPC P0-A | ✅ 已更新 §4 切片总表 | 货解冻时机由用户决定；R 项中触发频繁的 R6/R1/R7 若日常使用中被吐槽，可单独立"小补丁"提前修，不必等切片 3 |
| 2026-09-04 | Kimi | 前置清理 | 工作区存在上一会话未提交的"跨会话注入+误杀在跑任务"修复（InstanceRequestHandler 跨会话接收端守卫 + task.start 先问再杀 + ScriptService.RunMulti Set() 条件化，交接文档在 Docs/交接-联机助手跨会话注入与任务误杀修复.md），先单独入库 b5386005 再开工，避免与切片 1 改动混杂 | ✅ 已入库 | 无 |
| 2026-09-04 | Kimi | 切片 1：模块一 + 助手 SDK | 4 commit 落地：①BGI 侧 Service/ExternalInterface/ 目录自包含模块（ext.hello 握手+能力协商、幂等窗口 TTL60s 命中重放 [IDEMPOTENT_REPLAY]、控制/查询/事件三平面、事件中心+200ms 轮询观察器——只读 CurrentCount 绝不 WaitAsync、只在有订阅者时运行；CommandPlane/QueryPlane 直接委托既有 handler 实现单一事实源）0f646029；②挂载点 3 处（HandleAsync 守卫后 ext. 单行分发、App.xaml.cs 一行 DI 注册、suspend/resume 成功路径末尾各一行事件发布），既有 22 个 v2 操作 switch 零改动 b1859c1f；③助手侧 BgiExternalClient SDK（长连接 hello 握手、requestId 关联、ext.event 分发、断线重连自动恢复订阅、老 BGI 返回 unsupported_operation → Legacy 静默降级）91dabd2e；④MainViewModel 上线检测接入事件通道（online.triggered 秒级到达替代 10s 轮询，事件回调与轮询边沿语义逐条对齐含 P0-B 基线同步；通道不可用自动退回原轮询）0111c3aa | ✅ sln 全量 Debug 编译 0 error（BGI/MHA/服务器）；A1~A6 逐项静态自检通过 | ①设计冲突 1 处已由独立审查回改文档（见下行）；②事件通道端到端实测（新助手+新 BGI 同机：上线事件 1s 内到达、退订无残留、重连补订）未跑，需用户实机验证一轮；③助手侧 task.status 等其余轮询仍走 v2（按范围控制只接管 online.triggered；全量替换留给切片 4）；④幂等窗口只缓存成功响应（失败瞬态不缓存，避免重放挡住合法重试），与设计"重放缓存响应"字面略有出入，属有意取舍；⑤AcceptLoop 阶梯退避仍遗留（P0-A 固定 300ms，切片 0 尾巴） |
| 2026-09-04 | Kimi | 切片 1 独立审查 | 对切片 1 全部代码做独立复审（外部接口 6 文件/3 挂载点/SDK 751 行/MainViewModel 接入），整体设计质量高、A1-A6 自证成立；**发现并直接修复 3 处 bug**（未提交，待用户审阅）：①InstanceRequestHandler.HandleTaskStart 幂等登记从拒绝检查之前移到之后——原实现被拒请求也污染 generation 去重，客户端重试会被吞成 already_executed 但任务从未启动；②CommandExecutor.StartGroupAsync/StartOneClickAsync 收到 task_already_running 业务拒绝时改为 1s×6 重发重试等旧任务退场、其他业务拒绝直接报失败带 ErrorCode，**不再进 KillBgi+RestartBgi 杀进程回退**（原代码会把业务拒绝当传输失败杀掉 BGI 正在跑的任务，违背 b5386005 无损拒绝初衷；仅 catch 传输异常才回退）——配套 IpcResponse 增加 ErrorCode 属性 + IpcClient 解析 errorCode 字段；③ExternalInterfaceSession.HandleHello 的 windowsSessionId/processId 改传 BGI 自身端点（Process.GetCurrentProcess().SessionId/Environment.ProcessId）——原实现误传对端助手的 ClientSessionId，SDK 跨会话自检恒等形同虚设（服务端守卫兜底不致害）。同步回改《通信方案》§3.3 设计冲突：信封 version 冻结在 2（InstanceConnection.cs:218 严格校验，升 v3 会被老 BGI 丢帧而非返回 unsupported_operation），功能协商走 hello capabilities，§3.4 示例与 9.1 标题同步修正 | ✅ BGI 项目编译 0 error；MHA 项目 C# 编译 0 error（error CS = 0），部署拷贝步骤因助手正在运行（PID 8888/30688 文件锁）按 AGENTS.md 约定放弃，需关闭助手后再做一次完整编译部署 | ①3 处修复未 commit，由用户审阅后提交；②行为变化需知悉：用户在 BGI 手动跑任务时助手快捷指令 task.start 从"取消替换"变为"重试 6s 后报失败、不杀进程"（b5386005 有意语义，修复使其真正无损）；③SDK 跨会话 _unrecoverable 置位后无自愈需重建 client，MHA 侧靠 1 分钟探测退避+新建 client 兜底，可接受；④切片 1 端到端实机验证仍未跑 |
| 2026-09-04 | Kimi | 切片 4（路先行版）：模块一通道增量同步 + 断线恢复 | 8 commit 落地：①BGI 侧事件近因环形缓冲（500 条，独立小锁只做入队/快照拷贝）+ ext.event.subscribe 携带 lastKnownRevision 断线续传（先取缓冲快照补发再注册订阅者——与实时推送零交叠不重、窗口期缺口由客户端跳号+快照兜底不丢；不可续/版本号回退返回 resyncRequired）+ ext.task.status 快照 revision 改前置取样（方向安全化）+ hello 能力位 event.replay（3e4ecf61）；②AcceptLoop 固定 300ms 退避升级阶梯 1s/2s/5s/10s/20s 封顶 30s、连续成功重置（a39560f0，切片 0 尾巴收口）；③SDK 连接状态机 Connecting→Handshaking→Ready→Degraded→Closed 只读属性+变更事件（49b98d1e）；④SDK revision 同源闭环：事件跳号自动拉 ext.task.status 快照（300ms 节流+在飞去重）、快照覆盖线去重（rev≤floor 不分派）、恢复订阅自动带 lastKnownRevision（能力门控）、hello sessionId 变更（BGI 重启）复位 revision 计数防永久跳帧、v2→ext 操作映射表 TryMapToExtOperation（f9ab762d）；⑤ReportStatusAsync 主轮询迁移：通道活跃时 task.status 不再周期轮询（订阅全事件+事件触发快照刷新+订阅基线快照，SnapshotUpdated 应用含 onlineGeneration 边沿 P0-B 同款语义）、config.list 走 ext 长连接，通道不可用走 v2 原路径逐字节兜底（feb99670）；⑥task.status 查询点×3（锄地结束 hasSuspendedTaskContext 检查/ClearLocalOnline 代序号读取/P1-C suspend 后等锁轮询）经 SendBgiIpcPreferredAsync ext 优先 v2 兜底（d6c8e8a4）；⑦config.list 读取点×3（远程编辑选组/GetConfigTasks/GetLocalConfigsAsync）同姿势（2952fd7a）；⑧远程配置编辑链路四操作迁 ext.*（config.pull_group→ext.config.pullGroup、apply_group→applyGroup、open_remote_editor/remote_editor_result 经 RemoteConfigEditService 注入 getExternalClient；BGI 侧 ext 映射切片 1 已就绪；v2 旧操作保留）（a01ac774） | ✅ sln 全量 Debug 编译 0 error（BGI/MHA/服务器三项目）；验收④⑤静态自证通过（单机零感知：观察器仍只在有订阅者时运行、v2 路径逐字节保留、BGI 侧改动仅在 ext.* 分发内与 AcceptLoop 退避数值） | ①验收①②③需同机实机：助手重启重连（快照/补差恢复、事件不丢不重）、杀 BGI 进程（Degraded→重连→Ready 日志，AddLog 有 [ext] 通道状态输出）、注入跳号事件（自动拉快照）——未跑，替代自测=静态推演+编译，列实机验证待办；②CommandExecutor 控制命令（task.start/stop/suspend/resume/action.*/config.set_task_enabled）按范围控制保留 v2 主路径（血泪降级逻辑所在地，红线 §3.3-9），BGI 侧 ext 控制面切片 1 已就绪，助手侧切换留待后续评估；③切片 1 遗留③（v2 轮询迁移）与切片 0 尾巴（阶梯退避）本切片收口；④快照边沿应用与轮询同语义，R7 幻影上线不变（切片 3 收口） |
| 2026-09-04 | Kimi | 切片 4 独立审查 | 对切片 4 全部 8 个 commit 逐文件复审（BGI 侧 5 文件 + 助手侧 SDK/RemoteConfigEditService/MainViewModel 约 790 行改动）。结论：整体设计落地质量高——近因环形缓冲独立小锁不与扇出写路径交叉、先补发后注册与实时推送零交叠（窗口期缺口由快照校准兜底，方向安全）、快照 revision 前置取样方向正确、AcceptLoop 阶梯退避含连续成功重置与失败管道丢弃、revision 同源闭环（跳号拉快照/覆盖线去重/sessionId 变更复位）推演自洽、v2→ext 迁移点全部保留逐字节兜底、BGI 侧 ext.config.* 四操作处理器切片 1 已就绪（CommandPlane 委托既有 handler，无版本内偏斜）。**修复 1 处**：BgiExternalClient.RefreshStatusSnapshotAsync 异常过滤 catch 后补兜底 catch-all——该方法常以 fire-and-forget 调用（revision 跳号/事件触发），非过滤清单内的异常逃逸会冒泡到 TaskScheduler.UnobservedTaskException 弹全局异常框（App.xaml.cs:85 已挂钩） | ✅ MHA 项目 C# 编译 0 error（error CS=0）；部署拷贝因助手运行中文件锁按纪律放弃；BGI 侧本切片无新改动需重验（沿用切片 1 编译结论） | ①修复未 commit 待用户审阅；②已知限制（不修）：SDK _unrecoverable（跨会话）后 ext 通道不再自建新 client，靠 v2 跨会话守卫兜底，场景解除后要重启助手才恢复 ext 通道——跨会话本身是持久态，影响可忽略；③实机验收①②③仍未跑，与切片 1 实机验证合并为一轮做 |
| 2026-09-04 | Kimi | 切片 2（路半边）：GatewayHub 骨架 + 兼容层 + RoomPhase 观测 | 13 commit 落地（仅动 BgiCoordinatorServer + BgiCoordinatorServer.Tests，客户端/wwwroot 零改动）：①GatewayHub 骨架 44c7e16d（GatewayEnvelope 信封/ session.hello 握手+能力协商 [能力缺省即不支持，ServerCapabilities 只列真实实现的 gateway.envelope.v3]/ Dispatch+Query 双入口 + 按族路由表基座/ GatewayBroadcaster 双发器 [旧名→/hub 组、evt→/gateway 组，一连接只属一个 Hub 天然单份，evt 侧异常就地捕获]/ RoomPhaseObserver 基座，与旧 Hub 双轨并存 /hub+/gateway）；②F1-F8 按族迁移 eab0ce8c/a528823e/f83e9105/b96f5c33/0df86b32/3d4f8149/88d33d6d/05ea1e58——65 个公开方法体逐字搬到 RoomOperations.*.cs partial 类（单一事实源），旧 Hub 3179→463 行全 3-5 行转发器，GatewayDispatcher 按族注册 41 个消息名（room.getState 分组收敛 section 区分 6 查询、exp.reportFightResult 按 kind 聚合 5 旧语义、sync.reportArrival/session.heartbeat 各聚合 2、MarkRoomStarted×2 聚合）；③事件映射补漏 80fa07ef（PlayerAnomalyRecovered 与 AbnormalPlayerRecovered 两旧名并存，均→anomaly.playerRecovered）；④F9 断线收口 0c523883（OnDisconnectedAsync 迁入 RoomOperations.HandleDisconnectAsync 双 Hub 共用——**修复组跟踪表分裂隐患**：若留旧 Hub 读自身 _connectionGroups（已空）控制房间断线清理会静默失效；HeartbeatMonitor 7 处广播双发；旧 Hub 残留字段/静态表清零）；⑤单测 51d2580c（23 例：65 方法↔映射表反射机器校验、40 客户端订阅事件双发核对、RoomPhase 推导 9 例、握手时序/错误码/双发 mock 验证、房间协议锁定、信封序列化）；⑥独立审查 + 修复 db8450b7（SendToCallerAsync V3 分支并入 SendEvtSafeAsync 就地捕获、路由完备性测试、注释纠偏×2、Forget 缺口注释声明）。**房间协议锁定**（§4.7）：Room.Protocol（legacy 默认/v3），建房按房主客户端协议锁定、join 同协议校验、v3 join 响应带 roomProtocol；**RoomPhase 观测**：Idle/WaitingForPlayers/RouteVerifying/ReadyToStart/Running/RoundTransition/Ended 纯函数推导 + 每操作入口观测日志（Information 只在变化时，Debug 每次），只推导不驱动 | ✅ sln 全量 Debug 编译 0 error（三项目）；服务器测试 42/42（18 存量 + 24 新增）；独立审查结论"通过"（15+ 方法新旧逐字对照无漂移，lock/CTS Dispose/Timer/静态表/fire-and-forget 语义全保留） | ①验收①（完整联机流程回归：建房→验证→开锄→多世界轮换→经验上限停止）与②（Phase 日志与真实流程吻合度）无法自测，列实机待办：部署后跑一次完整联机并观察 [RoomPhase] 日志一个版本周期；②URL 归一化的客户端部分（旧配置带 /hub 加载时归一化并告警、SDK 拼路径）属客户端范围未做——服务器侧兼容已落地（/hub 不动 + /gateway 新增，§4.8 基地址约定留待客户端 SDK 化时启用）；③§4.5/4.6（revision+delta、事件日志重放）按范围顺延；④设计冲突 2 处以代码为准：信封 Payload 用 System.Text.Json（服务器无 Newtonsoft 依赖，设计 §4.2 括注以代码为准）；PlayerAnomalyRecovered 实际广播名与文档编目不一并已补录；⑤RoomPhaseObserver.Forget 不覆盖 RoomManager 内部三条隐式删房路径（同 UID 淘汰/LeaveRoom 空房/心跳空房），每废房残留几十字节观测条目，货冻结期不改 RoomManager，切片 3 收口（代码注释已声明）；⑥GatewayDispatcher 对畸形 payload 的解析异常统一包 internal_error（不逃逸），payload 键大小写严格 camelCase 留待 v3 客户端联调时注意 |
| 2026-09-04 | Kimi（会话侧） | 切片 2 独立复审 | 在 Kimi Code 自审（db8450b7）之后做第二轮独立复审：通读网关骨架全部新文件（信封/路由/握手/双发器/会话跟踪）+ 高危路径逐字对照新旧实现（JoinRoom 含版本校验与协议锁定、ReportOnlineEvent+StartConfirmAsync 上线确认链、HandleDisconnectAsync 断线清理、HeartbeatMonitor 7 处双发、CloseRoom/MarkRoomStarted）。结论：**通过**——①JoinRoom/ReportOnlineEvent/StartConfirmAsync 与旧实现逐字等价（仅 Context→ctx、Clients→_broadcaster 参数化），"缺人不开锄"取舍完整保留；②组隔离成立（一连接只属一个 Hub，双发天然单份），组跟踪表已与组操作/断线清理同源（0c523883 修复核实到位）；③协议锁定默认 legacy，旧客户端路径逐字节不变；④RoomPhase 观测纯函数+lock 内取样+调用点 try/catch 兜底，Forget 缺口已声明属切片 3；⑤畸形 payload 不逃逸。**修复 1 处**（轻微）：GatewayBroadcaster.SendToConnectionAsync 的 v3 分支遇无映射旧事件时静默丢弃且无日志，违背自身"无映射必须留痕"纪律，补 LogWarning（v3 客户端尚不存在，无现网影响）。服务器编译 0 error + 测试 42/42 复跑通过 | ✅ 已提交 | ①实机验收①②仍待用户真机回归（与切片 1/4 实机遗留合并为一轮）；②65 方法中抽查覆盖率约 15 个高危方法，其余 50 个简单方法由机器化映射校验+反射完备性测试兜底，未逐个人工对照 |

| 2026-09-04 | Kimi（会话侧） | 切片 2 实机回归修复 | 用户真机验证发现两台助手同房成员列表全空（在线正常、进房日志正常）→ 定位：`GatewayBroadcaster` 3 处旧协议侧广播 `SendAsync(legacyEventName, legacyArgs)` 触发**双层嵌套**——`IClientProxy.SendAsync` 只有逐参重载（arg1..arg10，无 params 数组重载，已拉 dotnet/aspnetcore release/8.0 `ClientProxyExtensions.cs` 源码坐实），传 `object?[]` 变量被绑成 `arg1=数组本身`，线上消息 `arguments[0]=[原参数数组]`，客户端 `On<List<ControlRoomPlayer>>` 反序列化静默失败 → **所有服务器→客户端旧协议广播全坏**（成员列表只是首发症状，AllReady/AllArrived/心跳广播全受影响）；客户端→服务器 Invoke 不受影响所以进房正常。旧 Hub 逐参写死所以没事，切片 2 抽成 `params object?[]` 才踩中。**为什么 42 测试没拦住**：mock 的是 `SendCoreAsync`（接口唯一方法），`SendAsync` 扩展方法真实执行后把双层嵌套传给 mock，但旧断言只看事件名不看参数形状。修复：3 处 legacy 发送改 `SendCoreAsync(legacyEventName, legacyArgs)`（BroadcastGroupAsync/SendToCallerAsync/SendToConnectionAsync，evt 侧单信封参数本就正确未动）+ 新增 `LegacyBroadcastShapeTests` 4 例直接断言参数形状（单列表不嵌套/多参保序/定向不嵌套/evt 单信封） | ✅ 已提交 08534c35；服务器测试 46/46（42 存量 + 4 新增） | ①用户需重传 `BgiCoordinatorServer/Gateway/GatewayBroadcaster.cs` 到服务器并 `bash deploy.sh` 后重跑真机回归（助手/BGI 端不用动，bug 纯服务器侧）；②切片 2 实机验收①②随本轮回归一并验收；③教训：扩展方法包装接口方法时，mock 断言必须覆盖参数形状而非仅方法名 |
| 2026-09-04 | Kimi（会话侧） | 实机回归双修（重构外功能 bug） | 用户真机再报 2 个 bug：**①远程配置编辑弹窗必失败**（"弹出编辑窗口失败: Requested value 'Accent' was not found"）——`RemoteConfigEditWindow.xaml` 的 `ui:Button Appearance="Accent"`，WPF-UI 4.0.2 `ControlAppearance` 枚举无 Accent（net481 程序集反射坐实合法集 9 值无 None/Accent），XAML 枚举校验是运行期的、编译期不报错，`InitializeComponent` 即抛 → 该功能自 3953a859 合入起从未真正打开过（此前仅编译验证未实机点过）；修复 Accent→Primary，并全仓 XAML `Appearance="..."` 取值排雷（仅此一处非法）。**②远程桌面截图模糊不可读**——`MemberScreenshotRelayService` 上报帧写死 `CaptureJpeg(480, 60)`（1080p 压 1/4 + 质量 60）；服务器上限 10MB 有大量余量，修复为宽度走统一设置 `ShareScreenshotWidth`（480/960/1280/1920 默认 1280，NormalizeLocked 数值域矫正）+ 质量 75 + 桌面监控 Tab 共享开关旁加画质下拉，1280px/q75 单帧约 100~180KB、10s 一帧 ≈15~25KB/s/人 | ✅ 已提交 e046cc07；BGI/MHA 均 0 error CS（BGI 输出拷贝因 BetterGI 运行中文件锁失败按纪律放弃） | ①两个修复都需实机复验：远程编辑弹窗应能正常打开、远程画面应能读字；②教训：**仅编译验证无法发现 XAML 运行期枚举错误**，涉及 XAML 新窗口的功能必须实机点开一次才算验收；③4080p 早期 P5 设计选 480px 是为省流，默认值已修正但带宽敏感用户仍可手动降回 |

---

## 6. 提示词模板

### 6.1 新会话开工模板（最常用）

```
你是 BGI 项目联机助手重构的执行者。先按顺序阅读以下文件（都在仓库根目录 E:\Program Files\better-genshin-impact-LCB\）：
1. 联机助手重构总控计划.md（全部——纪律、切片定义、进度日志）
2. 联机助手通信架构标准化方案.md 或 联机助手架构审核与重构方案.md（按切片归属选读）

然后执行【切片 N：<名称>】。要求：
- 严格遵守总控计划 §3 硬性纪律；
- 动手前 git branch --show-current 确认分支；
- 只读本切片涉及的设计文档章节和代码文件，不要全文翻仓库；
- 完成后：编译验证（dotnet build BetterGenshinImpact.sln -c Debug）、按切片验收点自检、
  更新总控计划 §5 进度日志和 §4 切片状态；
- 遇到设计文档与代码冲突，以代码为准并报告，不要擅自改设计。
```

### 6.2 切片中途续作模板

```
继续 BGI 联机助手重构【切片 N】。先读 联机助手重构总控计划.md 的 §5 进度日志，
找到上次会话留下的进度和遗留问题，从断点继续。纪律同总控计划 §3。
```

### 6.3 代码审查模板（切片完成后独立会话复核）

```
你是代码审查员。审查 BGI 仓库当前未提交/最近 N 个 commit 的改动，背景读：
联机助手重构总控计划.md §3 纪律 + 对应切片的设计文档章节。
按 AGENTS.md 的 Code Review Rules 输出（简体中文、五段式：严重程度/位置/原因/影响/修复建议）。
重点：崩溃/死锁/资源泄漏/数据竞争、CancellationToken 传递、IDisposable 释放、
单机零感知红线是否被破坏。
```

### 6.4 联机实测记录模板（测完填进 §5 日志）

```
| 日期 | 执行人 | 切片N | 联机实测：<场景> | 通过/失败 | 失败现象与日志位置 |
```

### 6.5 Kimi Code 开工模板（切片 1 实测版，直接粘贴）

> 用法：在 Kimi Code 中打开仓库根目录 `E:\Program Files\better-genshin-impact-LCB`，新建会话，整段粘贴。后续切片只改【切片编号+名称】和"本切片范围"一段即可。

````
你是 BGI（Better Genshin Impact）项目"联机助手"重构的执行工程师，工作区就是当前仓库。

## 第一步：读文档（按顺序，不许跳）

1. `联机助手重构总控计划.md` —— 全文。§3 硬性纪律、§4 切片定义、§5 进度日志。
2. `联机助手通信架构标准化方案.md` —— §1 现状基线、§3 模块一全部、§5 统一约束、§7 验收矩阵。
3. 总控计划 §2.3 列出的领域知识库中与本切片相关的：`.agents/knowledge/domains/ipc-signalr.md`。

## 第二步：确认环境

- `git branch --show-current` 必须是序号最大的 `main-OldTeaBag-*` 分支，不是就先报告并停下。
- 当前路线：货冻结、路先行（总控计划 §4 顶部说明）。本切片只做"路"。

## 第三步：执行【切片 1：模块一 BgiExternalInterface + 助手 SDK】

本切片范围（严格按《通信方案》§3，不得扩大）：
1. BGI 侧新建 `BetterGenshinImpact/Service/ExternalInterface/` 目录，实现 ext.hello 握手
   （在已上线的 ping 会话校验通道上演进，见 IpcClient.VerifyRemoteSessionAsync）、
   幂等窗口（§3.5）、三通信面（§3.6）、ext.event 事件推送（只在有订阅者时启动观察器）。
2. 挂载点 ≤3 处单行，现有 22 个 IPC 操作行为逐字节不变。
3. 助手侧新建 `BgiExternalClient` SDK 封装 ext.* 调用，IpcClient/CommandExecutor 旧路径保留作降级。
4. 验收：编译 0 error + 《通信方案》§7 的 A1~A6 逐项自检 + 新助手连老 BGI 自动降级无报错。

## 硬性要求

- 遵守总控计划 §3 全部红线（帧格式不改、TaskSemaphore 只读轮询、Dispatcher.InvokeAsync、
  单机零感知、CommandExecutor 降级逻辑逐条保留）。
- 每个逻辑单元独立 commit，commit message 格式：`feat(联机助手): 切片1-……`，不要一次性大杂烩提交。
- 遇到设计文档与代码冲突：以代码为准，先报告冲突点，等我确认再继续，不要擅自改设计。
- 完成后：编译验证 `dotnet build BetterGenshinImpact.sln -c Debug`（程序占用编不过就注明放弃），
  更新总控计划 §5 进度日志（日期/切片/动作/结果/遗留）和 §4 切片状态。
- 最后输出：改动文件清单、每个验收点的自证、遗留问题。
````

### 6.6 Kimi Code 使用要点（给 owner 的操作说明）

1. **一个会话只做一个切片**，做完验收、更新日志、关会话。切片中途干不完 → 用 §6.2 模板开新会话续作。
2. Kimi Code 里直接让它跑编译命令；报"程序占用"就按纪律放弃编译验证并在日志注明。
3. 每个切片完成后，建议再开一个**独立审查会话**（§6.3 模板）复核一遍再合并/继续。
4. 如果 Kimi Code 的会话上下文不够长，提示词里"第一步读文档"不要省——文档就是它的记忆。

---

## 7. 术语速查

| 术语 | 含义 |
|---|---|
| 路 | 通信协议标准化（《联机助手通信架构标准化方案.md》：模块一 IPC + 模块二 SignalR） |
| 货 | 业务逻辑重构（《联机助手架构审核与重构方案.md》v2：权威化、Actor 化、状态机） |
| 茶包版 | 本地 `main-OldTeaBag-*` 前缀分支，序号最大者为当前主干 |
| 公版 | `origin-lcb/main`（fork 自上游 babalae/better-genshin-impact 的纯公版主线） |
| 模块一 / BgiExternalInterface | BGI 侧对外 IPC 标准化模块（ext.* 操作 + 事件推送） |
| 模块二 / ServerGateway | 服务器 SignalR 标准化（GatewayHub：Dispatch/Query + evt/state） |
| 双脑计数 | 同一业务计数客户端、服务端各记一半账的现状（经验上限为代表） |

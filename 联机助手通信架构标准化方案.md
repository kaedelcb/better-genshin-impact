# 联机助手通信架构标准化方案

> 文档定位：本文是《联机助手架构审核与重构方案.md》（下称《审核方案》）的**配套落地设计**。
> 《审核方案》回答"为什么重构、往哪个方向重构"（权威服务器、状态机、事件驱动三阶段）；
> 本文回答"两条通信链路具体怎么标准化"：
>
> - **模块一 BgiExternalInterface**：BGI ↔ 联机助手 的本地 IPC 链路标准化（可独立合并到公版/任意 BGI 版本）。
> - **模块二 ServerGateway**：联机助手/BGI ↔ 协调服务器 的 SignalR 链路标准化（替代 65 个 Hub 方法，v2.1 复核时从 57 增至 65）。
>
> 方法论参考：LSP（Language Server Protocol）、DAP（Debug Adapter Protocol）。
> 所有设计点均已对照当前代码核实（核实日期 2026-09-04，分支茶包版主干；v2.1 复核 2026-09-04 晚：止血 P0-A/P1-F 已落地、IPC 新增 ping 跨会话握手、新增 4 个 config.* 远程编辑操作、日志三件套新增 8 个 Hub 方法——相关章节已就地更新）。
>
> **实施状态注记（2026-09-06，切片 8）**：BGI 侧模块二客户端已落地——`CoordinatorClient` 换芯到 `/gateway` + evt 信封（`Multiplayer/Gateway/` 新目录），服务器零改动。本文涉及 `CoordinatorClient.cs` 行号的"现状"描述（如 :167-174 URL、:182-302 事件订阅、:316 重连）均为**迁移前快照**，保留作设计依据；当前代码以 `CoordinatorClient.cs` 顶部切片 8 注释与总控计划 §4/§5 为准。**（2026-09-06，切片 9：MHA 侧 `SignalRClient` 亦已换芯——`MultiplayerHoeingAssistant/Services/Gateway/` 新目录三文件[信封/常量/MhaGatewayClient]，SignalRClient 公开面零变化；本文涉及 `SignalRClient.cs` 行号的描述同为迁移前快照。）**

---

## 0. 前置说明：输入文档核对结果

任务输入中列出的 5 份文档，实际在仓库中的核对结果如下（**本文已用等效资料 + 直接读源码补齐**）：

| 任务中列出的文档 | 实际情况 | 本文采用的信息源 |
|---|---|---|
| ① `联机助手架构审核与重构方案.md` | 存在（693 行，仓库根目录） | 直接阅读 ✓ |
| ② `BetterGenshinImpact 架构与实现模式v2.md` | 文件名不存在；等效文件为 `.agents/rules/bgi-implementation-patterns-v2.md` | 直接阅读 ✓ |
| ③ `联机助手 CommandExecutor 深度分析.md` | **未找到** | 改读源码 `MultiplayerHoeingAssistant/Services/CommandExecutor.cs`（440 行）全文 ✓ |
| ④ `BGI 内部系统分析：CancellationContext、TaskSemaphore 与任务执行机制.md` | **未找到** | 改读源码 `Core/Script/CancellationContext.cs`（158 行）全文、`GameTask/Common/TaskControl.cs`、`GameTask/TaskRunner.cs`，及知识库 `.agents/knowledge/domains/ipc-signalr.md` §4 ✓ |
| ⑤ `BGI IPC 机制深度分析：InstanceService 与 InstanceConnection.md` | **未找到** | 改读源码 `Service/Instance/` 全目录（InstanceIpcProtocol.cs 347 行 / InstanceService.cs 640 行 / InstanceConnection.cs 400 行 / MessageHandlers/InstanceRequestHandler.cs 1342 行），及设计文档 `Docs/design/multi-instance-ipc.md` ✓ |

同时核实了《审核方案》中的若干规模数据（用于本文证据基线）：

| 《审核方案》声称 | 实测 | 结论 |
|---|---|---|
| CoordinatorHub "30+ / ~40+ Hub 方法" | 初版实测 **57 个**；v2.1 复核 **65 个** `public` 方法 | 方向正确，膨胀仍在加速，本文按 65 计 |
| Room.cs 228 行 | **257 行** | 略有增长，结论不变 |
| RoomManager 1490 行 | v2.1 复核 **1575 行**（+85：日志订阅表） | 基本一致 |
| MainViewModel 4900 行 | v2.1 复核 **5398 行**（当日 +497 行） | 持续增长，切片 5 瘦身紧迫性上升 |
| AutoHoeingTask 5000+ 行 | 5000 行 ✓ | 一致 |
| CoordinatorClient 1531 行 | 1531 行 ✓ | 一致 |
| MultiplayerCoordinator 817 行 | 817 行 ✓ | 一致 |

---

## 1. 现状证据基线（代码核实后）

### 1.1 两条通信链路的现状

**链路 A：BGI ↔ 助手（NamedPipe IPC）**

- 管道名 `BetterGI.v2.user-{Windows用户SID}.root`（`MultiplayerHoeingAssistant/Services/IpcClient.cs`），按 Windows 用户隔离，同一台机器多 Session 共享根管道。**（v2.1：已上线跨会话 ping 握手校验——连接成功后比对对端 windowsSessionId，跨会话/无法确认一律不可信，见 `IpcClient.VerifyRemoteSessionAsync` 与 `CommandExecutor.CheckCrossSessionBlock`）**
- 帧格式 `[uint32 payloadLength][byte payloadType][payload]`，length 只计 JSON 字节、流中实际占 `length+1`（`BetterGenshinImpact/Service/Instance/InstanceIpcProtocol.cs:113-347`）。该格式曾让助手侧踩过两次坑（漏 type 字节、length+1 截断，见 `.agents/knowledge/domains/ipc-signalr.md` §1.2）。
- 信封 `InstanceIpcEnvelope { version, requestId, operation, success, errorCode, errorMessage, data }`，当前 `Version = 2`（`InstanceIpcProtocol.cs:37-115`）。
- 已有操作 22 个（`InstanceOperations`，`InstanceIpcProtocol.cs`，不含 `response`；v2.1 新增远程配置编辑 4 个：`config.pull_group / config.open_remote_editor / config.remote_editor_result / config.apply_group`），其中任务控制类 9 个：`task.start / task.stop / task.status / task.suspend / task.resume / config.list / action.execute_hotkey / action.close_game / config.set_task_enabled`。
- 分发入口 `InstanceRequestHandler.HandleAsync` 的 switch（`InstanceRequestHandler.cs:66-103`），未知操作返回 `unsupported_operation`——**这是天然的协议扩展点，新增操作不破坏旧客户端**。
- **单向请求-响应，无推送**：助手对 `task.status` 做 10 秒轮询 + `onlineGeneration` 边沿检测来发现"联机锄地上线"事件（`InstanceRequestHandler.cs:774-775`）。
- 助手侧每次命令新建 `IpcClient` 连接（`CommandExecutor.cs` 各方法 `using var ipcClient = new IpcClient()`），无长连接。
- BGI 侧 AcceptLoop 在 `IOException` 时**整体退出监听循环**（`InstanceService.cs:337-370` 的外层 catch 只记日志不重启循环）——这是已实测发生过的故障（助手每 10 秒 `TimeoutException` 的根因之一，见 `.agents/memory/project-experience.md` IPC 诊断条目）。**（2026-09-04 止血 P0-A 已修复：catch 移入循环内，单连接异常仅记日志 + 300ms 退避后继续监听，失败管道实例丢弃重建；阶梯退避留待重构期）**

**链路 B：助手/BGI ↔ 服务器（SignalR）**

- `CoordinatorHub` 65 个 public 方法（`BgiCoordinatorServer/Hubs/CoordinatorHub.cs`，3179 行；v2.1 复核时从 57 增至 65，日志三件套 +8），无协议版本字段，版本兼容靠 `VersionCompatibilityDecisions` 字符串比较 + try/catch。
- BGI 侧客户端 `CoordinatorClient.ConnectAsync` 直接传完整 Hub URL（`CoordinatorClient.cs:167-174`）；助手侧 `SignalRClient.ConnectAsync` 手动拼 `{serverUrl}/hub`（`SignalRClient.cs:58`）——**同一服务器两套 URL 约定**，是已踩过的配置坑（`.agents/knowledge/domains/ipc-signalr.md` §2）。
- 重连：`WithAutomaticReconnect(0s/2s/10s/30s)`；重连成功后客户端**只重新 JoinRoom/JoinControlRoom，不做状态重同步**（`CoordinatorClient.cs:316`、`SignalRClient.cs:87-102`）。**（v2.1：助手侧已落地 P1-F——内置重连耗尽触发 Closed 后启动 30s 低频自愈，对同一连接 StartAsync，成功后重新入房；严格避开"与内置重连并存"的历史竞态。BGI 侧 CoordinatorClient 尚无对应自愈。）**
- 事件订阅方式：`connection.On<EventName>` 逐事件注册，BGI 侧 20+ 个事件名（`CoordinatorClient.cs:182-302`），新增功能必须同时加 Hub 方法 + 事件名 + 双端处理器。
- 已有良好实践（必须保留）：断线门控 + 不 throw（`SignalRClient.cs:153-171`）、先订阅后动作（`.agents/knowledge/domains/multiplayer-hoeing.md` §4.1）、Closed 事件必须观察异常参数（`SignalRClient.cs:104-122`）。

### 1.2 BGI 内部任务控制机制（模块一的桥接对象）

| 机制 | 位置 | 关键事实 |
|---|---|---|
| 全局取消上下文 | `Core/Script/CancellationContext.cs` | 单例。`Cancel()`（L96）置 `WasCancelled=true` 并取消令牌；`ManualCancel()`（L65）额外置 `IsManualStop`；`CancelTokenOnly()`（L121）取消旧令牌并**重建新 Cts 但不清 WasCancelled**（IPC task.start 专用）；`Register()`（L54）链接外部 CTS；`Set()`（L45）任务开始时全量重置 |
| 单任务锁 | `GameTask/Common/TaskControl.cs:36` | `public static readonly SemaphoreSlim TaskSemaphore = new(1,1)`。`TaskRunner` 用 `WaitAsync(0)` 抢锁，运行期 `CurrentCount==0`。外部等锁释放的**唯一安全姿势是只读轮询 `CurrentCount`**（200ms 轮询 + 15s 兜底，见 `InstanceRequestHandler.cs:540-545`），不能 `WaitAsync` 抢锁（会死锁/抢跑） |
| 任务执行 | `GameTask/TaskRunner.cs`、`Core/Script/ScriptService.cs` | `ScriptService.RunMulti(projects, groupName)` 内部 new TaskRunner 抢同一把锁；`RunMulti` 是异步长任务，**必须在 Dispatcher 上调起但用 `TaskCompletionSource` 等待真正完成**（`InstanceRequestHandler.cs:566-621` 是标准范式） |
| UI 线程 | WPF STA | 所有触碰 ViewModel/配置/任务启动的代码必须 `Application.Current.Dispatcher.InvokeAsync`（**不能用同步 `Invoke`**，会阻塞消息泵导致全局键盘钩子延迟，`InstanceRequestHandler.cs:627` 注释）；纯文件 I/O（读配置组 JSON）在后台线程做（`InstanceRequestHandler.cs:550-556`） |
| 状态读取 | `RunnerContext.Instance.taskProgress` / `TaskContext.Instance().CurrentScriptProject` / `AutoHoeingProgress`（静态，`lock(AutoHoeingProgress.Sync)`）/ `NotifyOnlineTask.CurrentGeneration` | 分散在 5 个全局单例/静态类，`HandleTaskStatus`（`InstanceRequestHandler.cs:680-783`）是当前唯一的聚合读取点，且已处理了残留清理（L708-718）、30 秒 recent 窗口（L737-750）等坑 |

---

## 2. 参考协议方法论：LSP 与 DAP 的提炼

### 2.1 LSP 提炼

LSP 3.17 的基础协议与本项目 IPC 高度同构，可直接借鉴四点 [LSP Specification 3.17](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/ "citation")：

1. **消息三分类**：Request（必须有 Response）/ Response / Notification（无响应，fire-and-forget）。本项目当前 IPC 只有 Request/Response 两类，缺 Notification——事件推送正是 Notification 语义。
2. **`initialize` 握手 + 能力协商（capabilities）**：客户端首请求必须是 `initialize`，携带 `ClientCapabilities`；服务端返回 `ServerCapabilities`；握手完成前双方不得发送其它消息（服务端对提前到达的 request 返回错误码 `-32002`，notification 直接丢弃）。**能力用结构化布尔/枚举字段逐项声明，而不是版本号字符串比较**。
3. **`$/cancelRequest` 带外取消**：取消是一个独立的 Notification，携带被取消请求的 id；**被取消的请求仍然必须返回响应**（不得悬挂），取消错误用专用错误码 `RequestCancelled`。这条规则直接解决"助手发了 task.start 又想撤回"的悬挂问题。
4. **进度汇报与请求解耦**：`workDoneToken` + `$/progress` 通知，进度是挂在某个请求上的通知流，与请求-响应主通道分离。

### 2.2 DAP 提炼

DAP 与 LSP 同族（同样的 header + JSON 帧），但面向"控制一个长生命周期执行体"，比 LSP 更贴近"助手控制 BGI"的场景 [DAP Specification](https://microsoft.github.io/debug-adapter-protocol//specification.html "citation"), [DAP Overview](https://microsoft.github.io/debug-adapter-protocol//overview.html "citation")：

1. **无版本号演进，纯 capabilities 标志**：DAP 明确声明"协议至今仍是第一版"，靠"每个新功能一个布尔 capability 标志，缺省即不支持"实现完全向后兼容。这是替代当前 `VersionCompatibilityDecisions` 字符串比较的直接方法论。
2. **三类消息 + 单调递增 `seq`**：request / response / **event**，每条消息带 seq。event 是服务端主动推送的第一类公民（`stopped`、`continued`、`output`……）——对应本项目"BGI 主动推送状态变更"。
3. **严格的生命周期时序**：`initialize` → adapter 回 capabilities → adapter 发 `initialized` event → 客户端下发配置 → `configurationDone` → `launch`/`attach`。**"初始化完成"是一个显式事件而非隐式假设**——对应本项目"连接建立后何时可以开始下发命令"。
4. **反向请求（reverse request）**：adapter 可以反过来向客户端发请求（如 `runInTerminal`）。对应本项目"BGI 请求助手确认/展示"的场景，属于可选远期能力。
5. **取消也是请求**：`cancel` request + `supportsCancelRequest` capability，能力未声明则客户端不得发送。

### 2.3 方法论 → 本项目的设计原则映射

| # | 原则 | 来源 | 模块一（IPC） | 模块二（SignalR） |
|---|---|---|---|---|
| P1 | 消息分三类：Request/Response/Event(Notification) | LSP/DAP | 新增 `event` 操作实现 BGI→助手推送 | 收敛为 `Dispatch`/`Query` 两个 Hub 方法 + `evt`/`state` 两个回调 |
| P2 | 连接先握手，能力用布尔标志集 | LSP initialize / DAP capabilities | 新增 `ext.hello` 操作 | 连接后首消息 `ClientHello`，服务端回 `ServerHello{capabilities[]}` |
| P3 | 信封版本只用于**帧/信封破坏性变更**，功能演进走 capability | DAP | `version` 冻结在 2（`InstanceConnection.cs:218` 严格校验），增量全部走 `ext.*` operation + hello capabilities | `protocolVersion` 同理冻结，功能协商走 caps |
| P4 | 每个请求带幂等键，重复投递安全去重 | 通用 + 现有 generation 实践 | 泛化现有 `_lastExecutedTask` generation 去重为统一幂等窗口 | 服务端按 `idempotencyKey` 去重 |
| P5 | 取消必须"有响应、不悬挂" | LSP `$/cancelRequest` | `task.stop` 语义保留，长任务响应必须最终返回（started/cancelled/failed） | 命令响应异步化：ACK 立即返回，终态走 event |
| P6 | 状态同步 = 快照 + 增量（revision） | LSP 文档同步模型 | `task.status` 保留为快照查询；事件流携带 `stateRevision` | RoomState 带 revision，广播 delta，断线按 revision 重同步 |
| P7 | 连接生命周期显式化：Connecting→Handshaking→Ready→Degraded→Closed | DAP 时序 | 新增连接状态机，助手 `IsConnected` 细化 | 复用 SignalR 状态但包装成统一状态机 |
| P8 | 未声明能力的功能不得使用，降级不报错 | DAP | 助手读 caps 决定 UI 可用性 | 客户端按 caps 降级（如服务端不支持增量同步则回退轮询快照） |

---

## 3. 模块一：BgiExternalInterface（BGI 侧独立外部接口模块）

### 3.1 设计目标与非目标

**目标**：

1. 一个**目录级自包含**的模块，可以作为一个独立 commit 优选/合并到公版（`origin-lcb/main`）或任意 BGI 版本——合并时应表现为"纯新增文件 + 少量挂载点插入"，符合 §8.3 公版合并姿势（`.agents/rules/bgi-implementation-patterns-v2.md`）。
2. 统一封装 BGI 内部系统（`CancellationContext` / `TaskSemaphore` / `TaskRunner` / `ScriptService` / `OneDragonFlowViewModel` / `RunnerContext` / `TaskContext` / `AutoHoeingProgress` / `NotifyOnlineTask`）的全部外部可触碰点，**外部进程只允许与本模块对话，不允许再各自直接摸内部单例**。
3. 三个通信面齐全：**控制流**（命令）、**任务流**（状态/进度查询）、**CT 控制**（取消/挂起/恢复/代际幂等），外加**事件流**（BGI 主动推送，消灭 10 秒轮询）。
4. 连接容错与优雅降级：BGI 未启动 / IPC 不可用 / 能力不支持，都有定义好的行为。

**非目标**：

- 不改 BGI 内部任务引擎的任何行为（单机零感知）。
- 不替换现有 `Service/Instance/` 多实例体系（根管道、WebView、相对鼠标），本模块**寄生在其传输层之上**。
- 不在本模块内实现联机协调逻辑（那是链路 B / 服务器的职责）。

### 3.2 分层架构

```
┌──────────────────────────── 外部进程（联机助手 / WebView / 任意工具） ───────────────────────────┐
│  BgiExternalClient SDK（助手侧新封装，替代 IpcClient + CommandExecutor 的裸调用）                  │
└──────────────────────────────────────┬───────────────────────────────────────────────────────┘
                                       │ NamedPipe（复用现有根管道与帧格式）
┌──────────────────────────────────────▼───────────────────────────────────────────────────────┐
│ BetterGenshinImpact/Service/ExternalInterface/        ← 新增目录，自包含，可独立合并             │
│  ┌───────────────────────────────────────────────────────────────────────────────────────┐  │
│  │ L1 协议层  ExternalInterfaceProtocol.cs                                                │  │
│  │   - 复用 InstanceIpcProtocol 帧读写                                                    │  │
│  │   - ext.hello 握手 / capability 集 / 幂等窗口 / 错误码                             │  │
│  ├───────────────────────────────────────────────────────────────────────────────────────┤  │
│  │ L2 会话层  ExternalInterfaceSession.cs                                                 │  │
│  │   - 每连接一个会话：状态机(Connecting→Handshaking→Ready→Degraded→Closed)                │  │
│  │   - 请求路由（按 operation 前缀分发到三个平面）                                          │  │
│  │   - 事件订阅管理 + 事件推送（复用 InstanceConnection.WriteJsonAsync 写锁）                │  │
│  ├───────────────────────────────────────────────────────────────────────────────────────┤  │
│  │ L3 平面处理器（每个一个文件，无相互依赖）                                                 │  │
│  │   CommandPlane.cs   task.start/stop/suspend/resume, action.*, config.set_task_enabled │  │
│  │   QueryPlane.cs     task.status, config.list                                          │  │
│  │   EventPlane.cs     event.subscribe/unsubscribe + 事件源桥接                             │  │
│  ├───────────────────────────────────────────────────────────────────────────────────────┤  │
│  │ L4 内部桥接层  Bridges/                                                                │  │
│  │   CancellationBridge.cs / TaskLockBridge.cs / DispatcherBridge.cs / StateReadBridge.cs │  │
│  │   - 唯一允许 using 内部单例的一层；对上只暴露强类型接口                                   │  │
│  └───────────────────────────────────────────────────────────────────────────────────────┘  │
│  挂载点（对现有代码的唯一插入，≤3 处）：                                                       │
│  ① InstanceRequestHandler.HandleAsync 的 switch 中新增 1 个 case：                           │
│     operation 以 "ext." 前缀开头 → ExternalInterfaceSession.RouteAsync(...)                  │
│  ② App.xaml.cs 服务注册一行：services.AddSingleton<ExternalInterfaceEventHub>()              │
│  ③ （可选）任务引擎事件源挂点：见 §3.6 事件桥接表                                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

**为什么用 `ext.` 前缀走统一入口，而不是把每个新操作加进现有 switch**：保持 `InstanceOperations`（公版共享文件）的 diff 最小化；公版合并时冲突面 = switch 中一行 + 注册一行，其余全部是纯新增文件。

### 3.3 传输层：完全复用，不改一个字节

- 管道名、ACL、帧格式、`MaxPayloadLength = 1 MiB`、信封字段——全部沿用 `InstanceIpcProtocol`（`InstanceIpcProtocol.cs:113-347`）。助手侧 `IpcClient` 的帧读写（`IpcClient.cs:64-97`）已验证正确，SDK 直接平移其逻辑。
- **信封 `version` 保持 2 不变，版本协商走 `ext.hello` 的 capabilities**（切片 1 落地时对本节原设计的修正）：`InstanceConnection.cs:218` 对入站帧做严格 `version == 2` 校验，升 v3 会导致老 BGI 直接丢帧而非返回 `unsupported_operation`，反而失去版本探测能力。因此外部接口的增量（`ext.hello` 握手 + `ext.event` 推送帧 + 错误码表）全部封装在 v2 信封的 `operation`/`data` 内：老 BGI 收到未知 `ext.hello` → 走现有 default 分支返回 `unsupported_operation` → 助手据此判定对端能力并降级为 v2 轮询模式（**这本身就是优雅的版本探测**）；新 BGI + 老助手（不发 hello、只用 v2 操作）零影响。此后功能演进只加 capability，信封 version 冻结在 2，除非未来发生帧级破坏性变更（原则 P3）。

### 3.4 握手与能力协商（`ext.hello`）

```jsonc
// → 请求（连接建立后必须第一个发送；LSP 规则：握手完成前不得发其它请求）
{ "version": 2, "requestId": "...", "operation": "ext.hello",
  "data": { "clientName": "MultiplayerHoeingAssistant", "clientVersion": "1.4.0",
            "requiredCapabilities": ["event.push"] } }

// ← 响应
{ "version": 2, "requestId": "...", "operation": "response", "success": true,
  "data": {
    "protocolVersion": 2,
    "bgiVersion": "0.51.0",
    "capabilities": {
      "task.start": true, "task.stop": true, "task.suspend": true, "task.resume": true,
      "task.status": true, "config.list": true,
      "action.executeHotkey": true, "action.closeGame": true, "config.setTaskEnabled": true,
      "event.push": true,            // 支持事件订阅推送（外部接口核心新增）
      "event.taskProgress": true,    // 任务进度事件粒度
      "event.hoeingProgress": true,  // 联机锄地进度事件
      "idempotency.window": true     // 服务端幂等去重窗口
    } } }
```

- **能力缺省即不支持**（DAP 规则）：助手读到 `event.push` 缺失/为 false → 自动降级为 10 秒轮询 `task.status`，UI 功能不报错只降档。
- **（v2.1）落地起点已存在**：止血期上线的 `ping` 会话校验握手（`IpcClient.VerifyRemoteSessionAsync` 比对 windowsSessionId）就是 ext.hello 的最小雏形——ext.hello 应在该通道上演进（复用连接后第一帧的时序约定，扩展 capabilities/sessionId 字段），而不是另起一套握手。
- 对端是老版本 BGI（无 `ext.hello`）→ 收到 `unsupported_operation` → 同样降级。**新版本助手 + 老版本 BGI 零报错共存**；新 BGI + 老助手（不发 hello，直接用 v2 操作）也零影响，因为现有 22 个操作全部保留。
- 握手响应同时携带 `sessionId`（GUID），事件帧与后续重连去重都挂在这个会话上。

### 3.5 幂等设计（泛化现有 generation 实践）

现状：`HandleTaskStart` 只对 `generation + taskName` 做去重（`InstanceRequestHandler.cs:504-520`），其它操作（stop/suspend/resume/hotkey）无幂等保护，网络重试/双击会重复执行。

标准化：

1. **所有写操作请求可携带 `data.idempotencyKey`（string）**。缺省时服务端按 `requestId` 自然幂等（同一信封重发去重）。
2. 服务端维护**幂等窗口**：`ConcurrentDictionary<string, (DateTime seenAt, InstanceIpcEnvelope response)>`，TTL 60 秒。命中重复 key → **直接重放缓存的响应**，不再执行。
3. `task.start` 的 generation 语义保留（它跨连接、跨批次，幂等窗口替代不了），实现上把 `_lastExecutedTask` 逻辑下沉为 `CommandPlane` 内的 `GenerationDedupStore`，行为逐字节不变。
4. 幂等窗口命中日志带 `[IDEMPOTENT_REPLAY]` 前缀，便于排查"为什么命令没执行"。

### 3.6 三个通信面与事件类型

**控制面（Command，Request/Response）**——操作名加 `ext.` 前缀，参数/语义与现有实现对齐：

| 新操作 | 语义 | 现有代码映射 |
|---|---|---|
| `ext.task.start` | 启动配置组/一条龙（含 startFromIndex、generation） | `InstanceRequestHandler.HandleTaskStart`（L495-678）的完整流程：generation 去重 → `CancelTokenOnly` → 轮询 `TaskSemaphore` → Dispatcher 起 `RunMulti` → TCS 等完成 → 检查 `WasCancelled` 返回 cancelled |
| `ext.task.stop` | 取消当前任务 | `HandleTaskStop`（L481-493）：`CancellationContext.Cancel()` |
| `ext.task.suspend` / `ext.task.resume` | 中断保存上下文 / 恢复 | `HandleTaskSuspend`（L1066）/ `HandleTaskResume`（L1199） |
| `ext.action.executeHotkey` | 触发热键动作 | `HandleExecuteHotkey`（L925） |
| `ext.action.closeGame` | 关闭游戏 | `HandleCloseGame`（L976） |
| `ext.config.setTaskEnabled` | 启停配置项 | `HandleSetTaskEnabled`（L990） |

**查询面（Query，Request/Response）**：

| 新操作 | 语义 | 现有代码映射 |
|---|---|---|
| `ext.task.status` | 任务状态快照 | `HandleTaskStatus`（L680-783）全部字段原样返回（running/wasCancelled/taskName/groupName/autoHoeingRunning/autoHoeingProgress/currentRouteDisplay/recentTaskName/onlineGeneration/hasSuspendedTaskContext），**新增 `stateRevision` 字段**（单调递增，见事件面） |
| `ext.config.list` | 配置组/一条龙清单 | `HandleConfigList`（L785） |

**事件面（Event，服务端→客户端 Notification，外部接口核心新增）**：

- 订阅：`ext.event.subscribe { events: ["task.started","task.progress","task.stopped","hoeing.progress","online.triggered","task.suspended","task.resumed"] }`；`ext.event.unsubscribe`。
- **（切片4 落地补注）断线续传**：`ext.event.subscribe` 可携带 `lastKnownRevision`（需 hello 能力位 `event.replay`）——服务端从近因环形缓冲（有界 500 条，`ExternalInterfaceEventHub`）补发缺失事件（先补发后注册订阅者，与实时推送零交叠）；缺口超出缓冲/版本号回退（BGI 重启）→ 响应 `resyncRequired: true`，客户端拉 `ext.task.status` 快照校准。订阅响应带 `currentRevision / replayed / resyncRequired`。SDK 侧另有两道兜底：事件 revision 跳号自动拉快照、快照覆盖线去重（rev ≤ floor 不分派）。
- 事件帧：信封 `operation = "ext.event"`，`data = { event, stateRevision, timestampUtc, payload }`。**每条事件都携带 `stateRevision`**——它是 `ext.task.status` 快照的同源版本号（切片4：快照 revision 前置取样，保证"快照版本 ≤ 已发布事件"的安全方向），客户端发现 revision 跳号 → 主动拉一次快照补齐（LSP 文档同步模型）。
- 推送写入复用 `InstanceConnection.WriteJsonAsync`（`_writeLock` 已保证单连接写串行，`InstanceConnection.cs:104-120`）。

事件与 BGI 内部事件源的桥接映射（每个事件一行挂载点，不改引擎行为）：

| 事件 | 触发源（代码位置） | 桥接方式 |
|---|---|---|
| `task.started` / `task.stopped` | `TaskControl.TaskSemaphore.CurrentCount` 0↔1 边沿 + `CancellationContext.WasCancelled` | EventPlane 内 200ms 轻量观察器（只读 CurrentCount，同 task.start 等锁姿势）做边沿检测；停止事件 payload 带 `wasCancelled` |
| `task.progress` | `RunnerContext.Instance.taskProgress`（`InstanceRequestHandler.cs:694-700` 同款读取） | 观察器检测到 `CurrentScriptGroupProjectInfo` 变化时推送 |
| `hoeing.progress` | `AutoHoeingProgress` 静态类（`InstanceRequestHandler.cs:687,723-735`，读取需 `lock(AutoHoeingProgress.Sync)`） | 观察器 1s 节流推送 `currentRouteDisplay` 变化；**不新增对 AutoHoeingProgress 的写** |
| `online.triggered` | `NotifyOnlineTask.CurrentGeneration` / `LastTriggeredAt`（`InstanceRequestHandler.cs:746-749,774-775`） | generation 边沿推送，替代助手轮询边沿检测 |
| `task.suspended` / `task.resumed` | `TaskContext.Config.SuspendedTaskContext` 写入/清除点（`HandleTaskSuspend` L1066 / `HandleTaskResume` L1199 内部） | 两个 handler 成功路径末尾调 `ExternalInterfaceEventHub.Publish(...)`（这是仅有的 2 处"插入现有文件"的挂载点之一，各 1 行） |

> 轮询观察器只是**过渡实现**（不改引擎、零回归）。远期在 `TaskRunner`/`RunnerContext` 内部加 `event Action` 钩子后可改为真推送，接口不变。

### 3.7 CT（CancellationToken）控制桥接——具体代码映射

| 桥接点 | 现有代码 | 模块内封装 | 规则 |
|---|---|---|---|
| 软取消（外部命令停止） | `CancellationContext.Cancel()`（`CancellationContext.cs:96`） | `CancellationBridge.RequestStop()` | 置 `WasCancelled=true`，任务层抛 OCE；**响应必须等任务真正退出后才返回终态**（原则 P5），实现 = Cancel 后轮询 `TaskSemaphore.CurrentCount==1`（15s 兜底） |
| 换批取消（不清 WasCancelled） | `CancelTokenOnly()`（L121） | `CancellationBridge.InterruptForNewBatch()` | 仅 `ext.task.start` 内部使用，外部不暴露 |
| 手动停止标记 | `ManualCancel()`（L65） | 不暴露 | 仅 F11/热键路径使用；外部命令一律走 `Cancel()`，保证 `task.status.wasCancelled` 语义统一 |
| 外部 CTS 链接 | `Register(externalToken)`（L54） | `CancellationBridge.RegisterLinked(ct)` | 长等待型请求（如等锁）把连接级 CTS 链接进全局上下文，客户端断连即中断等待 |
| 取消状态查询 | `WasCancelled` / `IsCancellationRequested` / `IsDisposed`（L18-34） | `StateReadBridge` 只读封装 | 读取必须判 `IsDisposed` 在前（现有 `HandleTaskStatus` L684-685 同款防御） |
| 单任务锁 | `TaskControl.TaskSemaphore`（`TaskControl.cs:36`） | `TaskLockBridge.WaitForIdleAsync(timeout)` | **只读轮询 `CurrentCount`**，200ms/15s，严禁 `WaitAsync`（会抢锁死锁，`.agents/knowledge/domains/ipc-signalr.md` §4） |
| 取消竞态 | `Cancel()`/`ManualCancel()` 内已 catch `ObjectDisposedException`（L81-84, L113-117） | 桥接层不吞额外异常 | 注意 `Clear()` 的实现有怪癖（L135-157：lock 前已置 `disposed=true`，lock 内分支恒 return），桥接层**不调用 `Clear()`**，也不依赖其语义 |

### 3.8 线程模型（WPF STA 纪律）

模块内每条规则都有现有代码出处：

1. **管道 I/O 在后台线程**（InstanceConnection 的 ReceiveLoop，`InstanceConnection.cs:70-77`）——L1/L2 层代码禁止触碰任何 WPF 对象。
2. **触碰 UI/ViewModel/任务启动必须 `Dispatcher.InvokeAsync`**（非 `Invoke`，`InstanceRequestHandler.cs:530-535, 567, 628` 是范式；同步 Invoke 会阻塞消息泵拖慢全局键盘钩子）。
3. **纯文件 I/O 留在后台线程**（读配置组 JSON 在 InvokeAsync 之外，`InstanceRequestHandler.cs:550-556`）。
4. **`RunMulti` 完成等待范式**：`TaskCompletionSource` + `Dispatcher.InvokeAsync(async () => { ... await RunMulti ...; tcs.SetResult(); })` + `await tcs.Task`（`InstanceRequestHandler.cs:566-621`），不能等 `InvokeAsync` 返回的 Task（那只等调度完成）。
5. **静态状态读取**：`AutoHoeingProgress` 必须 `lock(AutoHoeingProgress.Sync)`（L723）；`RunnerContext`/`TaskContext` 读取必须 try/catch 包裹（L692-705 范式，单例可能处于半初始化态）。
6. **事件推送从观察器线程发出**，经 `InstanceConnection.WriteJsonAsync` 的 `_writeLock` 串行化；不允许在 Dispatcher 线程上做管道写（避免 STA 上挂起等待 I/O）。

### 3.9 连接容错与优雅降级

| 场景 | 检测 | 行为 |
|---|---|---|
| 助手连不上 BGI（未启动/管道不存在） | `ConnectAsync` TimeoutException（`IpcClient.cs:32-37`） | 助手 SDK 标记 `LinkState.Down`；控制类命令按 CommandExecutor 现有两阶段策略降级（IPC → 杀进程 + 命令行 `--startGroups/--startOneDragon`，`CommandExecutor.cs:164-186,282-302`），该兜底**保留在助手侧**，不进 BGI |
| BGI AcceptLoop 崩溃（已实测：`IOException` 后监听循环整体退出） | `InstanceService.cs:337-370` 外层 catch 只记日志 | **模块必须修复**：AcceptLoop 改为"单连接异常不退出循环"——把 catch 移到循环内，连续失败退避（1s/2s/5s，封顶 30s）后重建 server。这是模块一的必修项，否则事件长连接把该故障概率放大 N 倍。**（2026-09-04 止血 P0-A 已落地基础版：循环内 catch + 300ms 固定退避 + 失败实例丢弃重建；阶梯退避遗留重构期）** |
| 探针"连上立刻断"触发对端异常（历史坑） | `.agents/memory/project-experience.md` IPC 诊断条目 | SDK 的 `WaitForBgiIpcReadyAsync` 保留"发正常命令再优雅关闭"姿势（`CommandExecutor.cs:50-70`） |
| 助手进程崩溃/断连 | 管道句柄断开 | 服务端会话进入 `Closed`，注销事件订阅；**不影响 BGI 任务运行**（CT 控制是即发即弃，不依赖连接存活） |
| 能力不支持 | hello caps / `unsupported_operation` | 按 §3.4 降级链：事件推送→轮询；新命令→UI 禁用对应按钮 |
| BGI 正在跑老助手不认识的命令 | —— | 老操作全部保留，行为逐字节不变（单机零感知底线） |

### 3.10 错误码表（模块一）

沿用信封 `errorCode` 字段，固定取值：

| errorCode | 含义 | 对应现有场景 |
|---|---|---|
| `unsupported_operation` | 未知操作/能力不支持 | 现有默认分支（L99-102） |
| `invalid_request` | 参数错误 | 现有 catch 分支（L109-120） |
| `service_unavailable` | 内部服务缺失 | `scriptService == null`（L524-525） |
| `task_busy` | 等锁超时（15s 仍有任务） | L540-545 兜底超时新显式化 |
| `task_cancelled` | 执行中被取消（F11） | cancelled 返回路径（L668-671） |
| `already_executed` | 幂等命中（generation 重复） | L514-516 |
| `task_start_failed` / `task_stop_failed` / ... | 各操作执行异常 | 现有各 handler 同款 |

### 3.11 IDisposable / 资源释放清单（模块一）

| 对象 | 归属 | 释放责任 | 依据 |
|---|---|---|---|
| `NamedPipeClientStream`（助手侧每命令一连） | 助手 SDK | `using` 或 SDK 长连接的 `Dispose` | `IpcClient.cs:119-123` 现有实现 |
| `InstanceConnection`（BGI 侧每连接） | BGI | `IAsyncDisposable`：断连/服务停止时释放 `_lifetimeCancellationTokenSource`、链接 CTS、管道流 | `InstanceConnection.cs:17-42` 现有实现 |
| 事件订阅（EventPlane） | BGI 会话 | 连接 `Closed` 时必须从 `ExternalInterfaceEventHub` 注销，防止向死连接推送累积 | 新增，订阅表挂会话生命周期 |
| `CancellationTokenSource`（链接 CTS） | 桥接层 | `CancellationBridge.RegisterLinked` 返回值由调用方 `using`；**不许泄漏进 `_externalCtsList` 而不释放**（`CancellationContext.ManualCancel` 会统一释放，但 `Cancel()` 不会——桥接层自己兜底） | `CancellationContext.cs:54-63,86-92` |
| 轮询观察器（Task） | EventPlane | 模块级 CTS 停止；BGI 退出时随 `InstanceService.StopAsync` 取消 | 新增 |

### 3.12 多实例场景（模块一）

- 管道按 **Windows 用户 SID** 命名，同用户多 Session（RDP 分身）共享根管道（`Docs/design/multi-instance-ipc.md`）。模块一不引入新的实例寻址——当前联机助手场景是"一台机器一个 BGI 根实例 + 一个助手"。**（v2.1：共享根管道的串话风险已由 ping 会话校验止血——跨会话连接被标记不可信，状态不采信、控制指令阻断；协议层的 `data.targetInstance` 预留仍是根治方向。）**
- 但协议字段预留 `data.targetInstance`（进程 ID 寻址，复用 WebView 寻址规则），将来"助手控制同用户下的桌面分身 BGI"不需要改协议。
- 助手侧多开：多个助手连同一根管道是天然支持的（每连接独立 `InstanceConnection`）；事件订阅按会话隔离，互不收对方的事件。
- 命令冲突（两个助手同时 `task.start`）：由幂等键 + 单任务锁天然收敛——后到者等锁 → 先取消前者再执行，与现有 `HandleTaskStart` 行为一致，文档化即可。

---

## 4. 模块二：ServerGateway（服务器通信架构）

### 4.1 设计目标

1. 把 `CoordinatorHub` 的 **65 个 public 方法收敛为 2 个入口方法 + 2 个客户端回调**，新增功能只加消息类型，不加 Hub 方法、不加事件名。
2. 事件驱动模型：客户端上报**意图事件**，服务端（权威）广播**状态变更**——与《审核方案》的权威服务器方向对齐，本文只负责"协议与传输"这一层，不重复其 RoomActor/状态机设计。
3. 增量状态同步：房间状态带 revision，广播 delta；断线重连按 revision 重同步（而非仅重新 Join）。
4. 协议版本化 + capability 协商（替代 `VersionCompatibilityDecisions` 字符串比较）。
5. 同时服务两类客户端（BGI 的 `CoordinatorClient`、助手的 `SignalRClient`），统一 URL 约定、统一连接状态机、统一断线门控。

### 4.2 协议结构（GatewayEnvelope）

```csharp
// 传输信封——所有消息共用，SignalR 上就是一个 DTO
public sealed record GatewayEnvelope
{
    public int ProtocolVersion { get; init; } = 3;          // 冻结，破坏性变更才升
    public required string Type { get; init; }              // "command" | "event" | "query" | "response" | "hello"
    public required string Name { get; init; }              // 如 "syncPoint.reportArrival"、"room.stateDelta"
    public string? RequestId { get; init; }                 // command/query 必填，response 原样带回
    public string? IdempotencyKey { get; init; }            // 写类 command 建议必填
    public string? RoomCode { get; init; }                  // 房间域消息必填（替代逐方法透传 roomCode）
    public long? StateRevision { get; init; }               // event/response 携带：产生此消息时的房间 revision
    public JsonObject? Payload { get; init; }               // 业务数据（Newtonsoft，camelCase，与现网一致）
    public DateTime SentAtUtc { get; init; }
}
```

**Hub 收敛为两个服务端方法 + 两个客户端回调**：

```csharp
public class GatewayHub : Hub
{
    // 客户端 → 服务端：命令与事件上报（统一入口，异步 ACK）
    public Task<GatewayEnvelope> Dispatch(GatewayEnvelope envelope);

    // 客户端 → 服务端：只读查询（同步返回）
    public Task<GatewayEnvelope> Query(GatewayEnvelope envelope);
}

// 服务端 → 客户端（IHubContext / Clients.Group 回调）只有两个名字：
//   "evt"   —— GatewayEnvelope(Type="event")，领域事件广播（AllArrived、FightDone、ExpCap……）
//   "state" —— GatewayEnvelope(Type="event", Name="room.stateDelta")，增量状态同步
```

- `Dispatch` 的返回值是 **ACK**（已受理/已拒绝 + 理由），**不是业务终态**。业务终态（如"全员到达"）永远走 `evt` 广播——这就是原则 P5 在 SignalR 上的落地：命令不悬挂、终态可幂等重放。
- `Name` 采用 `<域>.<动作>` 点分命名（`room.create`、`syncPoint.reportArrival`、`exp.reportFightResult`），与 LSP 的 `textDocument/didOpen` 同风格；未知 Name 返回 `unsupported_operation`，新旧端天然互容。

### 4.3 现有 65 个 Hub 方法 → 消息名映射（分组收敛，v2.1）

| 消息族 | 新 Name | 被替代的现有 Hub 方法 |
|---|---|---|
| 房间生命周期 | `room.create / room.join / room.leave / room.close / room.markStarted` | CreateRoom, JoinRoom, LeaveRoom, CloseRoom, MarkRoomStarted, MarkRoomStartedWithProgress |
| 房间查询 | `room.getState / room.listOnline / room.getConfig / room.getRoundHostOrder` | GetOnlineRooms, GetRoomConfig, GetRoundHostOrder, IsHostReady, IsHostRouteListUploaded, GetHostRouteList, GetHostRouteListStatus, GetWorldJoinedCount, GetMemberProgress（全部走 `Query`） |
| 房间配置 | `room.setConfig / room.setWhitelist / room.setHostRouteList / room.reportHostReady` | SetRoomConfig, UpdateWhitelist, SetHostRouteList, ReportHostReady |
| 路线验证 | `route.reportList / route.reportVariantSchema / route.reportVerificationDone` | ReportRouteList, ReportRouteVariantSchema, ReportRouteVerificationDone |
| 同步点 | `sync.reportArrival` | ReportArrival, ReportArrivalWithExpectedCount（expectedCount 收进 payload） |
| 战斗 | `fight.reportParticipant / fight.reportDone` | ReportFightParticipant, ReportFightDone |
| 经验上限 | `exp.reportFightResult`（客户端只报"本局有无经验"，计数上迁服务端，见《审核方案》§6.2.1） | ReportExpCapReached, ReportExpCapCleared, ReportExpArmed, ReportTwoConsecutiveNoExp, ReportTwoConsecutiveNoExpCleared |
| 万叶 | `kazuha.declareCapability / kazuha.notifyCollectStarted` | DeclareKazuhaCapability, SetKazuhaPlayer(废弃), NotifyKazuhaCollectStarted |
| 世界加入 | `world.reportJoined / world.resetJoined / world.resetForNewRound` | ReportWorldJoined, ResetWorldJoined, ResetForNewWorldRound |
| 异常协调 | `anomaly.reportWaitPoint` 等 | WaitPointReport 及 realign 系列（§4.7 迁移期内先进兼容层） |
| 心跳 | `session.heartbeat`（progress 收进 payload） | Heartbeat, HeartbeatWithProgress |
| 控制房间（助手） | `control.joinRoom / control.sendCommand / control.reportStatus / control.confirmAllReady / control.reportOnlineEvent / control.clearOnlineHistory` | JoinControlRoom, SendRemoteCommand, ReportControlStatus, ConfirmAllReady, ReportOnlineEvent, ClearOnlineHistory |
| 日志三件套（v2.1 新增） | `log.reportBatch / log.subscribe / log.unsubscribe / log.requestFiles / log.reportFiles / log.requestDownload / log.reportChunk`（订阅数与文件/块推送走 `evt`：`log.subscribersChanged / log.filesRequested / log.fileList / log.downloadRequested / log.fileChunk / log.batch`） | ReportMemberLogBatch, SubscribeMemberLog, UnsubscribeMemberLog, RequestMemberLogFiles, ReportMemberLogFiles, RequestMemberLogDownload, ReportMemberLogChunk |
| 截图汇聚（v2.1 新增） | `screenshot.report`（推送走 `evt`：`screenshot.member`） | ReportMemberScreenshot |
| 服务端→客户端事件 | `evt` 回调，Name 如 `sync.allArrived`、`fight.allDone`、`room.closed`、`room.playerListChanged`、`exp.allCapReached`、`kazuha.collectStarted`、`anomaly.playerNotified`、`control.remoteCommand`、`control.playersUpdated`、`control.allReady` | 现有 20+ 个 `connection.On<...>` 事件名（`CoordinatorClient.cs:182-302`、`SignalRClient.cs`） |

收敛效果：**65 方法 + 20 余事件名 → 2 方法 + 2 回调 + ~45 个消息名**。消息名是纯数据（字符串 + payload schema），新增不改 Hub 签名，服务端路由表驱动。

### 4.4 握手与能力协商

连接建立（`StartAsync` 成功）后，客户端**第一条消息必须是 hello**（DAP 时序：握手完成前服务端丢弃/拒绝其它消息）：

```jsonc
// → Query({ type: "hello", name: "session.hello", payload: {
//     clientKind: "bgi" | "assistant" | "web", clientVersion, protocolVersion: 3,
//     capabilities: ["room.stateDelta", "exp.serverSideCount"] } })
// ← response payload: {
//     protocolVersion: 3, serverVersion,
//     capabilities: ["room.stateDelta","exp.serverSideCount","control.v2", ...],
//     minimumClientProtocol: 2 }          // 低于此版本直接拒绝并给原因
```

- 能力缺省即不支持；客户端按 caps 决定行为（如服务端无 `exp.serverSideCount` → 客户端走旧的自计数上报路径——**这正是新旧混跑的降级开关**）。
- `VersionCompatibilityDecisions`（`BgiCoordinatorServer/Services/VersionCompatibilityDecisions.cs`）保留为 hello 内部的一个判定输入（老客户端 hello 缺失时按 v2 走旧路径），但**新代码不再新增字符串版本比较**。

### 4.5 增量状态同步（revision + delta）

房间状态（`Room`，257 行，50+ 字段）标准化为一个带版本的状态视图：

```csharp
public sealed record RoomStateDelta
{
    public required string RoomCode { get; init; }
    public required long Revision { get; init; }        // 单调递增，每事件 +1
    public required JsonObject Changes { get; init; }   // JSON Merge Patch 语义：只含变化字段
    public bool IsSnapshot { get; init; }               // true = 全量（首同步/重同步兜底）
}
```

规则：

1. 每个房间事件被 RoomActor 串行应用后 `Revision++`，广播 `{ Revision, Changes }`（`state` 回调）。
2. 客户端缓存 `lastRevision`；收到 delta 的 `Revision != lastRevision + 1` → **跳号**，主动 `Query(room.getState)` 拉全量快照补齐（LSP 文档同步同款）。
3. 首加入/重连：`room.join` 响应直接带 `IsSnapshot=true` 的全量状态 + 当前 revision。
4. **不要求所有状态都走 delta**：低频复杂状态（路线列表、变体 schema）允许永远走快照字段；delta 只覆盖高频小字段（在线成员、到达计数、心跳进度、就绪标志）。这是务实的折中，避免为 50 个字段手写 merge 逻辑。

### 4.6 断线重连与状态恢复

现状问题：重连后只 `JoinRoom`/`JoinControlRoom`，不重同步状态（`CoordinatorClient.cs:316`、`SignalRClient.cs:87-102`），联机执行期的同步点集合、经验计数等全靠下次广播"碰巧"恢复。

标准化流程：

```
Closed/Reconnecting（SignalR 内置 0s/2s/10s/30s 重试，策略保留；
  v2.1：助手侧已实现 Closed 后 30s 低频自愈——内置耗尽不等于永久离线，该实践直接并入本节）
  → Reconnected（内置重连成功）或 自愈 StartAsync 成功（P1-F 姿势：同一连接、绝不重建）
  → ① session.hello（重新协商，服务端可能已升级）
  → ② room.join { roomCode, lastKnownRevision }
        ├─ 服务端认得该成员且在宽限期（HeartbeatMonitor 既有宽限）且 revision 可续
        │    → 响应带增量缺失段（事件日志重放，见下）或快照
        └─ 成员已被清理/房间已回收 → 响应明确告知 roomNotFound / memberEvicted
             → 客户端走"外部终止"语义（复用现有三分支：自己关的/轮换中/外部终止，
                .agents/knowledge/domains/multiplayer-hoeing.md §4.3）
  → ③ 恢复订阅，客户端状态机从 Degraded 回 Ready
```

- 服务端每房间维护**有界事件日志**（环形缓冲，最近 N=500 条事件 + 每条事件的 revision），这是《审核方案》§7.1"轻量事件日志"的直接复用：重放补差 + 调试回放共用一个设施。
- 心跳沿用现有 `HeartbeatMonitor` 宽限期判死；**宽限期内重连 = 无感恢复**，宽限期外 = 按掉线处理（现有三信号互补机制不变，multiplayer-hoeing.md §5.1）。

### 4.7 兼容层（迁移期双轨）

迁移期内新旧协议共存（《审核方案》阶段 1 的"不改行为"边界）：

```csharp
// CoordinatorHub 旧方法全部保留，瘦身为转发器（每个方法 3-5 行）：
public async Task ReportArrival(string syncPointId)
    => await _gateway.DispatchAsync(Context.ConnectionId,
           GatewayEnvelope.Command("sync.reportArrival", new { syncPointId },
               roomCode: GetRoomCode(Context.ConnectionId)));
```

- 旧客户端：完全无感（方法签名不变）。
- 新客户端：直连 `GatewayHub.Dispatch`。
- **同一房间不允许新旧协议混用**（hello 时房间协议按房主客户端版本锁定，服务端在 join 响应中告知），避免同一房间内两套计数逻辑分叉。
- 旧事件广播（`AllArrived` 等）在迁移期内由网关**双发**：新旧事件名都广播一次，新客户端忽略旧名，旧客户端忽略 `evt`。双发清单逐事件核对 `CoordinatorClient.cs:182-302` 与 `SignalRClient.cs:62-76` 的现有订阅，一个都不能漏。

### 4.8 SignalR 落地细节（保留现有已验证实践）

| 实践 | 现状出处 | 模块二规定 |
|---|---|---|
| URL 约定分裂（BGI 不加 `/hub`，助手手拼 `/hub`） | `CoordinatorClient.cs:173` vs `SignalRClient.cs:58` | **统一为：配置只填服务器基地址（如 `http://xxx:8080`），SDK 内部拼网关路径**；旧配置（带 `/hub`）在加载时归一化并告警。**（2026-09-06 切片 8 已落地 BGI 侧：归一化在 `BgiGatewayClient.ConnectAsync` 唯一咽喉剥 `/hub` 并告警，覆盖 BGI 全部 4 处消费点；MHA 侧切片 9 已落地：归一化在 SignalRClient.ConnectAsync 唯一咽喉剥 `/hub` 并经 OnLog 告警[按原始配置值去重防重试刷屏]，覆盖 MainViewModel 全部 4 处消费点）** |
| 自动重连策略 | 两处都是 `0s/2s/10s/30s` | 保留；耗尽后 `Closed` → 状态机进 `Down`，手动刷新恢复（`SignalRClient.RefreshAsync` 模式保留）。**（2026-09-04 止血 P1-F 已加自愈：Closed 后每 30s 对同一连接 `StartAsync`，成功后重新 JoinControlRoom；模块二落地时把该自愈语义并入状态机即可）** |
| 断线门控 + 不 throw | `SignalRClient.cs:153-171,174-193` | 沉淀为 SDK 基类 `GatewayClientBase.SafeInvokeAsync`：非 Connected 直接返回、异常记一次日志不上抛 |
| 先订阅后动作 | multiplayer-hoeing.md §4.1 | 收敛为 SDK 原语 `WaitEventAsync(name, filter, timeout, ct)`：内部 TCS + finally 退订 + 绑定 ct——所有回执型流程（到达、集合、路线校验、就绪确认）**只许用这个原语**，禁止散落的手写订阅 |
| Closed 必须观察异常参数 | `SignalRClient.cs:104-122` | SDK 统一注册，防 `UnobservedTaskException` 弹全局异常框 |
| 重连回调是 async void lambda 必须内捕异常 | `SignalRClient.cs:87-102` | SDK 统一包裹 |
| 并发状态风暴 | project-experience.md 日志风暴条目 | 事件处理入口去抖：同一 Name 的高频事件（心跳进度）按 500ms 节流 |

### 4.9 线程与资源（模块二）

- SignalR 回调在线程池线程触发，**事件进 WPF 前必须 `Dispatcher.BeginInvoke`**（助手）或走 BGI 任务线程安全队列；SDK 提供 `ObserveOn(SynchronizationContext)` 适配。
- `HubConnection` 是 `IAsyncDisposable`：SDK 统一 `DisposeAsync`（`SignalRClient.cs:213-221` 范式）；重建连接必须先 Dispose 旧的（避免双连接并发收发，`SignalRClient.cs:127-130` 注释的教训）。
- 重同步期间到达的事件**先入暂存队列**，快照应用完再按 revision 顺序回放——防止"快照+乱序 delta"把状态改花。
- 命令幂等：`IdempotencyKey` 缺省时 SDK 自动填 `Guid.NewGuid()`；重试（断线重发）**必须复用同一 key**，SDK 在连接恢复后自动重发未 ACK 的命令（带原 key），服务端幂等窗口去重——这就是"断线重连恢复"对命令面的完整闭环。

---

## 5. 跨模块统一约束（两个模块共同遵守）

### 5.1 线程安全矩阵

| 线程上下文 | 允许做什么 | 禁止做什么 | 依据 |
|---|---|---|---|
| WPF STA（BGI/助手 UI 线程） | ViewModel/配置读写、任务启动调起、`Dispatcher.InvokeAsync` 内代码 | 管道/网络同步等待、同步 `Dispatcher.Invoke`（堵消息泵、拖慢键盘钩子） | `InstanceRequestHandler.cs:530-535,627` |
| 管道接收线程（InstanceConnection ReceiveLoop） | 协议解析、路由、纯计算、后台文件 I/O | 触碰任何 WPF 对象、写 UI 绑定属性 | `InstanceConnection.cs:70-77` |
| SignalR 回调线程（线程池） | 入队、置标志、TCS 完成 | 直接改 UI、直接执行游戏动作 | `SignalRClient.cs` 事件处理现状 |
| 任务执行线程（TaskRunner/AutoHoeingTask） | 读 CancellationContext.Token、经桥接层上报事件 | 直接写管道（须经 EventHub 中转） | TaskRunner.cs |
| 事件观察器线程（模块一新增） | 只读 `TaskSemaphore.CurrentCount`、`lock(AutoHoeingProgress.Sync)` 读静态进度 | `TaskSemaphore.WaitAsync`（抢锁死锁）、持有锁跨 await | ipc-signalr.md §4 |

统一规则：**任何跨线程边界只传不可变 DTO，不传活对象引用**（Region/Mat/RO 等 IDisposable 绝不出现在消息里——截图生命周期归任务线程，见 TaskControl.CaptureToRectArea 体系）。

### 5.2 CancellationToken 传递规则

1. **每个入站请求一个 CTS**：连接级 CTS（断连即取消）`CreateLinkedTokenSource` 上请求级超时 CTS；长等待（等锁、等 RunMulti）必须传这个 token，不许 `Task.Delay(200)` 裸轮询不挂 token。
2. **BGI 任务取消只有三个语义档**，外部命令必须明确选档并在文档/日志中体现：
   - `Cancel()`——软取消，置 `WasCancelled`（task.stop、F11 等价语义）；
   - `ManualCancel()`——手动停止（仅 UI 热键路径，外部接口不暴露）；
   - `CancelTokenOnly()`——换批中断不清标记（仅 `task.start` 内部）。
3. **取消不悬挂**：任何因取消而中断的命令必须返回终态响应（cancelled/failed），服务端（SignalR）侧则"ACK 后终态走事件"（LSP `$/cancelRequest` 规则的两种落地）。
4. **禁止捕获 `OperationCanceledException` 后当成功返回**——取消状态必须透传到响应/事件（现有 `HandleTaskStart` 的 `cancelled` 返回是好范式，`InstanceRequestHandler.cs:668-671`）。
5. 外部断连 ≠ 任务取消：助手断连只取消"该连接上的等待"，**绝不**触发 `CancellationContext.Cancel()`（任务继续跑是特性——BGI 独立性原则，见 project-experience.md 助手设计决策）。

### 5.3 IDisposable 统一清单

| 类型 | 出现位置 | 释放规则 |
|---|---|---|
| `NamedPipeClientStream` / `NamedPipeServerStream` | IpcClient、InstanceService/Connection | 客户端 `using`；服务端随 InstanceConnection 生命周期 `DisposeAsync` |
| `HubConnection` | CoordinatorClient、SignalRClient | `DisposeAsync`；重建前必须释放旧实例 |
| `CancellationTokenSource`（含 linked） | 各请求/会话/模块级 | 谁创建谁 `using`/finally Dispose；`CancellationContext._externalCtsList` 内的由 `ManualCancel/Clear` 统一释放，桥接层不得额外泄漏 |
| `SemaphoreSlim`（TaskSemaphore） | TaskControl 静态 | 进程级不释放；**任何人不得 Dispose 它** |
| `Mat`/`Bitmap`/`Region`/`ImageRegion` | 任务层截图链 | 绝不跨越通信边界；任务线程内 `using`（既有纪律，通信模块零接触） |
| `Channel` 读写器（RelativeMouse、事件队列） | InstanceConnection、EventPlane | `Writer.Complete()` 后等 Reader 排空再 Dispose 宿主 |
| 文件流（配置读写） | HandleConfigList 等 | `await File.ReadAllTextAsync` 范式，无句柄泄漏点 |

### 5.4 多实例场景统一约定

| 场景 | 约定 |
|---|---|
| 一台机器 = 一个 BGI 根实例（按用户 SID 一根管道） | 模块一默认寻址根管道；`targetInstance` 字段预留 PID 寻址 |
| 同机 RDP 桌面分身 BGI | 分身不持根管道（现有 v2 拓扑），助手默认控制根；控制分身属远期需求，走 `targetInstance` |
| 多助手同时连接 | 每连接独立会话/订阅；写命令靠幂等键 + 单任务锁收敛；操作权限仍按"所有成员可操作"的既有产品决策 |
| 一台机器多开原神（多账号） | 超出当前架构（BGI 单任务锁全局唯一）；协议不为此预留，明确声明为**不支持** |
| 服务器多房间 | 模块二天然多房间（Group = roomCode）；房间间零共享状态，`RoomActor` 串行化（《审核方案》§7.2 的 Channel 方案） |

---

## 6. 迁移路线图（与《审核方案》三阶段对齐）

### 阶段 1（低风险）：模块一落地 + 模块二协议骨架

1. 新建 `Service/ExternalInterface/` 目录：协议层 + 会话层 + 三个平面，**全部新文件**。
2. `InstanceRequestHandler` switch 加 1 行 `ext.` 前缀分发；`App.xaml.cs` 加 1 行注册。
3. 修复 `InstanceService.AcceptLoopAsync` 单连接异常退出监听循环问题（必修，§3.9）。
4. 助手侧新 SDK（`BgiExternalClient`）替换 `IpcClient`+`CommandExecutor` 裸调用：**保留全部现有降级路径**（两阶段停止、杀进程回退、`WaitForBgiIpcReadyAsync`、`_hasRestartedThisBatch` 批次语义——这些全是血泪修复，见 `CommandExecutor.cs` 各注释与 project-experience.md，迁移时逐条对照不许丢）。
5. 事件面先上观察器实现；助手把 10 秒轮询降级为"事件通道不可用时的兜底"。
6. 模块二：`GatewayHub` 与旧 `CoordinatorHub` 并存；hello/capability/信封落地；旧方法瘦身转发（§4.7）。

### 阶段 2（中风险）：模块二状态同步 + 权威化配套

7. RoomState revision + delta 广播 + 客户端 `lastRevision` 重同步（§4.5/4.6）。
8. 房间事件日志环形缓冲（重放补差 + 调试回放）。
9. 经验计数等客户端逻辑上迁服务端（《审核方案》§6.2.1，协议侧已留 `exp.reportFightResult`）。
10. 助手 `SignalRClient`、BGI `CoordinatorClient` 换用统一 `GatewayClientBase` SDK；URL 约定归一。**（2026-09-06 切片 8 已完成 BGI 侧一半：`CoordinatorClient` 换芯到 `BgiGatewayClient`（23 事件 evt 分发 + 全部方法 v3 消息名 + 断线自愈逐字保留），URL 归一同步落地。**实现偏差记录：**方案设想双端共享 `GatewayClientBase` 基类，实际落地为 BGI 侧 `BgiGatewayClient` 与助手侧 `BgiExternalClient` 两个独立 SDK 各自镜像服务器协议常量——功能等价但无共享基类；MHA 侧切片 9（2026-09-06）完成另一半：`SignalRClient` 换芯到 `MhaGatewayClient`（13 事件 evt 分发 + 16 方法 v3 消息名 + P1-F 自愈逐字保留），URL 归一化同步落地；公共基类评估结论为**不抽**（两程序集无共享基建），维持两侧各自镜像服务器协议常量。）**

### 阶段 3（收尾）：旧协议退役

11. 全量客户端切新协议后，旧 Hub 方法标记 `[Obsolete]` → 观察一个版本周期 → 删除。
12. `VersionCompatibilityDecisions` 字符串比较退役，能力协商全权接管。
13. 模块一观察器事件源换成引擎内真钩子（若评估值得），接口不变。

---

## 7. 验收标准与测试矩阵

| # | 验收项 | 标准 |
|---|---|---|
| A1 | 单机零感知 | 不连助手、不开联机时，BGI 所有现有行为与测试不变；模块一不加载任何额外后台负载（观察器仅在存在事件订阅者时启动） |
| A2 | 协议兼容 | 新助手+老 BGI（v2）：hello 收到 `unsupported_operation` → 自动降级轮询，全程无错误弹窗；老助手+新 BGI：现有 18 操作行为逐字节不变 |
| A3 | 幂等 | 同一 `task.start`（同 generation）投递 3 次 → 执行 1 次，后 2 次返回 `already_executed`（现有语义保持）；同 idempotencyKey 的 stop 重发 → 重放响应不重复执行 |
| A4 | 取消不悬挂 | task.start 执行中发 task.stop → start 必须返回终态（cancelled），stop 返回 stopped，无悬挂请求；`TaskSemaphore.CurrentCount` 最终回 1 |
| A5 | 事件通道 | 订阅后触发"联机锄地上线"→ 助手 1s 内收到 `online.triggered`（对比现状 10s 轮询）；断开订阅连接后 BGI 无残留推送异常 |
| A6 | AcceptLoop 韧性 | 模拟客户端"连上立刻断"100 次 → 监听循环不退出，后续连接正常（修复 §3.9 实测故障） |
| A7 | SignalR 断线恢复 | 执行期拔网 20s 恢复 → hello → join 带 lastRevision → 状态补齐，房间成员无感；超过宽限期 → 按既有掉线三信号语义停止 |
| A8 | 双发兼容 | 迁移期新客户端忽略旧事件名、旧客户端忽略 `evt`/`state`，同一房间内同协议（§4.7 锁定规则） |
| A9 | 资源泄漏 | 1000 次连接/断开循环后：句柄数、CTS 数、订阅表条目回到基线 |
| A10 | 构建 | `dotnet build BetterGenshinImpact.sln -c Debug` + 三项目（BGI/Server/Assistant）各自 0 error |

测试落地纪律沿用既有规则：纯决策/协议编解码函数必须 PBT；IPC 处理至少 mock 测试（project-experience.md §测试落地）。

---

## 8. 风险与对策

| 风险 | 级别 | 对策 |
|---|---|---|
| 模块一被做成"第二个 InstanceRequestHandler"，两套任务控制逻辑分叉 | 高 | 模块一内部**复用**现有 handler 的桥接点（或直接搬移实现、`ext.` 入口与旧入口共用同一份 CommandPlane 实现），旧操作转发到新实现，单一事实源 |
| 事件推送放大 AcceptLoop 崩溃概率 | 高 | §3.9 修复列为阶段 1 必修，先于事件面上线 |
| 增量同步的 merge 逻辑引入状态错乱 | 中 | delta 只覆盖高频小字段白名单；revision 跳号强制全量兜底；房间协议锁定（§4.7） |
| 双轨期协议测试矩阵爆炸 | 中 | 阶段 1/2 明确"房间协议按房主版本锁定"，消除同房间混协议场景 |
| 助手迁移丢失血泪降级逻辑 | 高 | §6 阶段 1 第 4 条的逐条对照清单（`CommandExecutor.cs` 七处关键注释 + project-experience.md 五轮修复记录）作为迁移验收附件 |
| 公版合并冲突 | 中 | 模块一目录自包含 + 2-3 处单行挂载点（§3.2）；模块二不动 BGI 共享引擎文件 |

---

## 9. 附录

### 9.1 模块一操作一览（信封 v2 + ext.* 扩展）

```
ext.hello                 握手与能力协商（v2.1：复用已上线的 ping 会话校验通道演进）
ext.task.start            启动配置组/一条龙 {groupName|configName, startFromIndex?, generation?, idempotencyKey?}
ext.task.stop             停止当前任务 {idempotencyKey?}
ext.task.suspend          中断并保存上下文
ext.task.resume           恢复/取消恢复 {cancel?}
ext.task.status           任务状态快照（+stateRevision；现有已含 onlineGeneration/onlineTriggeredAt/hasSuspendedTaskContext）
ext.config.list           配置清单
ext.config.setTaskEnabled 启停配置项 {groupName, configName, taskIndex, enabled}
ext.config.pullGroup      拉取配置组包（v2.1 已有 v2 实现 config.pull_group，迁移期进兼容层）
ext.config.openRemoteEditor / ext.config.remoteEditorResult / ext.config.applyGroup
                          远程配置编辑三件套（v2.1 已有 v2 实现，同上）
ext.action.executeHotkey  执行热键 {hotkeyConfigName}
ext.action.closeGame      关闭游戏
ext.event.subscribe       订阅事件 {events[], lastKnownRevision?（切片4：断线续传，需 event.replay 能力；响应带 currentRevision/replayed/resyncRequired）}
ext.event.unsubscribe     退订 {events[]}
ext.event                 （服务端→客户端）事件帧 {event, stateRevision, timestampUtc, payload}
```

### 9.2 模块二消息名一览（v3，按族）

```
session.hello / session.heartbeat
room.create|join|leave|close|markStarted|setConfig|setWhitelist|setHostRouteList|reportHostReady
room.getState|listOnline|getConfig|getRoundHostOrder          （Query）
route.reportList|reportVariantSchema|reportVerificationDone
sync.reportArrival
fight.reportParticipant|reportDone
exp.reportFightResult
kazuha.declareCapability|notifyCollectStarted
world.reportJoined|resetJoined|resetForNewRound
anomaly.reportWaitPoint（realign 系列迁移期经兼容层）
control.joinRoom|sendCommand|reportStatus|confirmAllReady|reportOnlineEvent|clearOnlineHistory
log.reportBatch|subscribe|unsubscribe|requestFiles|reportFiles|requestDownload|reportChunk   （v2.1 新增）
screenshot.report                                                                        （v2.1 新增）
服务端→客户端：evt（Name=sync.allArrived / fight.allDone / room.closed / exp.allCapReached /
  kazuha.collectStarted / anomaly.* / control.remoteCommand / control.playersUpdated /
  control.allReady / room.playerListChanged / log.subscribersChanged / log.batch /
  log.filesRequested / log.fileList / log.downloadRequested / log.fileChunk /
  screenshot.member …）+ state（room.stateDelta）
```

### 9.3 参考来源

- LSP 3.17 规范（基础协议帧、`$/cancelRequest`、initialize/capabilities、进度通知）。[LSP Specification 3.17](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/ "citation")
- DAP 规范（capabilities 无版本演进、initialized 时序、reverse request、cancel request）。[DAP Specification](https://microsoft.github.io/debug-adapter-protocol//specification.html "citation"), [DAP Overview](https://microsoft.github.io/debug-adapter-protocol//overview.html "citation")
- 《联机助手架构审核与重构方案.md》（权威服务器、三阶段、RoomActor/Channel、事件日志取舍）。
- `.agents/knowledge/domains/ipc-signalr.md`、`Docs/design/multi-instance-ipc.md`、`.agents/knowledge/domains/multiplayer-hoeing.md`、`.agents/memory/project-experience.md`（帧格式坑、URL 拼接坑、单任务锁等锁姿势、断线门控、重连教训）。
- 代码核实：`InstanceIpcProtocol.cs`、`InstanceService.cs`、`InstanceConnection.cs`、`InstanceRequestHandler.cs`、`CancellationContext.cs`、`TaskControl.cs`、`CommandExecutor.cs`、`IpcClient.cs`、`SignalRClient.cs`、`CoordinatorClient.cs`、`CoordinatorHub.cs`（行号见正文各处引用，核实日期 2026-09-04）。

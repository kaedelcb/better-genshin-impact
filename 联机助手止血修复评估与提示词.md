# 联机助手止血修复评估与提示词

> 版本：v1.0（2026-09-04）
> 性质：**最小止血**，不是重构。目标只有两个：① 不掉线；② 上线后人齐能可靠触发锄地（BGI 内任务流不混乱）。
> 重构总计划见《联机助手重构总控计划.md》（切片 0-6），本文档的 P0-A 修复即切片 0 的止血版。
> 排查方式：代码静态分析（分支 main-OldTeaBag-* 最大序号版）。若实际修复时能提供失败时段的 BGI 日志与助手 `log/assistant_runtime.*.log`，可进一步坐实根因。

---

## 一、现状链路图（上线触发锄地全链路）

```
[每个成员本机]
BGI 执行"联机锄地上线"任务（NotifyOnlineTask）
  └─ static CurrentGeneration 自增（进程内从 1 开始，不持久化）★P0-B
       │
助手 10s 轮询 IPC task.status（IpcClient → BGI NamedPipe）★P0-A
  └─ 边沿检测：onlineGeneration > _lastOnlineGeneration ★P0-B
       └─ SignalR ReportOnlineEventAsync(generation)
            │
[服务器 CoordinatorHub.ReportOnlineEvent → RoomManager]
  └─ RoomManager.ReportOnlineEvent：generation <= 该成员历史值则丢弃 ★P0-B
       └─ CheckAndTransition：就绪人数 ≥ min(各成员 ExpectedHoeingPlayers)
            ├─ 单人：直接 ConsumeOnlineReady + 广播 AllReady
            └─ 多人：BeginConfirming + StartConfirmAsync
                 （3 次 × 30s 逐人发 AllReadyConfirm，等 ack）★P2-G
                 ├─ 全员 ack → ConsumeOnlineReady + 广播 AllReady
                 └─ 耗尽 → MarkExhausted，整轮放弃 ★P2-G
            │
[每个成员助手收到 AllReady / AllReadyConfirm]
  └─ MainViewModel.OnAllReadyConfirmedInternal(generation)
       ├─ CommandExecutor.ExecuteSuspendAsync → BGI task.suspend
       │    └─ BGI HandleTaskSuspend：Cancel + 等锁 ≤5s，存 SuspendedTaskContext（不持久化）★P2-H
       │    └─ 失败 → KillBgi + RestartBgi 回退 ★P1-E ★P2-H
       ├─ Task.Delay(1500) 固定等待 ★P1-C
       ├─ 逐配置组 task.start（start_group / start_oneclick）
       └─ 完成后 task.resume 恢复原任务
            └─ BGI HandleTaskResume：group 分支同步 Dispatcher.Invoke ★P1-D
```

---

## 二、已确认 Bug 清单（按止血优先级）

### P0-A：BGI IPC AcceptLoop 单点崩溃 → 全链路瘫痪

- **位置**：`BetterGenshinImpact/Service/Instance/InstanceService.cs:337-370`
- **根因**：外层 try-catch 包住整个监听循环，任意一次连接 accept/处理抛 `IOException`（客户端"连上即断"探针、助手轮询客户端 2s 超时断开等都是日常）就**整体退出监听循环**。BGI 进程还活着，但 NamedPipe 再也不接受新连接。
- **症状对应**：这就是"断线 + 任务流混乱"的总根因——IPC 瘫后助手 10s 轮询全部 TimeoutException → suspend / task.start 全失败 → 助手走 KillBgi+RestartBgi 回退 → SuspendedTaskContext 丢失（P2-H）→ 触发 P0-B（generation 归零）→ 全面混乱。2026-08-26 实测记录过该崩溃。
- **修复**：把 per-connection 的处理包进内层 try-catch，单次失败仅记日志并 continue（加 200-500ms 退避）；仅在listener 被显式停止（Dispose/CancellationToken 触发）时退出循环。

### P0-B：onlineGeneration 不持久化 + 边沿检测不随 BGI 重启复位

- **位置**：
  - `BetterGenshinImpact/GameTask/AutoOnline/NotifyOnlineTask.cs:18-33` — `static int _nextGeneration = 1`，进程级静态，**BGI 每重启一次就归零重新从 1 开始**。
  - `MultiplayerHoeingAssistant/ViewModels/MainViewModel.cs:526` — 边沿检测 `gen > _lastOnlineGeneration`，`_lastOnlineGeneration` 初始化后**从不随 BGI 重启复位**。
  - `MultiplayerHoeingAssistant/ViewModels/MainViewModel.cs:1333/1352/1357/1362/3204` — 多处 `int.MaxValue` 兜底：IPC 读失败一次，边沿检测**永久静音**直到助手重启。
  - `BgiCoordinatorServer/Services/RoomManager.cs:1292` — 服务端 `generation <= player.OnlineEventGeneration` 丢弃旧事件；BGI 重启后 generation=1 永远 ≤ 服务端记忆的历史值，**服务端同样永久丢弃**。
- **症状对应**："经常无法协调上线后的触发锄地任务"的头号嫌疑。只要 BGI 重启过一次（崩溃、被助手杀回退），当天后续上线事件在助手侧和服务端侧被双重静默丢弃。
- **修复（双保险，两个都做）**：
  1. BGI 侧：`NotifyOnlineTask` 的 generation 持久化到小文件（如 `User/online_generation.txt`，启动时加载、自增后写盘），保证跨进程单调递增。
  2. 助手侧：检测到 BGI（重）启动（`OnBgiStarted` 事件 / `WaitForBgiIpcReadyAsync` 成功后）将 `_lastOnlineGeneration` 重置为 0；`int.MaxValue` 兜底场景在下次成功读到 task.status 时也应恢复为真实值。

### P1-C：suspend 成功后固定 `Task.Delay(1500)`

- **位置**：`MultiplayerHoeingAssistant/ViewModels/MainViewModel.cs:4619`
- **根因**：BGI 的 HandleTaskSuspend 自己已经 Cancel + 轮询 TaskSemaphore 等锁最多 5s，助手再固定等 1500ms 既浪费又不可靠（锁未释放时 1500ms 远远不够）。
- **症状对应**：时序混乱——任务没停稳就 task.start，BGI 内 TaskSemaphore 互斥导致 start 被拒或行为不可预期。
- **修复**：改为轮询 IPC `task.status`（200ms 间隔，上限 6s），确认无任务运行（或 suspend 上下文已就位）再发 task.start；超时记警告日志并继续（保持现有容错语义）。

### P1-D：resume 的 group 分支用了同步 `Dispatcher.Invoke`

- **位置**：`BetterGenshinImpact/Service/Instance/MessageHandlers/InstanceRequestHandler.cs:1256`
- **根因**：违反项目异步纪律（公版一律 `InvokeAsync`），同步 Invoke 在 UI 线程繁忙时阻塞 IPC 处理线程，且 fire-and-forget 无完成跟踪。
- **修复**：改 `Dispatcher.InvokeAsync`，异常捕获记日志。语义不变。

### P1-E：BgiProcessMonitor 崩溃事件无边沿检测 → 连环重启/双开 BGI

- **位置**：
  - `MultiplayerHoeingAssistant/Services/BgiProcessMonitor.cs:51-60` — `CheckBgiStatus` 每 5s 轮询，只要 BGI 不在运行就触发 `OnBgiCrashed`，**无 running→stopped 跳变检测**。
  - `MultiplayerHoeingAssistant/ViewModels/MainViewModel.cs:3210-3216` — 事件处理里直接 `RestartBgi()`，无重入守卫。
- **症状对应**：BGI 启动慢（>5s）或被助手主动 Kill 后的间隙，每 5s 重复 `Process.Start` → 双开/多开 BGI → 两个实例抢 TaskSemaphore、抢截图、任务流彻底混乱。代码里的 `[DUPLAUNCH_PROBE]` 探针就是为追查此问题加的。
- **修复**：监视器内加 `_wasRunning` 边沿检测，只在"运行→消失"跳变时触发一次事件；重启成功后等进程实际出现再重新武装。

### P1-F：SignalR 内置重连耗尽后永久离线

- **位置**：`MultiplayerHoeingAssistant/Services/SignalRClient.cs:59`（`WithAutomaticReconnect(0s/2s/10s/30s)` ≈ 42s 后放弃）与 `:114` Closed 处理。
- **根因**：网络抖动超过约 42 秒、服务器重启等场景，内置重连耗尽 → Closed → 助手永久"离线"，只能手动刷新/重连。
- **症状对应**："不掉线"目标的直接缺口。
- **修复**：仅在 `Closed`（内置重连已耗尽）之后启动低频自愈定时器（每 30s 对同一 connection 调 `StartAsync`，成功后停止定时器）。**严禁**与内置重连并存——历史教训：自愈循环与内置重连并发曾互相 Dispose 连接导致彻底离线（见 SignalRClient.cs:109-113 注释）。重连成功后必须重新 JoinControlRoom（现有 `Reconnected` 已做，Closed 自愈路径也要做）。

### P2-G：多人确认阶段 3×30s 耗尽 → 整轮放弃

- **位置**：`BgiCoordinatorServer/Hubs/CoordinatorHub.cs:2626-2746`（StartConfirmAsync）+ `RoomManager.cs:1435-1439`（MarkExhausted）。
- **根因**：确认阶段任一成员慢/掉线/助手忙，3 次 × 30s 后 `MarkExhausted`，**整轮开锄作废**，无任何降级。
- **修复（止血）**：~~耗尽时降级——对当前在线成员直接广播 AllReady 开锄（宁可有人漏锄，不要全轮报废）~~。**（2026-09-04 已被用户取舍推翻：宁可不锄/漏锄也不能缺人开锄。最终落地 = 耗尽即整轮放弃不开锄，仅改进日志明确标注"确认超时，本轮放弃开锄（缺人不开锄），未确认成员：xxx"，MarkExhausted 与 ControlRoomPlayersUpdated 广播保持原样）**

### P2-H：KillBgi 回退后 SuspendedTaskContext 必丢失

- **位置**：`BetterGenshinImpact` 侧 `AllConfig.SuspendedTaskContext`（`[JsonIgnore]` 不持久化）；`InstanceRequestHandler.cs` HandleTaskResume 无上下文返回 `no_context`。
- **症状对应**：suspend 失败 → 杀 BGI → 上下文随进程消失 → resume 必失败 → 原任务（调度器/一条龙）回不来了，且用户无感知。
- **修复（止血）**：助手侧 resume 返回 `no_context` 时，房间日志明确提示"原任务无法自动恢复，请手动在 BGI 中重新启动调度器/一条龙"。上下文持久化属重构范围，本次不做。

### P2-I（已知边界，本次不要动）

批次内所有配置组共用同一 AllReady generation，`HandleTaskStart`（`InstanceRequestHandler.cs:504-520`）去重键是 generation+组名，同组名重复或重试场景可能误拦。止血期保持现状，仅记录。

---

## 三、症状 → 根因对照

| 用户症状 | 主要根因 |
|---|---|
| 断线、助手离线 | P0-A（IPC 瘫）、P1-F（SignalR 重连耗尽） |
| 上线后经常不触发锄地 | P0-B（generation 归零，双重静默丢弃）、P2-G（确认耗尽整轮放弃） |
| 时序混乱、任务流混乱 | P1-C（固定 1500ms）、P1-D（同步 Invoke）、P1-E（连环重启双开） |
| 锄完原任务回不来 | P2-H（上下文丢失） |

---

## 四、可粘贴提示词（复制到另一个会话执行）

````markdown
# 角色

你是 Better Genshin Impact (BGI) 仓库的资深维护工程师，熟悉 WPF、CommunityToolkit.Mvvm、NamedPipe IPC、SignalR。本次任务是对"联机助手"做**最小止血修复**，不是重构。

# 工作区与纪律（动手前必读）

- 工作区：`E:\Program Files\better-genshin-impact-LCB`
- **分支**：必须切到序号最大的 `main-OldTeaBag-*` 分支（茶包版滚动主干），动手前先 `git branch` 确认。不是 `pr/*`，不是功能分支。
- 编译验证：`dotnet build BetterGenshinImpact.sln -c Debug`，编译通过即算成功，无需运行；若程序被占用导致无法编译，直接放弃编译验证并说明。
- 遵守仓库根目录 AGENTS.md：MVVM（CommunityToolkit.Mvvm）、JSON 优先 Newtonsoft.Json（已被 System.Text.Json 序列化过的模型除外）、对话框用 ThemedMessageBox。
- **血泪纪律**：
  - 不碰 IPC 帧格式（4 字节小端长度前缀 + UTF-8 JSON）。
  - 不新增/修改 SignalR Hub 方法签名，不改协议。
  - BGI 的 TaskSemaphore 只允许只读轮询（`CurrentCount`），不允许 TryAcquire/Release 干预。
  - 现有代码里标注了血泪教训的降级/兼容逻辑（如 recentTaskName 降级、int.MaxValue 兜底、Closed 事件吞异常注释）必须保留语义，只能修 bug 不能删功能。
  - 单机（无助手）场景必须零感知：BGI 侧所有改动对不使用联机助手的用户不可见。
  - 每个修复保持最小 diff，不顺手重构、不改命名、不动无关代码。

# 背景（一段话）

联机助手 = 助手程序（MultiplayerHoeingAssistant，WPF）+ BGI 插件侧 IPC 服务（NamedPipe）+ 远程协调服务器（BgiCoordinatorServer，SignalR）。当前上线锄地功能不可用：BGI 执行"联机锄地上线"任务后，助手轮询 IPC 发现 onlineGeneration 递增 → 上报服务器 → 服务器确认全员就绪 → 广播 AllReady → 各成员助手 suspend 本机 BGI 当前任务 → 逐配置组 task.start 锄地 → 完成后 resume 恢复原任务。现在该链路断线、时序混乱、经常不触发。已完成的静态代码排查锁定了下述 8 个 bug，你的任务是**按下述指定方案逐项修复**。

# 必读文件（先读再改）

1. `联机助手止血修复评估与提示词.md`（仓库根目录，即本评估全文，含链路图与症状对照）
2. `BetterGenshinImpact/Service/Instance/InstanceService.cs`（重点 337-370 行 AcceptLoop）
3. `BetterGenshinImpact/GameTask/AutoOnline/NotifyOnlineTask.cs`（全文 38 行）
4. `BetterGenshinImpact/Service/Instance/MessageHandlers/InstanceRequestHandler.cs`（重点 1066-1197 suspend、1199-1341 resume、504-520 幂等）
5. `MultiplayerHoeingAssistant/ViewModels/MainViewModel.cs`（重点 500-560 上线检测、1320-1370 ClearLocalOnline、3200-3223 进程监控接线、4552-4710 OnAllReadyConfirmedInternal）
6. `MultiplayerHoeingAssistant/Services/BgiProcessMonitor.cs`（全文 120 行）
7. `MultiplayerHoeingAssistant/Services/SignalRClient.cs`（重点 56-130 连接/重连）
8. `BgiCoordinatorServer/Hubs/CoordinatorHub.cs`（重点 2626-2746 确认阶段）
9. `BgiCoordinatorServer/Services/RoomManager.cs`（重点 1284-1343 ReportOnlineEvent/CheckAndTransition、1435-1439 MarkExhausted）

# 修复清单（按顺序执行，逐项验收）

## P0-A：IPC AcceptLoop 单点崩溃
- 文件：`BetterGenshinImpact/Service/Instance/InstanceService.cs:337-370`
- 改法：监听循环内每次 accept/连接处理包独立 try-catch；`IOException`/客户端断开类异常仅记日志后 continue（加 200-500ms 退避防打满日志）；仅当 listener 被显式停止（CancellationToken/Dispose）时退出循环。保持现有帧处理逻辑不变。
- 验收：构造"客户端连上即断"场景（或走查代码确认），单次连接异常不再导致退出监听循环；编译通过。

## P0-B：onlineGeneration 跨重启单调递增（双保险，两处都改）
- 改法 1（BGI 侧）：`NotifyOnlineTask.cs` 的 generation 持久化到 BGI User 目录小文件（如 `User/online_generation.txt`）。静态初始化时读取（不存在/解析失败则从 1 开始），`Start()` 自增后立即写盘。写盘失败仅记日志不影响主流程。文件 I/O 用 try-catch 包裹，单机用户无感知。
- 改法 2（助手侧）：`MainViewModel.cs` 在检测到 BGI（重）启动后（`OnBgiStarted` 事件处理处、`WaitForBgiIpcReadyAsync` 成功后）将 `_lastOnlineGeneration` 重置为 0；另外当 task.status 成功返回但 onlineGeneration < `_lastOnlineGeneration`（说明 BGI 重启过）时，也将 `_lastOnlineGeneration` 同步为当前读到的值再比较，避免永久静音。`int.MaxValue` 兜底逻辑保留，但成功读到真实值后要恢复。
- 验收：走查确认 BGI 重启 → 再次执行"联机锄地上线"→ 助手边沿检测能触发、服务端 RoomManager.ReportOnlineEvent（1292 行）不再误丢弃。

## P1-C：suspend 后固定等待改轮询
- 文件：`MainViewModel.cs:4619`（`Task.Delay(1500)`）
- 改法：改为轮询 IPC `task.status`（200ms 间隔、上限 6s），确认 BGI 无任务在运行（或 hasSuspendedTaskContext 为 true）后再进入批次 task.start；超时记警告日志后继续（保持容错语义）。每次轮询用独立短生命周期 IpcClient（与现有代码姿势一致）。
- 验收：编译通过；走查确认等待逻辑在锁未释放时会继续等而不是固定 1500ms 盲等。

## P1-D：resume 同步 Dispatcher.Invoke 改 InvokeAsync
- 文件：`InstanceRequestHandler.cs:1256`（group 分支）
- 改法：`Dispatcher.Invoke` → `Dispatcher.InvokeAsync`，异常捕获记日志，语义不变。
- 验收：编译通过；与文件内其他分支姿势一致。

## P1-E：BgiProcessMonitor 崩溃事件边沿检测
- 文件：`BgiProcessMonitor.cs:51-60`
- 改法：加 `_wasRunning` 字段，仅在"运行 → 消失"跳变时触发一次 `OnBgiCrashed`；事件触发后进入"等待重启"状态，检测到进程重新出现后复位。`MainViewModel.cs:3210-3216` 的事件处理可加 `Interlocked` 重入守卫双保险。
- 验收：走查确认 BGI 不在运行的每 5s 轮询不会重复触发 RestartBgi（[DUPLAUNCH_PROBE] 日志每次崩溃只应出现一条 RestartBgi 记录）。

## P1-F：SignalR 重连耗尽后自愈
- 文件：`SignalRClient.cs:114`（Closed 事件）
- 改法：Closed（内置重连 0/2/10/30s 耗尽后）启动一个自愈 Timer，每 30s 对同一 HubConnection 调 `StartAsync()`；成功后停止 Timer、触发 OnConnectionStateChanged(true) 并重新 JoinControlRoom（复用现有 Reconnected 里的入房逻辑，注意入房失败仅记日志）。**严禁**在内置重连进行期间启动自愈（历史竞态教训，见 109-113 行注释）。Timer 要 IDisposable 妥善释放，连接对象 Dispose 时一并停掉。
- 验收：走查确认 Closed 后 30s 内有重连尝试日志、成功后恢复在线并入房；异常全部被观察（不会冒泡到 TaskScheduler.UnobservedTaskException）。

## P2-G：确认阶段耗尽降级开锄
- 文件：`CoordinatorHub.cs:2626-2746`（StartConfirmAsync 耗尽分支）
- 改法：3×30s 耗尽后不再单纯 MarkExhausted 放弃；改为：对当前在线成员广播 AllReady（复用单人路径的 ConsumeOnlineReady + 广播逻辑），Console 与房间日志标注"确认超时降级开锄，未确认成员：xxx"。MarkExhausted 语义保留（状态记录）。
- 验收：走查确认耗尽路径仍会触发开锄广播且日志明确。

## P2-H：resume no_context 明确提示
- 文件：`MainViewModel.cs`（resume 调用点约 594 行附近及 OnAllReadyConfirmedInternal 尾部）
- 改法：ExecuteResumeAsync 返回 `no_context`（或失败）时，AddLog 明确提示"原任务上下文已丢失（BGI 曾被重启），请手动在 BGI 中重新启动调度器/一条龙"。
- 验收：编译通过；日志文案明确。

## 不要动
- `InstanceRequestHandler.cs:504-520` 的 generation+组名幂等去重逻辑（已知边界，重构期处理）。
- IPC 帧格式、Hub 方法签名、TaskSemaphore 内部实现。
- 任何与上述 8 项无关的代码。

# 完成后

1. `dotnet build BetterGenshinImpact.sln -c Debug` 编译验证（占用则放弃并说明）。
2. 输出修复报告：每项修复的实际改动文件：行号、diff 摘要、验收结论。
3. 在《联机助手重构总控计划.md》§5 进度日志追加一行：日期、完成内容（止血修复 P0-A/B、P1-C/D/E/F、P2-G/H）、遗留事项。
4. 不要提交 git commit，由用户自行审阅后提交。
````

---

## 五、遗留（止血期不做，归入重构计划）

- SuspendedTaskContext 持久化（治本 P2-H）。
- 批次内 generation 幂等粒度（P2-I）。
- 40+ Hub 方法收敛、协议版本化、增量状态同步 → 《联机助手通信架构标准化方案.md》。
- CommandExecutor 重构、任务流编排 → 《联机助手架构审核与重构方案.md》v2 + 《联机助手重构总控计划.md》切片 1-6。

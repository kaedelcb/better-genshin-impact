# 联机助手整体架构审核与重构方案（v2 重写版）

> **v2.1 复核（2026-09-04 晚）**：止血 8 项（P0-A/B、P1-C/D/E/F、P2-G/H，其中 P2-G 按用户决策反转为"缺人不开锄"）+ 定时上线 S1-S5 已全部落实并逐条代码核实；同日新增三大功能（嘟嘟可日志系统 P1-P5 + 日志三件套、远程配置组编辑、IPC 跨会话检测）与万叶聚物修复。关键规模数据已同步刷新（Hub 57→65 方法、MainViewModel 4901→5398 行等），新增风险清单 R1-R10 见附录 C。
>
> **v2 说明（2026-09-04）**：本文由新任负责人对 v1（2026-09-04 早前版本，693 行）全文重写。
> 保留 v1 的骨架（5 大问题诊断 + 参考模式 + 三阶段重构），但：
>
> 1. **全部规模数据经源码实测修正**（v1 部分数据偏小或过时，见附录 A 对照表）；
> 2. **每个诊断结论给出文件:行号级证据**，可直接核实；
> 3. **新增"权威边界精确划分"（§五）**——v1 只说"服务器要更权威"，v2 明确哪些判定能迁、哪些永远不能迁；
> 4. **重构方案每条迁移项给出"现状代码位置 → 目标位置"映射**（§七）；
> 5. 通信协议细节（信封、握手、幂等、增量同步、断线恢复）不重复展开，**统一引用配套文档《联机助手通信架构标准化方案.md》**（下称《通信方案》）。
>
> 审核范围：BGI 本体 ↔ 联机助手 ↔ 协调服务器 三层架构
> 方法论支撑：权威服务器模式、CQRS、事件溯源（轻量）、Actor 模型、状态机驱动
> 参考产品：Unity Boss Room (Netcode)、Colyseus、CFWK、Metaplay、Nakama
> 代码核实日期：2026-09-04（茶包版主干）

---

## 一、执行摘要

当前联机锄地架构在功能层面已能支撑多人联机核心场景，但存在**系统性架构债务**。v2 审核在 v1 基础上修正了一个重要判断：

> **服务器并非"没有业务逻辑"，而是业务逻辑长错了地方**——大量协调判定（迟到者放行、异常自动清理、集体卡死监测、经验覆盖判定）确实在服务端，但它们与 SignalR 传输层、Group 管理、版本校验**全部绞在 `CoordinatorHub` 一个类里**，靠 **69 处 `lock(room)`** 维持正确性。真正"不在服务端"的是**计数类状态**（连续无经验计数等），它们在客户端形成了**双脑计数**问题。

因此 v2 的诊断从 v1 的"服务器不够权威"细化为三个更准确的问题：**逻辑错位（Hub 当业务层用）、双脑计数（客户端服务端各记一半账）、状态失控（Room 上帝对象 + 散落的静态缓存）**。

重构目标不变：以状态机为核心、事件驱动、权威服务器的分布式协调架构；路径不变：三阶段渐进重构；底线不变：**单机零感知、联机行为不变**。

---

## 二、当前架构全景（v2 实测修正）

### 2.1 三层结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           协调服务器 (BgiCoordinatorServer)                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐   │
│  │ CoordinatorHub  │  │  RoomManager    │  │  Room (上帝对象, 257行)      │   │
│  │  (3179行)       │  │  (1575行)       │  │  40+运行时字段               │   │
│  │  65个Hub方法    │  │  69处lock(room) │  │  10+并发集合                │   │
│  │  +4个静态缓存    │  │  (3个文件合计)   │  │  +Hub层4个静态缓存          │   │
│  │  +日志限流/请求  │  │  +日志订阅表     │  │                             │   │
│  │  映射等静态表    │  │  +AllReady状态机│  │                             │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────────────┘   │
│           ▲                              │                                   │
│           │ SignalR (WebSocket)          │ lock(room)                        │
│           ▼                              ▼                                   │
└─────────────────────────────────────────────────────────────────────────────┘
           │                                    │
           │    ┌───────────────────────────────┘
           ▼    ▼
┌─────────────────────────┐    NamedPipe IPC     ┌─────────────────────────────┐
│    BGI 主进程            │◄────────────────────►│    联机助手 (独立WPF)         │
│  ┌─────────────────┐    │   (IpcClient ↔     │  ┌───────────────────────┐   │
│  │ AutoHoeingTask  │    │    InstanceRequest  │  │ MainViewModel         │   │
│  │ (5000行)        │    │    Handler, 1739行) │  │ (5398行)              │   │
│  └─────────────────┘    │                     │  └───────────────────────┘   │
│  ┌─────────────────┐    │                     │  ┌───────────────────────┐   │
│  │ MultiplayerCoord│    │                     │  │ SignalRClient         │   │
│  │ inator (817行)  │    │                     │  │ (518行, 控制房间+自愈) │   │
│  └─────────────────┘    │                     │  └───────────────────────┘   │
│  ┌─────────────────┐    │                     │  ┌───────────────────────┐   │
│  │ CoordinatorClient│   │                     │  │ IpcClient             │   │
│  │ (1531行)        │◄───┘                     │  │ (205行,跨会话校验)     │   │
│  └─────────────────┘                          │  │ +CommandExec(481行)    │   │
│                                               │  │ +嘟嘟可日志服务群      │   │
└─────────────────────────┘                     └─────────────────────────────┘
```

### 2.2 关键数据流（与 v1 一致）

**控制流**（助手 → BGI）：助手 UI → MainViewModel → SignalRClient → 服务器 → SignalRClient → 助手 → IPC(`task.start`/`task.stop`) → BGI `InstanceRequestHandler`

**联机协调流**（BGI ↔ 服务器）：BGI(`AutoHoeingTask`/`PathExecutor` 联机分支) → `MultiplayerCoordinator` → `CoordinatorClient` → `CoordinatorHub` → `RoomManager` → `Room`，状态经 `Clients.Group(roomCode).SendAsync` 广播回各端。

---

## 三、核心问题诊断（v2，全部带行号证据）

### 问题 1：业务逻辑错位——Hub 被当成业务层用（v2 重新定性，最严重）

**v1 的判断**："服务器仅做信号转发和简单集合计数，80% 代码是参数透传 + 广播"。

**v2 的修正**：这个判断**不准确**。服务端实际承担了大量协调判定，证据：

- `WaitForAllPlayers`（`CoordinatorHub.cs:1869-2000+`）是联机同步的真正核心，内含四套交织的判定逻辑：
  - 异常玩家到达自动清理（L1899-1909，`IsAbnormal/TargetProgress` 条件复位）；
  - 已放行补发（L1915-1940，`BroadcastedSyncIds` 回放判定，`SyncReplayDecisions`）；
  - 孤立落后者放行（L1921-1951，`LaggingMemberReleaseDecisions.ShouldReleaseLaggingCaller`，基于"除 caller 外全部在线正常玩家进度"的集合比较）；
  - 全量重评估 + 广播 + 清集合（L1953-1968，`CollectSatisfiedSyncsLocked`）。
- 集体卡死监测 piggyback 在 `ReportArrival` 上（`CoordinatorHub.cs:423-427, 450-454`，`EvaluateCollectiveStuckPiggybackAsync`），配套 per-room `Timer`（`Room.cs:220`）、快照比对（`Room.cs:207-213`）、连续触发降级计数（`Room.cs:227`）。
- 版本一致性校验在 `JoinRoom` 入口硬阻断（`CoordinatorHub.cs:108-128`，`VersionCompatibilityDecisions.CanJoin`，基准=房内第一个非通配版本）。

**真正的问题**不是"服务端没逻辑"，而是：

1. **这些逻辑写在 Hub 里**——Hub 是 SignalR 传输层抽象，每个方法都要自己处理 `Context.ConnectionId` → 房间解析、`UpdateHeartbeat`、Group 广播、异常兜底，业务判定被传输细节淹没；
2. **Hub 层还私藏了 4 个静态缓存**（`CoordinatorHub.cs:14-29`）：`RouteReports`、`VariantSchemaReports`、`VariantSchemaTimeouts`（含 `CancellationTokenSource` 泄漏面）、`_connectionGroups`——这些是不在 `Room` 里的"影子房间状态"，绕过了 RoomManager 的统一管理；
3. **业务判定与广播副作用交错**：`lock(room)` 内做集合运算、lock 外立即 `await Clients.Group(...).SendAsync(...)`（如 L1955-1968），异常时状态已改但广播未发，一致性靠运气。

**对比业界**：Unity Boss Room 的 "Server is authoritative" 模式里，权威逻辑在**独立的领域层**（GameState/ServerCharacter），网络层（Netcode RPC）只是薄壳。CFWK："Nothing the client sends is trusted without server verification"——验证逻辑同样独立于传输。

**影响**：新功能只能继续往 Hub 里堆（v2 初版 57 个方法的直接成因；v2.1 复核当天即增至 65 个）；业务逻辑无法脱离 SignalR 单测；`lock(room)` 与 `await` 交错是死锁/竞态温床。

---

### 问题 2：双脑计数——同一本账客户端服务端各记一半

这是 v1"服务器不够权威"判断中**仍然成立的核心部分**，v2 精确定位：

**证据**（经验上限域）：

| 状态 | 位置 | 说明 |
|---|---|---|
| `_consecutiveNoExpCount` | `MultiplayerCoordinator.cs:76` | **客户端**本机连续无经验计数 |
| `_expCapReported` / `_twoConsecutiveReported` / `_expArmedSent` | `MultiplayerCoordinator.cs:78-82` | **客户端**上报态翻转门控 |
| `ExpCapReachedSet` / `TwoConsecutiveNoExpSet` | `Room.cs:50, 74` | **服务端**"谁上报了"集合 |
| `ExpCapArmed` / `ExpCapBroadcasted` | `Room.cs:64, 56` | **服务端**团队 arming/幂等标志 |

判定链条：客户端自己数"连续 N 场无经验"（客户端账本）→ 上报 `ReportExpCapReached` → 服务端只做集合覆盖检查（`IsAllOnlineMembersInEitherSet`，`RoomManager.cs:568`）。**服务端不知道"为什么"达上限，客户端不知道"别人怎么样"**——同一业务规则被劈成两半，改阈值要双端同步改。

同类双脑还有：

- 集体卡死：客户端 `_remoteSkipRequested/_remoteSkipTargetProgress` 门控（`MultiplayerCoordinator.cs:93-98`）+ 服务端 `ConsecutiveCollectiveSkipCount`（`Room.cs:227`）；
- 万叶选举：客户端 `DeclareKazuhaCapabilityIfPresentAsync`（`AutoHoeingTask.cs:351`）识别队伍后声明，服务端按声明顺序取第一个在线者（`Room.cs:191` 注释）——**选举规则一半在客户端识别、一半在服务端排序**。

**例外（已经是权威的好榜样）**：多世界轮换序列 `RoundHostOrder` 由服务端在 `MarkRoomStarted` 时基于 Players 一次性生成（`Room.cs:117-125`，spec `multiplayer-server-authoritative-round-order`），各端查询同一序列——**这证明权威化在本文 codebase 里已有一次成功实践，有样板可抄**。

**影响**：阈值/规则变更必须双端部署；断线重连后客户端账本丢失与服务端账本不一致；调试"为什么没停"要同时翻两端的账。

---

### 问题 3：协议层缺乏抽象，Hub 方法爆炸（数据修正：65 个，v2.1）

**现状**：`CoordinatorHub` 实测 **65 个 public 方法**（v2 初版时 57 个；日志三件套一天之内再 +8，验证膨胀仍在加速），且无协议版本字段。

**方法分布**（实测归类，完整映射见《通信方案》§4.3）：

```
房间生命周期 (6)   CreateRoom, JoinRoom, LeaveRoom, CloseRoom, MarkRoomStarted(+WithProgress)
房间查询 (9)       GetOnlineRooms, GetRoomConfig, GetRoundHostOrder, IsHostReady,
                   IsHostRouteListUploaded, GetHostRouteList(+Status), GetWorldJoinedCount, GetMemberProgress
房间配置 (4)       SetRoomConfig, UpdateWhitelist, SetHostRouteList, ReportHostReady
路线验证 (3)       ReportRouteList, ReportRouteVariantSchema, ReportRouteVerificationDone
同步点 (3)         ReportArrival(+WithExpectedCount), WaitForAllPlayers
战斗 (3)           ReportFightParticipant, ReportFightDone, FightingStatusChanged
经验上限 (5)       ReportExpCapReached/Cleared, ReportExpArmed, ReportTwoConsecutiveNoExp(+Cleared)
万叶 (3)           DeclareKazuhaCapability, SetKazuhaPlayer(废弃), NotifyKazuhaCollectStarted
世界加入 (3)       ReportWorldJoined, ResetWorldJoined, ResetForNewWorldRound
异常协调 (10)      WaitPointReport, ReportArrivalAtWaitPoint, ClearAbnormalStatus,
                   PlayerAnomalyNotify(+FightPoint), PlayerAnomalyRecovered,
                   MemberStatusChanged, ReportMemberProgress, RouteSkipped, WaitPointReached
心跳 (2)           Heartbeat, HeartbeatWithProgress
控制房间 (6)       JoinControlRoom, SendRemoteCommand, ReportControlStatus,
                   ConfirmAllReady, ReportOnlineEvent, ClearOnlineHistory
日志三件套 (8)     ReportMemberLogBatch, SubscribeMemberLog, UnsubscribeMemberLog,
                   RequestMemberLogFiles, ReportMemberLogFiles, RequestMemberLogDownload,
                   ReportMemberLogChunk, ReportMemberScreenshot（v2.1 新增）
```

外加客户端侧 **20+ 个广播事件名**（`CoordinatorClient.cs:182-302`、`SignalRClient.cs`），每个新功能 = 新增 3-5 个方法 + 1-2 个事件名 + 双端处理器。v2.1 注：远程配置组编辑走了**不新增 Hub 方法**的路线（复用 SendRemoteCommand 定向命令 + remote_config.* 命令族），是值得肯定的抑制膨胀姿势；日志三件套则按惯例直加 8 个方法，兼容层映射已同步（《通信方案》§4.3 v2.1）。

**版本兼容现状**：靠 `VersionCompatibilityDecisions`（`BgiCoordinatorServer/Services/VersionCompatibilityDecisions.cs`）字符串比较 + 通配符 + try/catch 兜底（如 `ReportTwoConsecutiveNoExp` 注释 L726："旧服务端无此方法 → HubException 被静默吞掉 → 退化"）。

**影响**（与 v1 一致）：协议膨胀、新旧兼容靠运气、部署顺序刚性。标准化方案见《通信方案》模块二（2 入口 + 2 回调 + ~35 消息名 + capability 协商）。

---

### 问题 4：Room 上帝对象 + 影子状态，锁 concurrency 失控

**Room.cs 实测 257 行**（v1 称 228 行，已增长），40+ 运行时字段，且**每个字段组都是一次功能 accretion 的化石**——字段注释直接引用各自 spec：

| 字段组 | 字段 | 来源 spec（注释原文） |
|---|---|---|
| 房间元数据 | Code, HostConnectionId, HostBaselineVersion, Players, CreatedAt, ExpectedPlayerCount, HostConfig, Whitelist | — |
| 断线宽限 | GracePendingMembers | （连接恢复） |
| 同步点 | ArrivalSets, BroadcastedSyncIds | fastsync-claim-short-circuit-premature-release-fix |
| 战斗 | FightDoneSets, FightParticipantSets, FightDoneBroadcasted | multiplayer-shared-fight-end-quorum-sync |
| 经验上限 | ExpCapReachedSet, ExpCapBroadcasted, ExpCapArmed, TwoConsecutiveNoExpSet, AllReadyBroadcasted | multiplayer-hoeing-exp-cap-stop / exp-cap-prefinal-stop-by-two-noexp |
| 路线 | HostRouteList, HostRouteListUploaded, RouteVerificationDoneSet | multiplayer-host-empty-route-member-wait-timeout-fix |
| 世界/轮换 | WorldJoinedSet, CurrentWorldRound, RoundHostOrder, IsStarted, HostReady | lock-room-after-start / multiplayer-server-authoritative-round-order |
| 异常等待 | WaitPoints, CoordinatedWaitPoint, AbnormalPlayerStates, CurrentUnifiedWaitPoint, WaitPointArrivals | multiplayer-abnormal-wait-coordination |
| 重对齐 | CurrentRealignProcess | multiplayer-abort-and-realign |
| 异常同步 | AbnormalPlayerInfos | multiplayer-abnormal-sync-server |
| 强制线路 | RouteEnforcementEnabled, RouteEnforcementThreshold | multiplayer-route-enforcement |
| 万叶 | KazuhaCandidates, KazuhaCollect | kazuha-player-auto-detection / hoeing-kazuha-collect-drop-terminal-signal |
| 集体卡死 | LastArrivalSetsSnapshot, ObservationStartTime, CollectiveSkipTimer, ConsecutiveCollectiveSkipCount | multiplayer-mutual-wait-collective-skip |

**锁实测**：`lock(room)` 共 **69 处**，横跨 3 个文件——`CoordinatorHub.cs` 34 处、`RoomManager.cs` 33 处、`HeartbeatMonitor.cs` 2 处。锁的正确性依赖"所有触碰者都记得加锁"的人肉纪律。

**影子状态**（不在 Room 里的房间状态）：

- Hub 层 4 个静态缓存（问题 1 已述，`CoordinatorHub.cs:14-29`）；
- RoomManager 层控制房间状态 4 个 `ConcurrentDictionary`（`RoomManager.cs:24-31`：`_controlRooms`、`_controlRoomAllReadyBroadcasted`、`_remoteControlConnections`、`_pendingCommands`）+ AllReady 代际状态机 `RoomAllReadyState`（`RoomManager.cs:1265-1281`，含 State/Generation/ConfirmedUids——**这已是一个手工小状态机，证明状态机需求是真实存在的，只是没有统一框架**）。

**影响**（与 v1 一致）：新功能必改 Room；锁是性能与死锁风险点；状态间依赖隐晦。注意 v2 观点：`CollectiveSkipTimer`（`Room.cs:220`，per-room `System.Threading.Timer`）是**房间里唯一的"时间驱动"状态**，它的 Dispose/重建时机（快照刷新时、轮换时）是隐性生命周期，Actor 化后应由邮箱内定时消息替代。

---

### 问题 5：三层职责模糊 + 缺乏会话生命周期（与 v1 一致，证据更新）

**BGI 与助手耦合**：

- `MainViewModel` 实测 5398 行（v2 初版时 4901 行，同日 +497 行：跨会话信任、定时上线 S1-S5、远程配置编辑接线等）；IPC 单向轮询（10s）+ `onlineGeneration` 边沿检测发现"联机锄地上线"（`InstanceRequestHandler.cs:774-775`）→《通信方案》模块一用 `ext.event` 推送根治。
- `AutoHoeingTask` 实测 5000 行，联机初始化 `InitializeMultiplayerAsync`（L786）、多世界 `RunMultiWorldAsync`（L2041）、轮换建房 `SetupNextRoundAsync`（L2297，房主重建房间带 3 次重试）、轮末同步 `SyncRoundEndAsync`（L2715）、离世界 `LeaveCurrentWorldAsync`（L2741）全部内联在一个任务类里；`MultiplayerCoordinator` 在两处独立构造（L1082、L2653）。

**生命周期缺失**：

- 无 `ISession`/`IRoomLifecycle`；状态转换靠标志位（`IsStarted`/`HostReady`/`AllReadyBroadcasted`/`HostRouteListUploaded`……）+ 各调用点自行检查；
- 多世界轮换的手动流程（CloseRoom → 创建新房间 → 重新加入，L2323-2373）每一步都可能失败，异常恢复散落；
- 已有的"准状态机"（`RoomAllReadyState` 代际确认）反证统一状态机框架的必要性。

---

## 四、参考架构模式分析（沿用 v1，压缩）

1. **权威服务器**：服务器是单一真相来源，客户端发"意图"，服务器广播"状态变更"。→ 落地为 §五的权威边界表。
2. **CQRS + 轻量事件日志**：命令与查询分离；每房间有界事件日志（环形 500 条）用于重放补差与调试，不做完整 Event Sourcing（会话 < 2 小时，无需长期存储）。
3. **Actor 模型（不引框架）**：`Channel<T>` + `Task` 实现每房间串行邮箱，消掉 69 处 `lock(room)`。并发量极低（50 房间 × 4 人，`RoomManager.cs:12, 33`），无需 Akka.NET/Orleans。
4. **状态机驱动**：`Idle → WaitingForPlayers → RouteVerifying → ReadyToStart → Running → RoundTransition → Ended`（转换矩阵见 §七阶段 1），把散落的标志位收敛为单一 Phase。

---

## 五、权威边界精确划分（v2 新增）

权威化不是"把一切都搬去服务端"。视觉识别只能在客户端（只有 BGI 能看到游戏画面），联机协调只能在服务端（只有服务器看到全房间）。v2 给出逐域边界：

### 5.1 必须留在客户端（BGI）的——视觉事实采集层

| 事实 | 采集位置 | 上报形式 |
|---|---|---|
| 本局战斗有无吃到经验 | PathExecutor/AutoFight 联机分支（经验球视觉检测） | `fight.reportResult { hasExp }` 事实事件 |
| 战斗开始/结束 | AutoFightEndDetection 等 | `fight.reportDone` |
| 到达某同步点 | PathExecutor 段循环 | `sync.reportArrival` |
| 视觉人数异常（退世界失明） | WorldStateMonitor 视觉旁路 | 本地停止决策（不上迁，见 multiplayer-hoeing.md §5.1 三信号互补） |
| 队伍构成（是否有万叶） | `AutoHoeingTask.DeclareKazuhaCapabilityIfPresentAsync`（L351）队伍识别 | `kazuha.declareCapability` 能力声明 |
| 本机进度（路线索引/段进度） | PathExecutor | 心跳/事件附带 |

**原则**：客户端上报的是**事实与意图**（"我看到什么""我想做什么"），不是**判定结论**（"我达上限了""大家都到了"）。

### 5.2 应该且能够迁到服务端的——协调判定层

| 判定 | 现状位置（双脑） | 目标位置 | 迁移依据 |
|---|---|---|---|
| 连续无经验计数 | 客户端 `_consecutiveNoExpCount`（`MultiplayerCoordinator.cs:76`） | 服务端 per-player 计数器 | 客户端只报 `hasExp` 事实，计数规则单点维护 |
| 达上限/预警判定与广播 | 已有雏形：服务端集合覆盖（`RoomManager.cs:425-568`） | 保留服务端，输入改为服务端计数 | 消除 `_expCapReported` 等客户端翻转门控（L78-82） |
| 万叶选举 | 客户端识别 + 服务端按声明顺序（`Room.cs:191`） | 服务端选举规则唯一化（声明顺序→显式优先级） | 选举是纯协调问题 |
| 集体卡死判定 | 已在服务端（MutualWaitMonitor + piggyback，`CoordinatorHub.cs:423-427`） | 保留，从 Hub 搬进 RoomActor | 本来就是权威逻辑，只是位置错 |
| 迟到者放行/补发回放 | 已在服务端（`WaitForAllPlayers` L1915-1951） | 保留，搬进 SyncPointService | 同上 |
| 统一等待点/异常协调 | 已在服务端（`Room.cs:127-160` 字段组） | 保留，搬进 AnomalyCoordinator | 同上 |
| 轮换房主序列 | 已在服务端（`RoundHostOrder`，`Room.cs:117-125`） | **样板，不动** | 权威化成功先例 |
| 在线判定/心跳宽限 | 已在服务端（`HeartbeatMonitor`、`GracePendingMembers`） | 保留 | — |

### 5.3 权威化的两个"不做"

1. **不做反作弊式不信任**：本场景是合作非对抗，客户端视觉事实默认可信。权威化的收益是**单点维护 + 断线一致**，不是防骗——避免为此付出双端校验的复杂度。
2. **不把视觉判定搬去服务端**：任何需要"看画面"的判定（经验球、战斗结束、人数）永远留客户端，服务端只消费其事实事件。

---

## 六、重构愿景（目标架构，v2 更新）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              协调服务器 (V2)                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ GatewayHub   │  │  RoomActor   │  │  Domain      │  │  EventLog       │  │
│  │ (薄传输壳,   │→ │  (per room)  │→ │  Services    │→ │  (per room,     │  │
│  │  Dispatch/   │  │  Channel邮箱  │  │  SyncPoint/  │  │  环形500条)      │  │
│  │  Query 两方法)│  │  +Phase状态机 │  │  Fight/Exp/  │  │  重放补差+调试   │  │
│  │              │  │  无lock       │  │  Kazuha/     │  │                 │  │
│  │              │  │              │  │  Anomaly     │  │                 │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────────┘  │
│         ▲                                                       │            │
│         │  evt / state(delta+revision) 广播                     │            │
│         │                                                       ▼            │
│  ┌──────────────┐                                   RoomState (Phase+子状态) │
│  │ HeartbeatMonitor (保留, 改为向 Actor 投递心跳消息)                          │
│  └──────────────┘                                                            │
└─────────────────────────────────────────────────────────────────────────────┘
         ▲  GatewayEnvelope（《通信方案》§4.2）
   ┌─────┴─────┐
   │           │
┌──▼───────┐ ┌─▼────────┐      ext.* IPC（《通信方案》模块一）
│ BGI      │ │ 联机助手  │◄────────────────►  BGI BgiExternalInterface
│ Gateway  │ │ Gateway  │   (事件推送替代 10s 轮询)
│ Client   │ │ Client   │
└──────────┘ └──────────┘
```

核心设计原则（v2 在 v1 表基础上修订）：

| 原则 | 说明 | 当前违背（v2 证据） |
|------|------|----------|
| 服务器权威 | 判定结论由服务端产出 | 双脑计数（§三问题 2） |
| 传输与业务分层 | Hub 只做解析/路由/广播 | 业务逻辑写在 Hub（§三问题 1） |
| 事件驱动协议 | 消息名 + payload，非方法爆炸 | 65 方法 + 20 事件名（§三问题 3） |
| 状态机隔离 | 每房间串行 Actor + Phase | Room 上帝对象 + 69 处 lock + 影子状态（§三问题 4） |
| 协议版本化 | capability 协商 | 字符串比较 + try/catch（§三问题 3） |
| BGI↔助手解耦 | 双向事件总线 | IPC 单向轮询（§三问题 5） |

---

## 七、重构方案（三阶段，v2 带代码映射）

> 与《通信方案》§6 路线图共用同一套阶段划分：本方案负责"货"（业务），《通信方案》负责"路"（协议）。
> 阶段 1 的两个文档切片（AcceptLoop 修复、模块一落地、GatewayHub 骨架）在《通信方案》§6 阶段 1 已列，此处不重复。

### 阶段 1：状态机骨架 + Room 拆分（不改行为，风险低）

#### 7.1.1 引入 RoomPhase 状态机（先只观测，不驱动）

```csharp
public enum RoomPhase
{
    Idle,               // 刚创建（CreateRoom 后）
    WaitingForPlayers,  // 等待成员加入
    RouteVerifying,     // 路线验证中（ReportRouteList/VariantSchema 流动）
    ReadyToStart,       // 房主就绪（HostReady=true），等待开锄
    Running,            // MarkRoomStarted 后（IsStarted=true）
    RoundTransition,    // 多世界轮换中（ResetForNewWorldRound 窗口）
    Ended               // CloseRoom / 全员达经验上限
}
```

**落地方式（不改行为）**：在 Room 上新增 `Phase` 只读属性，**由现有标志位推导**（`IsStarted/HostReady/HostRouteListUploaded/RouteVerificationDoneSet` 的组合），在每个 Hub 方法入口打 `Phase` 日志。跑一个版本周期，用日志验证推导规则与真实流程吻合后，才允许阶段 2 用它做门控。转换矩阵（推导规则）：

| Phase | 推导条件（现状标志位） |
|---|---|
| Idle → WaitingForPlayers | CreateRoom 完成 |
| WaitingForPlayers → RouteVerifying | 首个 ReportRouteList/VariantSchema 到达 |
| RouteVerifying → ReadyToStart | HostRouteListUploaded ∧ 路线验证完成 ∧ HostReady |
| ReadyToStart → Running | MarkRoomStarted |
| Running → RoundTransition | ResetForNewWorldRound 调用窗口 |
| RoundTransition → WaitingForPlayers | 新房间首位成员加入 |
| Running → Ended | AllReachedExpCap 广播 ∨ CloseRoom |

#### 7.1.2 Room 字段分组（纯结构重构，不改语义）

按 §三问题 4 的字段组表，把 Room 拆为 `RoomMetadata`（只读元数据）+ `RoomRuntimeState`（按子系统分组的嵌套类：`SyncPointState/FightState/ExpCapState/RouteState/WorldState/AnomalyState/KazuhaState/CollectiveSkipState`）。**此步只做字段搬家 + 属性转发**，所有引用点（Hub 34 处 lock、RoomManager 33 处）逐组迁移，每组一个 commit。影子状态（Hub 4 个静态缓存、RoomManager 控制房间 4 个字典）登记造册但本阶段不搬。

#### 7.1.3 Hub 方法瘦身（与《通信方案》兼容层同步）

65 个方法逐个改为"解析 → 调 RoomManager/服务 → 广播"的三段式，把业务判定从 Hub 方法体里下沉到 RoomManager 的新方法。**此步不做协议替换**（新旧协议双轨由《通信方案》§4.7 负责），只是把逻辑从传输层剥离，为阶段 2 的 Actor 化备料。

### 阶段 2：RoomActor 化 + 双脑计数上迁（风险中等，架构收益最大）

#### 7.2.1 RoomActor：消掉 69 处 lock(room)

```csharp
public class RoomActor
{
    private readonly Channel<RoomMessage> _mailbox = Channel.CreateUnbounded<RoomMessage>();
    private readonly RoomRuntimeState _state = new();
    private readonly RoomPhaseMachine _phase = new();
    private readonly BoundedEventLog _eventLog = new(capacity: 500);

    public async Task RunAsync(CancellationToken ct)
    {
        await foreach (var msg in _mailbox.Reader.ReadAllAsync(ct))
        {
            var effects = _state.Apply(msg, _phase);   // 纯判定，无 I/O
            _eventLog.Append(msg, _state.Revision);
            await effects.ExecuteAsync();              // 广播等副作用在 lock 外统一执行
        }
    }
}
```

要点：

- `Apply` 是纯函数（可 PBT）；副作用（`Clients.Group.SendAsync`）以 Effect 对象返回、在邮箱外统一执行——根治"lock 内改状态 + lock 外广播"的交错问题（§三问题 1.3）。
- Hub 静态缓存（RouteReports 等 4 个）与 `CollectiveSkipTimer` 全部搬进 Actor 状态；定时器改为"向自己邮箱投递延迟消息"。
- `HeartbeatMonitor`（2 处 lock）改为向各房间 Actor 投递心跳判死消息，不直接摸 Room。

#### 7.2.2 双脑计数上迁（按 §5.2 表逐域迁移）

| 域 | 现状（客户端） | 目标（服务端） | 客户端改动 |
|---|---|---|---|
| 经验计数 | `MultiplayerCoordinator._consecutiveNoExpCount`（L76）+ `_expCapReported`/`_twoConsecutiveReported`/`_expArmedSent`（L78-82） | RoomActor per-player 计数 + 覆盖判定（吸收 `RoomManager.RecordExpCapReached` 等 L425-568 逻辑） | 只上报 `fight.reportResult{hasExp}`；删除 4 个客户端状态字段 |
| 万叶选举 | 客户端识别 + 服务端声明顺序（`Room.cs:191`） | 服务端选举服务（声明顺序→显式规则） | 只声明能力，不感知选举结果 |
| 集体卡死 | 服务端判定 + 客户端 `_remoteSkipRequested` 门控（L93-98） | 判定保留服务端（搬入 Actor） | 门控保留（它是执行侧状态，§5.3 不上迁） |

**兼容策略**：房间协议锁定（《通信方案》§4.7）保证同一房间内不会新旧计数逻辑混跑；老客户端房间走旧路径，新客户端房间走权威路径。

### 阶段 3：会话生命周期 + 客户端瘦身（风险中等）

#### 7.3.1 Session 生命周期显式化

- `RoomPhase` 从"推导观测"升级为"驱动门控"：每个操作声明允许的 Phase 集合，不匹配返回明确错误（替代现在的标志位 if 链）；
- 多世界轮换从 `AutoHoeingTask` 内联流程（L2041/2297/2715/2741 四段）抽为 `RoundTransitionCoordinator`，由 Phase 转换驱动；
- 断线恢复走《通信方案》§4.6 的 revision 重同步，`GracePendingMembers`（`Room.cs:16`）语义保留。

#### 7.3.2 BGI 侧瘦身

- `CoordinatorClient`（1531 行，20+ 事件订阅）→ `BgiRoomClient`（连接 + 上报 + 统一 `OnStateChanged`）；
- `MultiplayerCoordinator` 删除已上迁的计数字段（§7.2.2），保留执行侧门控（RemoteSkip、快速抢报 `_fastReportedSyncIds` L112）；
- `AutoHoeingTask` 联机段落（L786-1084 初始化、L2041+ 多世界）抽为独立 `MultiplayerSession` 类，任务类只保留执行编排。

#### 7.3.3 助手侧解耦

- IPC 轮询 → `ext.event` 推送（《通信方案》模块一）；
- `MainViewModel`（5398 行，v2.1）按"连接管理 / 命令编排 / UI 状态"三层拆分。

---

## 八、关键设计决策（v2 更新）

### 8.1 是否引入事件溯源？

**结论（同 v1）**：不引入完整 Event Sourcing，只做**每房间有界事件日志**（环形 500 条）。

理由：会话生命周期短（< 2 小时）；事件日志的用途是两个具体场景——断线重连的增量补差（《通信方案》§4.6）和"为什么这次集体卡死了"的调试回放；不需要 CQRS 读模型（查询简单、并发低）。

### 8.2 是否引入 Actor 框架？

**结论（同 v1）**：不引入 Akka.NET/Orleans，`Channel<T>` + `Task` 足够。

理由：并发上限 50 房间 × 4 人（`RoomManager.cs:12, 33` 的 MaxPlayers/默认 maxRooms）；`Channel` 天然提供串行邮箱语义；避免框架学习成本与部署依赖。

### 8.3 权威化的次序：先计数、后协调

§5.2 的迁移清单按风险排序执行：**经验计数先迁**（规则简单、双脑痛点最明显、有 `exp.reportFightResult` 协议预留），万叶选举次之，集体卡死/异常协调最后（它们已在服务端，只是搬家，风险最低但收益也最低）。

### 8.4 协议兼容策略（移交《通信方案》）

v1 的"每个 RoomEvent 带 ProtocolVersion 字段"建议，v2 修订为《通信方案》§4.2/§4.4 的完整方案：信封版本冻结 + capability 布尔标志协商（DAP 方法论）+ 房间协议按房主版本锁定。`VersionCompatibilityDecisions` 字符串比较退役（阶段 3）。

### 8.5 `CollectiveSkipTimer` 的归属

房间里唯一的 `System.Threading.Timer`（`Room.cs:220`）在 Actor 化时**不得保留为回调定时器**（回调会并发于邮箱处理，重新引入竞态），改为"邮箱延迟消息"模式（`Channel` + `Task.Delay` 后自投递），到期评估与消息处理天然串行。

---

## 九、实施路线图（v2 合并"路"与"货"）

### 近期（切片 0-2，约 1-2 个月）

1. **AcceptLoop 韧性修复**（《通信方案》§3.9，必修前置）——⚠️ 止血版已完成（2026-09-04，P0-A：循环内 catch + 300ms 固定退避 + 失败实例重建），仅剩阶梯退避（1s/2s/5s 封顶 30s）随切片 1 并入
2. **模块一 BgiExternalInterface 落地**（《通信方案》§3：ext.hello / 幂等窗口 / ext.event 事件推送 / AcceptLoop 修复随此前置完成）
3. **RoomPhase 观测性引入**（§7.1.1：只加推导属性和日志，不改行为）
4. **GatewayHub 骨架 + 旧方法瘦身**（《通信方案》§4.7 + 本文 §7.1.3 同步进行）

### 中期（切片 3-4，约 2-4 个月）

5. **Room 字段分组**（§7.1.2，每组一个 commit）
6. **RoomActor 化**（§7.2.1：Channel 邮箱、Effect 外置、静态缓存搬家、Timer 改延迟消息）
7. **经验计数上迁**（§7.2.2 第一刀，配 capability `exp.serverSideCount`）
8. **revision + delta 增量同步 + 断线重同步**（《通信方案》§4.5/4.6）
9. **万叶选举规则服务端唯一化**

### 远期（切片 5，约 4-6 个月）

10. **Phase 驱动门控 + RoundTransitionCoordinator**（§7.3.1）
11. **双端客户端瘦身**（§7.3.2/7.3.3）
12. **旧 Hub 方法与版本字符串比较退役**（《通信方案》§6 阶段 3）
13. **事件日志回放调试工具**（§8.1 的第二用途）

每阶段验收以《通信方案》§7 的 A1-A10 矩阵 + 本文 §十 风险表为准；底线不变：**单机零感知、联机行为不变**。

---

## 十、风险评估（v2 更新）

| 风险 | 级别 | 缓解措施 |
|------|------|----------|
| 重构期引入回归 Bug | 高 | 每阶段保持现有测试通过；`Apply` 纯函数化后可 PBT 覆盖状态转换；Phase 先观测后驱动（§7.1.1 两步走） |
| 新旧客户端/服务端兼容性 | 高 | 房间协议锁定（《通信方案》§4.7）；双发过渡；旧方法保留为转发器直至阶段 3 |
| Actor 化改变并发时序引入隐性竞态 | 高 | Effect 外置模式保证"状态变更"与"广播"的相对顺序与现网一致；先搬 SyncPoint 域（判定最成熟）验证模式，再推广 |
| `CollectiveSkipTimer` 等时间驱动逻辑在邮箱化后时序漂移 | 中 | 延迟消息语义与原 Timer 回调的差异（触发点从"回调线程"变"邮箱位置"）需逐场景核对，保留原宽限参数不变 |
| 业务逻辑上迁改变"谁在什么时机知道什么" | 中 | 经验计数上迁后客户端失去本地账本，UI 展示所需数据必须包含在 delta 白名单内（《通信方案》§4.5 规则 4） |
| 重构范围失控 | 中 | 严格切片划分；每片"不改行为"边界明确；Room 分组每组独立 commit 可回滚 |
| 联机功能不可用 | 高 | 每阶段测试环境充分验证后合并；保留回滚能力 |

---

## 十一、结论（v2）

v1 说"服务器不够权威，协议不够抽象，状态不够清晰"——方向正确。v2 用代码证据把它修正为更精确的三句话：

1. **业务逻辑没缺位，但长错了地方**：大量权威判定已在服务端，却绞在 Hub 传输层里，靠 69 处 `lock(room)` 和 4 个静态缓存维持；
2. **真正缺位的是计数类状态**：双脑计数（客户端记账、服务端集合）是"改一个阈值要动两个进程"的根源；
3. **状态不是不清晰，是无处安放**：Room 的 40+ 字段每个都有明确 spec 出处，问题是没有生命周期框架承载它们——`RoomAllReadyState` 这个手工小状态机已经证明了需求。

演进路径不变：先抽象（状态机观测 + 协议骨架），再迁移（Actor 化 + 计数上迁），后解耦（生命周期 + 客户端瘦身）。每一次重构以**"单机零感知、联机行为不变"**为底线，以**"测试通过 + PBT 覆盖 + 真实联机验证"**为验收标准。

---

## 附录 A：v1 → v2 数据修正对照表

| 项目 | v1 声称 | v2 实测（2026-09-04） | 影响 |
|---|---|---|---|
| CoordinatorHub 方法数 | 30+ / ~40+ | **57** | 问题 3 比 v1 估计更严重 |
| Room.cs 行数 | 228 | **257** | 持续增长中 |
| Room 字段数 | 50+ | **40+**（精确按属性计数） | 描述修正，结论不变 |
| lock(room) 数量 | "大量" | **69 处**（Hub 34 + RoomManager 33 + HeartbeatMonitor 2） | 首次精确量化 |
| Hub 层静态缓存 | 未提及 | **4 个**（RouteReports/VariantSchemaReports/VariantSchemaTimeouts/_connectionGroups） | v2 新发现的影子状态 |
| 服务端业务逻辑 | "80% 透传，没有业务规则" | WaitForAllPlayers/集体卡死/版本校验等**大量判定在服务端但错位在 Hub** | 问题 1 重新定性 |
| 经验上限判定 | "客户端计数 → 服务端集合覆盖" | 一致，v2 补充客户端 4 个状态字段精确位置（MultiplayerCoordinator.cs:76-82） | 问题 2 精化 |
| AutoHoeingTask | 5000+ 行 | **5000 行整** | 一致 |
| MainViewModel | 4900 行 | **4901 行**（v2.1 复核时已增至 **5398 行**） | 一致；v2.1 当日 +497 行 |
| CoordinatorClient / MultiplayerCoordinator / RoomManager | 1531 / 817 / 1490 行 | 前两者一致；RoomManager v2.1 复核时 **1575 行**（+85：日志订阅表） | — |

## 附录 C：v2.1 现状复核（2026-09-04 晚，全局代码复审结论）

### C.1 止血修复落实核验（全部通过）

| 修复项 | 位置 | 核验结论 |
|---|---|---|
| P0-A AcceptLoop 韧性 | `InstanceService.cs` AcceptLoopAsync | ✅ catch 已移入循环内，单次 accept 失败 300ms 退避后继续，失败管道实例丢弃重建，仅在 cancellation/Dispose 时退出 |
| P0-B generation 持久化 | `NotifyOnlineTask.cs`（BGI 侧写 User/online_generation.txt）+ `MainViewModel.cs`（OnBgiStarted 复位基线 + 读值回退同步） | ✅ 双保险均已落地 |
| P1-C suspend 后轮询 | `MainViewModel.cs`（200ms×30 轮询 task.status 的 running/hasSuspendedTaskContext，超时容错继续） | ✅ 依赖字段已在 task.status 响应中核实存在（`InstanceRequestHandler.cs:772`） |
| P1-D resume InvokeAsync | `InstanceRequestHandler.cs:1256` group 分支 | ✅ 已改 InvokeAsync + 异常捕获 |
| P1-E 崩溃事件边沿检测 | `BgiProcessMonitor.cs`（_wasRunning 跳变检测）+ `MainViewModel.cs`（Interlocked 重入守卫） | ✅ 双层防护 |
| P1-F SignalR Closed 自愈 | `SignalRClient.cs`（Closed 后 30s 自愈定时器、防重入、连接替换检测、成功后重入房） | ✅ 严格避开了"与内置重连并存"的历史竞态 |
| P2-G 确认耗尽 | `CoordinatorHub.cs` StartConfirmAsync 耗尽分支 | ✅ 按用户明确决策**反转**为"缺人不开锄"（整轮放弃 + 明确日志），非降级开锄 |
| P2-H resume no_context 提示 | `MainViewModel.cs` 两处 resume 失败分支 | ✅ 已加明确提示文案 |
| S1-S5 定时上线链路 | `MainViewModel.cs` + `RoomManager.cs` CheckAndTransition | ✅ 全部落实（武装初始化/断线重试/generation 持久化/防重入/等待日志） |

### C.2 同日新增功能（重构范围外，但影响设计假设）

1. **嘟嘟可日志系统 P1-P5 + 三件套**（7fcbbafb + dd5cf141）：`BgiLogTailService`（专用后台线程 tail，Watcher+1s 轮询双保险，逐订阅者隔离分发，Dispose 先 Join 线程再释资源）、`MemberLogRelayService`（观众驱动：无订阅零渲染零流量，500ms 合批，300 环形补发）、`MemberLogShareService`（gzip 分块 192KB/160ms 节流，双端文件名白名单防目录穿越）。服务端配套：订阅表（上限 20/目标，断线清理）、请求映射单播（10 分钟 TTL）、双限流（4 批/秒 + 30 块/秒）。**工程质量高**，观众驱动模式正是《通信方案》事件驱动的预演。配套测试 `BgiCoordinatorServer.Tests/MemberLogSubscriptionTests.cs` 18/18 通过。
2. **远程配置组编辑**（3953a859）：新增 4 个 IPC 操作（config.pull_group/open_remote_editor/remote_editor_result/apply_group）+ BGI 侧 `RemoteEditSession`（静态单会话 + 15 分钟僵尸回收，线程安全）+ `RemoteConfigEditWindow`。**未新增 Hub 方法**（复用 SendRemoteCommand 定向命令），是抑制问题 3 膨胀的正确姿势。注意点：`HandleConfigOpenRemoteEditor` 有意使用同步 `Dispatcher.Invoke`（为同步回报窗口创建失败），与 P1-D 的 InvokeAsync 方向相反但操作有界，可接受。
3. **IPC 跨会话检测**（42b81a04）：`IpcClient.ConnectAsync` 成功后 ping 握手比对 windowsSessionId，跨会话/无法确认一律不可信（不采信状态、阻断控制指令且不进入杀进程回退），标题栏 ⚠ 徽章。**ping 握手即《通信方案》§3.4 ext.hello 的雏形**，切片 1 应在其上演进。代价：每次连接多一次 ping 往返（10s 轮询路径可接受）。
4. **万叶聚物三项修复**（78e7edf5）：BGI 侧战斗逻辑（残留 CD 尾轮询/快照第 1 层/syncKey 隔离），与通信重构正交。

### C.3 新增风险清单 R1-R10（来自《联机锄地上线链路稳定性分析报告.md》，全部纳入切片 3）

- **高**：R1 ConsumeOnlineReady 只消费 minGen 成员 → 高 gen 成员残留武装 30 分钟可重复触发；R2 AllReadyConfirm 即开锄，耗尽无法召回已开锄成员（"缺人不开锄"在客户端层面无法兑现，需确认/开锄信号分离）；R3 轮次 id=minGen 跨轮不单调 vs 客户端幂等号假设单调 → 特定重启时序下整轮卡死。
- **中**：R4 断线即抹上线事件且重连不补报；R5 确认期任何新上报把状态打回 idle 打断确认；R6 threshold=各成员 ExpectedHoeingPlayers 最小值且不对在线人数封顶（2 人房默认 4 → 永远不上线；任一配 1 → 单人直达误判）；R7 助手重启即幻影上线（边沿基线归零 + 忽略 onlineTriggeredAt）。
- **低**：R8 同成员双路径 generation 不同源；R9 exhausted 无手动重置入口；R10 状态机 "waiting" 是死状态（文档与实现不符）。

### C.4 对重构方案的影响

- 问题 3（方法爆炸）的兼容层映射必须覆盖 65 个方法（《通信方案》§4.3 已同步 v2.1）。
- 切片 3（RoomActor 化）目标已扩展为"Actor 化 + 计数上迁 + 上线链路状态机收口（R1-R9）"，见总控计划 §4。
- 模块一 ext.hello 直接复用已上线的 ping 会话校验通道，降低切片 1 落地成本。
- 新增功能未破坏任何红线：单机零感知保持、帧格式未动、TaskSemaphore 仍只读轮询、TaskSemaphore/TaskRunner 未被新功能触碰。

## 附录 B：引用与资料

- 配套文档：《联机助手通信架构标准化方案.md》（模块一 BGI 侧 IPC 标准化、模块二 SignalR 协议标准化、迁移验收矩阵 A1-A10）。
- 领域知识：`.agents/knowledge/domains/multiplayer-hoeing.md`（三信号掉线、轮换双标志、路线索引语义）、`.agents/knowledge/domains/ipc-signalr.md`（帧格式、单任务锁、URL 拼接）。
- 关键代码（全部已直接阅读核实）：`BgiCoordinatorServer/Hubs/CoordinatorHub.cs`、`BgiCoordinatorServer/Services/RoomManager.cs`、`BgiCoordinatorServer/Models/Room.cs`、`BetterGenshinImpact/GameTask/AutoHoeing/Multiplayer/MultiplayerCoordinator.cs`、`BetterGenshinImpact/GameTask/AutoHoeing/AutoHoeingTask.cs`（联机段落）、`BetterGenshinImpact/GameTask/AutoHoeing/Multiplayer/CoordinatorClient.cs`。
- 房间内 spec 化石索引（Room.cs 字段注释引用）：multiplayer-shared-fight-end-quorum-sync、multiplayer-hoeing-exp-cap-stop、exp-cap-prefinal-stop-by-two-noexp、multiplayer-mutual-wait-collective-skip、multiplayer-abnormal-wait-coordination、multiplayer-abort-and-realign、multiplayer-abnormal-sync-server、multiplayer-route-enforcement、kazuha-player-auto-detection、hoeing-kazuha-collect-drop-terminal-signal、lock-room-after-start、multiplayer-server-authoritative-round-order、fastsync-claim-short-circuit-premature-release-fix、multiplayer-host-empty-route-member-wait-timeout-fix。

# 槲寄生 · 调度器总计划（Mistletoe Master Plan）

> 本文档是联机助手「槲寄生」调度器的唯一规划与进度枢纽。
> 创建：2026-09-08。维护者：项目 owner + 各执行会话。
> 姊妹篇：`联机助手重构总控计划.md`（管"路与货"的重构）；本文只管助手侧调度器这个功能面。

---

## 0. 定位

槲寄生是 BGI 的**体外大脑**：BGI 什么时间做什么、什么条件做什么、怎么做、循环策略是什么，全部由助手的调度器统筹。
它不是一张静态任务计划表，而是**智能、可条件分支、可拖拽编排**的调度系统。

技术底座已经具备：

- **耕地机**已搭好助手 ↔ BGI 的 IPC 通道（`IpcClient` + `CommandExecutor`，含跨会话守卫、`ext.` 扩展通道），助手可以随意更改 BGI 执行的任务序列（`task.start` / `task.stop` / `task.suspend` / `task.resume` / `config.*`）。
- **嘟嘟可**已提供日志与监控数据（日志关键词、异常、统计），后续可作为调度器的"感知输入"。

槲寄生在此之上做**决策与编排**。

---

## 1. 页面总结构（四个子页）

槲寄生页面内部分四个 Tab：

| # | 子页 | 职责 | 状态 |
|---|---|---|---|
| 一 | 启动中心 | 助手启动后 → BGI 执行任务**之前**的所有动作编排 | ✅ 本次实施 |
| 二 | 任务中心 | 任务总序列指挥所：BGI 任务序列的编排、顺序、循环 | 📋 构想中 |
| 三 | 执行策略 | 执行过程中的自动策略判断（失败重试、冲突、异常分支） | 📋 构想中 |
| 四 | 三方接入 | ABGI、莫酱工具、第三方工具箱等外部软件的统一接入管理 | 📋 构想中 |

### 双端差异总原则（重要，后续每页都要遵守）

- **监控端（遥控器模式）只有「启动中心」，没有后面三个 Tab。** 监控端无本地 BGI，任务编排/执行策略/三方接入都围绕"执行 BGI 的机器"展开，对监控端无意义，直接在 UI 上隐藏，不做置灰。
- **启动中心两端都有，但语义不同**：它编排的是"本机"的启动动作。执行端可以启动 BGI/游戏/下发任务；监控端本机没有 BGI，BGI 类节点自动跳过并记日志，本机类节点（CMD、第三方程序、等待、条件判断）照常可用。
- **配置隔离**：槲寄生各页的配置一律存本机（`%APPDATA%/NexusBGI/`，与 `assistant-config.json` 同目录、按 Windows 用户隔离），**不走 SignalR 同步**。同一台电脑的执行端/监控端（不同 Windows 用户）各自独立配置，互不覆盖。后续若需要"监控端编排执行端的任务序列"，走已有的远程命令链路（SignalR 定向命令），那是任务中心的设计课题，届时单独评审。

### 离线优先总原则（2026-09-11 立项，后续每个子页/节点开发前必读）

核心一句话：**服务器是可选增强，不是运行前提**。槲寄生全部功能默认纯本地可用（不填服务器地址也要能正常使用），联机能力（远程监控、远程下发）是在本地能力之上的叠加层，不是地基。

背景：目标用户不都需要联机锄地与远程监控；典型场景就是本地一台电脑、不同 Windows 用户各跑一个执行端/监控端，只用槲寄生做启动与任务调度。

四条落地规则：

1. **采集与上报解耦**：本地状态采集（IPC/ext 通道 → `LatestLocalStatus` 快照）必须独立于 SignalR 连接运行；SignalR 只是"连接时的上报出口"之一，连不上不影响本地采集。~~现状断点（待修）~~**已修（2026-09-11）**：`LatestLocalStatus` 原先只在 SignalR 首连成功后才启动的 10s 上报循环里刷新，离线时「BGI 任务状态/任务名」本会话条件与电子狗对应分支失效；修复后采集循环（`EnsureStatusTimerStarted`，幂等）在 `InitializeAsync` 无条件启动，`ReportStatusCoreAsync` 仅末尾 SignalR 上报段以 null 守卫跳过。
2. **能力门控而非模式判断**：功能代码不判"是否离线模式"，判"当前具备哪些能力"（本地 IPC / 跨会话只读管道 / 服务器连接），按能力降级并**留痕**（写日志，遵守"一切静默容错必须留痕"纪律）。`ObserverMode` 是这一思路的雏形，照它扩展。
3. **连接策略与总开关**（2026-09-11 owner 决策并落地；同日三次修订：撤销两级开关，只留总开关）：
   - **空地址 = 恒定单机**：`ServerUrl` 留空时完全不发起连接（不连接、不重试、不刷失败日志）；非空 = 维持现状行为（10 秒重试 + 每次失败留痕，**不降噪**——不存在"故意填错"的场景，填错是用户自己的事，重试日志是"连不回来"时区分服务器不可达与服务器拒绝的唯一依据）。
   - **总开关**（整个软件唯一开关，标题栏按钮 `ToggleStandaloneModeCommand`，**弹窗确认** `ShowSwitchConfirmDialog`）：在线 = 耕地机等全部联机功能可用；单机 = 断连并停重试（复用 `SignalRClient.DisposeAsync`/`ConnectSignalRAsync` 既有路径）、**联机功能全部不可用**。选择持久化 `AssistConfig.StandaloneMode`（json `standaloneMode`），重启保持。未填地址时点「切到在线」只记日志不动作（恒定单机）。连接是 App 级共享的（耕地机/嘟嘟可同一条连接），故总开关是全局开关，放在标题栏、全页面可见。
   - **单机关门控收口 = SignalR 访问器门控**：嘟嘟可各 Relay 服务构造注入 `() => IsStandaloneMode ? null : SignalR`，订阅/汇聚/下载/成员墙/画面取图随单机整体消失（服务内部一行不动，既有"未连接不发送"语义自动生效；成员墙/订阅决策点另有 `IsStandaloneMode` 分支，经 `OnMainVmPropertyChanged` 响应重建）。槲寄生本体纯本机、无服务器依赖，无需门控。
   - **曾设子开关后撤销**（2026-09-11 owner 决策）：嘟嘟可/槲寄生页面级单机开关（`LocalPagesStandalone`）因"槲寄生本就纯本机、嘟嘟可自动跟随连接态即可，子开关无实际意义"被撤除，配置字段/UI/代理属性全部移除，不留死代码。后续若出现"连接正常但某页面要单独断服务器"的真实需求再议。
   - **术语纪律**：**单机** = 有意不连（健康态，徽章中性色显示「单机」）；**离线** = 想连没连上（异常态，红色「离线」）；在线/监控 = 已连接。徽章三态（`IsStandaloneMode` 触发器后写覆盖先写）按此区分，后续开发不得混用"离线/单机"。
   - `ServerUrl` 默认值保留 `http://localhost:5000`（不改默认，避免影响联机新装用户习惯）；单机入口 = 首启/设置弹窗中清空地址，或标题栏手动切换。
4. **首启分轨**：纯本地模式下首启设置弹窗只要求本地项（BGI 路径等），联机字段（房间密码/TeamUids/玩家名/UID）留空合法，不完成不阻断启动；免责声明保留。**已落地（2026-09-11）**：`InitializeAsync` 的强制设置弹窗仅联机模式生效；`SettingsWindow` 校验改为"地址留空 = 离线模式、跳过联机字段校验"，非空时校验逐字节不变。

已知设计债务（登记，暂不动工）：

- **同机跨会话「控制」通道缺失**：现有跨会话管道（`BetterGI.v2.status-p{pid}` 只读状态查询）只能"看"不能"控"；监控端离线编排/控制执行端需要新建同机本机传输。属任务中心课题，届时单独评审（见 §3 开放问题④）。

---

## 2. 启动中心（本次实施，已落地）

### 2.1 职责边界

负责"助手进程启动完成 → BGI 开始执行任务"之间的所有动作：要不要启动 BGI、要不要先起游戏、先跑哪个第三方工具、执行什么 CMD、每步之前要不要先判断条件（时间、进程状态等）。

**不负责**：任务执行过程中的策略（→ 执行策略页）、任务序列本身（→ 任务中心页）。

### 2.2 功能清单

1. **开机自启动（复用参数）**：直接读写现有 `AssistConfig.AutoLaunchOnBoot` / `AutoLaunchOnBootMinimized`，与设置页是同一个参数，双向同步，不另起炉灶。
2. **启动流程总开关**：`启用后，助手启动完成自动执行一遍启动流程`，可配延迟秒数（默认 5 秒，等 SignalR/IPC 稳定）。
3. **流程图式编辑器（二轮升级为树形分支）**：启动流程是**树形**结构——条件节点分出「是 / 否」两条子链，子链横向并排展示，跑完子链后**汇合回父链**继续，子链里可再放条件节点（可嵌套）——
   - 节点分两类：**条件判断**（◇ 金色，带是/否两个分支列）与**动作**（▶ 青色）；
   - 拖动节点卡可改变顺序，也可**跨链拖动**（主链 ↔ 分支链、分支链 ↔ 分支链），拖到链空白区追加到链尾；另有 ↑↓ 按钮兜底；
   - 每条链有自己的「＋ 添加节点」按钮，从节点目录中选类型插入该链；每个节点可单独启用/禁用、删除；
   - 点击节点展开内联参数编辑器，改完自动保存（防抖 500ms）；
   - 环检测：禁止把条件节点拖进它自己的子孙分支（ViewModel 层沿 ParentCondition 链上走判定）。
4. **手动试运行**：「立即执行一遍」按钮，便于调试验证流程；执行中有状态显示与取消。
5. **全程日志**：每个节点的判断结果/执行结果写入左侧「冒险日志」。

### 2.3 节点目录

**条件判断节点**（分出「是 / 否」两条子链，判断结果决定走哪条；子链跑完回父链汇合。旧版"失败后策略 onFail"字段已废弃删除）：

| 类型 | 参数 | 说明 |
|---|---|---|
| 时间区间 | 起止时间 HH:mm | 当前时间落在区间内为「是」（支持跨零点） |
| 星期 | 周一~周日多选 | 今天是指定星期为「是」 |
| BGI 进程状态 | 正在运行 / 未运行 | 仅当前 Windows 会话的 BetterGI 进程 |
| 游戏进程状态 | 正在运行 / 未运行 | Yuanshen / GenshinImpact，当前会话 |
| 指定进程状态 | 进程名 + 运行/未运行 | 任意第三方程序守门 |
| 人工确认 | 提示内容 + 超时秒数（0=不限）+ 超时走向 | 弹窗由人点「是/否」决定分支；弹窗同时列出两条分支的后续动作供判断；超时或直接关窗未选按默认走向（ConfirmStepWindow，仿 RemoteConfigGroupSelectWindow 风格，本工程无 ThemedMessageBox） |

**动作节点**：

| 类型 | 参数 | 实现 |
|---|---|---|
| 启动 BGI | 启动参数（可空，传给 BetterGI.exe，如 `--startGroups "组名"`）+ 启动前先关闭 BGI 开关 | 走 `CommandExecutor`（`start_bgi` + args 参数），继承既有会话守卫与策略；勾选先关闭时强杀本会话 BGI 等 2 秒再带参启动（已在运行的 BGI 不抢占，参数不会生效，此开关就是为了强制让参数生效） |
| 关闭 BGI | 无 | `BgiProcessMonitor.KillBgi()`（只杀本会话） |
| 启动游戏 | 程序路径 + 参数 | `Process.Start`（Yuanshen.exe 路径由用户配置） |
| 启动第三方程序 | 程序路径 + 参数 | `Process.Start` |
| 执行 CMD 命令 | 命令文本 | `cmd /c`，隐藏窗口， fire-and-forget |
| 关闭程序 | 进程名 | 按进程名强制结束当前会话的指定程序（与条件检测同口径，只杀本会话） |
| 等待 | 秒数 | 流程内延时（可被取消） |
| **定时触发器** | 触发时间 HH:mm + 每天重复开关 + 「到点执行」子链 | 流程跑到此节点时挂载定时器：今天的该时刻未到则今天触发，已过则顺延明天；到点执行子链（可放任意动作节点）。定时中页面顶部「定时中的触发器」卡片显示状态、可单独取消；每天重复时触发后自动重新挂载。定时器是运行态不持久化，助手重启后由流程重跑重新挂载 |
| **进入任务中心执行** | 无 | 占位节点：跑到这里表示启动阶段完成，移交任务中心接管（任务中心落地前只记日志，流程继续/结束） |
| **结束流程** | 无 | 显式终止整条流程（含所有分支回溯），用于分支末尾提前收尾。**链尾终点约束**：它必须是所在链最后一个节点——链里已有它时「＋添加」禁用、↑↓ 不能越过它、拖拽时普通节点自动夹到它前面、拖它自己强制落链尾 |

> 二轮变更：从目录中**移除**了「BGI 配置组 / BGI 一条龙」两个具体任务节点（启动中心只做任务中心的准备工作，不直接排任务）。Runner 里保留分支兼容旧配置（日志标注"旧版节点"），但不能再从目录新增。
> 扩展预留：节点模型是 `Kind` 字符串 + 扁平参数字段 + `TrueSteps`/`FalseSteps` 子链，新增节点类型只需加目录项 + Runner 一个分支 + 编辑器一字段区，不动序列化格式。

### 2.4 存储与代码位置

- 配置文件：`%APPDATA%/NexusBGI/startup-flow.json`（System.Text.Json，与 AssistConfig 一致的风格；按 Windows 用户隔离）。
- 模型：`MultiplayerHoeingAssistant/Models/StartupFlowModels.cs`
- 存储：`Services/StartupFlowStore.cs`
- 执行引擎：`Services/StartupFlowRunner.cs`（重入守卫、CancellationToken、逐节点日志）
- 页面：`Views/MistletoePage.xaml(.cs)`、`ViewModels/MistletoeViewModel.cs`（手写 INPC，继承 `ViewModelBase`，同嘟嘟可模式）

### 2.5 验收点

- [x] 编译 0 error（`dotnet build BetterGenshinImpact.sln -c Debug`）
- [ ] 实机：执行端编排"条件+动作"混合流程并自动/手动跑通（需实测）
- [ ] 实机：监控端只见启动中心 Tab，BGI 类节点跳过有日志（需实测）

---

## 3. 任务中心（构想中）—— 任务总序列指挥所

**用户的构想**：任务总序列的指挥中心，BGI 做什么任务、什么顺序、什么循环，在这里编排。

**初步方案（未定稿，先记录）**：

- 形态：与启动中心同一套「流程图节点链」交互范式（拖拽、增删、条件门控），但节点语义换成 **BGI 任务**：配置组、一条龙、JS 脚本、快捷键，节点之间可挂条件（时间/星期/上一任务成败/游戏在线状态）。
- 与 BGI 原生调度器的关系：**不取代、只指挥**。任务中心下发的是 `task.start` 序列；BGI 侧仍用自己的调度器执行。循环策略（每天 N 轮、跑完隔多久再来）在助手侧用计时器实现。
- 依赖切片 7 的队列式 `task.start` 与任务生命周期事件（`ext.` 通道）——任务"完成/失败"事件是链式推进的扳机，不用再轮询 `task.status`。
- 序列可以"下发到执行端"：监控端编辑 → SignalR 定向命令 → 执行端落盘生效（需要新的 `ext.` 协议操作，届时评审）。
- 开放问题：① 循环/分支的表达上限（纯链式 vs 允许跳转）；② 与现有「定时上线」「全员就绪打断」两套既有调度的优先级仲裁；③ 执行中用户手动操作的冲突策略；④ **离线形态**：纯离线下任务中心是否只需要"各端各编本机"（监控端任务中心直接隐藏），还是要新建同机跨会话控制通道支撑"监控端离线编排执行端"（§1 离线优先原则登记的设计债务）——动工前先回答这条，再定节点语义。

## 4. 执行策略（构想中）

**用户的构想**：执行过程中的自动策略判断。

**初步方案（未定稿，先记录）**：

- 输入感知：嘟嘟可的日志关键词命中、异常事件、BGI 崩溃事件、任务终态事件（成败/取消）、游戏掉线。
- 策略规则表：「当 X 发生 → 做 Y」的规则列表，Y 可以是重试当前任务（N 次封顶）、跳过当前任务、停止全部、执行指定任务、通知（托盘气泡）。
- 与既有逻辑的合并点：现有「上线锄地完成后动作」「6 键固定收尾策略」「守护 BGI」本质都是执行策略的硬编码特例，远期可收编进规则表（**不动红线：收编前行为逐字节不变**）。

## 5. 三方接入（构想中）

**用户的构想**：ABGI、莫酱工具、第三方工具箱等外部软件统一在此管理。

**初步方案（未定稿，先记录）**：

- 每个三方软件一张"接入卡"：程序路径、启动参数、健康检测（进程存在/心跳）、联动动作（随启动中心拉起、随 BGI 退出关闭）。
- 启动中心的「启动第三方程序」节点是三方接入的执行出口之一；远期三方接入页配置的软件可直接变成启动中心/任务中心的可选节点。
- 开放问题：① ABGI/莫酱各自有无命令行或 IPC 接口可深度联动（需要调研）；② 是否做"配置同步"（风险高，默认不做）。

---

## 6. 技术纪律（继承总控计划 §3，此处只列调度器相关）

1. IPC 帧格式与现有 22 个 IPC 操作行为不变；调度器若需新 IPC 操作，一律 `ext.` 前缀并先评审。
2. 触碰 UI 一律 `Dispatcher.InvokeAsync`。
3. 调度器所有计时/延时都必须可被 CancellationToken 取消；助手退出时不残留后台任务。
4. 流程配置损坏（JSON 反序列化失败）→ 落回空流程并在冒险日志报警，**绝不让助手启动失败**。
5. 执行端/监控端差异：新增每个子页时先回答"监控端要不要、要什么形态"，再动手。
6. 离线优先（§1 离线优先总原则）：新增任何子页/节点/状态来源，先回答"无服务器时是否可用、降级成什么、降级是否留痕"，再动手；依赖 SignalR 的能力一律走能力门控，不允许硬依赖服务器连接。

---

## 7. 进度日志

| 日期 | 内容 |
|---|---|
| 2026-09-08 | 总计划创建；启动中心实施（模型/存储/执行引擎/流程图编辑器 UI/导航接入），助手项目编译 0 CS/MC error（部署复制步骤因助手正在运行被文件锁跳过，符合 AGENTS.md「程序占用放弃」约定；还原阶段在本机 shell 缺 PROGRAMDATA 环境下报 NuGet path1 错，用 --no-restore 绕过）；顺手删除已无引用的 FeaturePlaceholderCommand/ShowFeaturePlaceholder 死代码 |
| 2026-09-08（二轮） | 按用户三条反馈改造：① 开机自启动参数与设置页实时同步修复（MistletoeViewModel 订阅 MainViewModel.PropertyChanged 转发 AutoLaunchOnBoot/AutoLaunchOnBootModeIndex）；② 单链升级为**树形分支**：条件节点分出是/否两条横向并排子链、跑完汇合回父链、可嵌套，支持跨链拖拽与环检测（StepChainViewModel + 递归 DataTemplate + 链空白区落点追加）；③ 移除 BGI 配置组/一条龙节点（Runner 保留兼容旧配置），新增「进入任务中心执行」（占位，待任务中心接管）与「结束流程」节点；编译 0 CS/MC error |
| 2026-09-08（三轮） | 启动中心增补：① 新增「关闭程序」动作节点（killProgram，按进程名结束当前会话指定程序，与条件检测同口径）；② 修复分支链「＋添加节点」弹层在空间受限位置下方截断（内容限高 420 + 内滚动）；③ 新增「定时触发器」节点（timerTrigger：触发时间 + 每天重复开关 + FireSteps 到点子链，流程跑到即挂载定时器，页面「定时中的触发器」卡片显示状态可取消；运行态不持久化）；编译 0 CS/MC error。三次提交：937428ee / 7b14d535 / 本次 |
| 2026-09-08（四轮） | 新增「人工确认」条件节点（manualConfirm）：弹窗点是/否决定分支走向，弹窗内列出两条分支后续动作预览，可设超时秒数（0=不限）与超时默认走向，关窗未选也按默认走向；新窗体 ConfirmStepWindow（DispatcherTimer 倒计时，Runner 注入异步确认委托）；顺带修复三轮引入的 StaticResource BasedOn 运行时 XAML 异常（7ab26650）；编译 0 CS/MC error |
| 2026-09-08（五轮） | ① 「结束流程」链尾终点约束（三处：链已有它时「＋添加」禁用、↑↓ 不可越过、拖拽自动夹到它前面/拖它强制落尾）；② 「启动 BGI」节点支持命令行参数 + 「启动前先关闭 BGI」开关（a2d2073d）；顺带修复 StepChainViewModel.HasSteps 从未通知导致空链提示不刷新的二轮遗留 bug；编译 0 CS/MC error（部署复制因助手在运行跳过） |
| 2026-09-08（六轮） | 条件节点是/否分支区可折叠：默认折叠，节点卡「▸/▾ 分支」单个展开，流程卡头部「⤢ 展开全部分支」一键递归展开/收起整棵树（运行态不持久化）；编译 0 CS/MC error |
| 2026-09-08（六轮修复） | 实机炸点修复：多用户会话托盘启动的助手主窗口从未显示，人工确认弹窗 Owner 指向它导致 ShowDialog 抛异常 + 构造函数里已 Start 的倒计时继续在未显示窗口设 DialogResult 二次炸；改为 Owner 校验 IsLoaded/IsVisible（不满足则 null + 居中屏幕）、计时器 Loaded 才启动 Closed 停止、Tick 加 IsVisible 兜底（经验已沉淀 project-experience.md） |
| 2026-09-11 | 立项「离线优先」总原则：服务器是可选增强、不是运行前提（§1 新增小节：四条落地规则——采集与上报解耦/能力门控/连接策略三态/首启分轨，登记现状断点 LatestLocalStatus 离线不刷新 + 同机跨会话控制通道设计债务；§3 任务中心补开放问题④离线形态；§6 技术纪律补第 6 条）。纯文档变更，无代码改动 |
| 2026-09-11（修订） | owner 决策修订规则 3：连接策略从「三态 + 重试降噪」改为「**ServerUrl 留空 = 离线模式**（完全不发起连接），非空 = 现状 10s 重试 + 失败留痕不降噪」（重连机制不动，填错地址是用户自己的事）；新增手动离线/在线切换按钮设计（运行时切换、持久化、App 级全局开关、复用 DisposeAsync/ConnectSignalRAsync 既有路径）。纯文档变更，无代码改动 |
| 2026-09-11（实施） | 离线优先四项改造落地（5 文件，MHA 编译 0 error）：①空地址门控——`ConnectSignalRAsync` 入口 `IsOfflineMode` 早退、catch 与重试体在飞切离线放弃、`RefreshAsync` 入口门控 + 连接在飞期间切离线销毁到手连接；②手动切换——`AssistConfig.OfflineMode` 持久化（`SettingsWindow.BuildConfig` 补保留，防重蹈 ClientInstanceId 幽灵条目覆辙）+ 标题栏「切到离线/联机」按钮 + `GoOfflineAsync`（停重试、Dispose 连接、**刻意不停采集定时器**）+ 设置保存后 `SyncOfflineStateAfterSettingsSaved`（清地址即时断连）；③采集/上报解耦——`EnsureStatusTimerStarted` 幂等启动收编 4 处启动点、`InitializeAsync` 无条件启动、`ReportStatusCoreAsync` 移除 null 早退、仅末尾上报段 null 守卫（已连接路径逐字节不变，RefreshAsync 不再停采集定时器）；④首启分轨——强制设置弹窗仅联机模式生效、`SettingsWindow` 地址留空跳过联机校验。待实机验证：离线模式下槲寄生条件/电子狗/任务中心卡片取到快照、切换按钮往返、空地址启动零重试日志 |
| 2026-09-11（两级化） | owner 决策二次修订规则 3：单开关升级为**两级开关**（§1 规则 3 已改写）。落地（MHA 编译 0 error）：①改名——`OfflineMode`→`StandaloneMode`（json `standaloneMode`）、`IsOfflineMode`→`IsStandaloneMode`（13 处）、全量"离线模式"改词"单机模式"（术语纪律：单机=有意不连/健康态，离线=想连没连上/异常态）；②总开关——标题栏「切到单机/切到在线」+ `ShowSwitchConfirmDialog` 弹窗确认（通用化自执行/监控切换弹窗，三开关共用）；徽章三态（`IsStandaloneMode` 触发器后写覆盖：中性紫「单机」，与红色「离线」区分）；③子开关——新增 `AssistConfig.LocalPagesStandalone`（`BuildConfig` 同步保留），有效值 `IsLocalPagesStandalone = IsStandaloneMode || LocalPagesStandalone`，总单机时子开关勾选且置灰；④嘟嘟可门控收口——三个 Relay 服务构造注入改为 `() => IsLocalPagesStandalone ? null : SignalR`（服务内部一行不动，既有"未连接不发送/换绑自动重挂"语义自动生效），`DesiredSubscriptionTarget`/`RebuildLogSources`/`RefreshRemoteMembers`/`RebuildMonitorOptions`/`RequestRemoteFrame` 加单机分支，`OnMainVmPropertyChanged` 响应 `IsLocalPagesStandalone` 重建；⑤子开关 UI——嘟嘟可/槲寄生页面顶栏各一个 CheckBox（OneWay + Command 弹窗确认，取消时重通知弹回），VM 各加 3 个代理属性。待实机验证：两个开关弹窗确认与往返、嘟嘟可单机时远程订阅/下载下拉/画面成员墙消失、徽章三态、空地址恒定单机点"切到在线"只留痕 |
| 2026-09-11（撤子开关） | owner 决策三次修订规则 3：**撤销两级开关，只留总开关**。理由：槲寄生本体纯本机、无任何 SignalR 依赖（`MistletoeViewModel` 无 `_mainVm.SignalR` 引用），子开关对它无实际效果；嘟嘟可的远程数据面跟随连接态自动降级即可，无需单独开关。撤除：`AssistConfig.LocalPagesStandalone` 字段、`ToggleLocalPagesStandaloneCommand`/`ToggleLocalPagesStandaloneWithConfirm`、`LocalPagesStandalone`/`IsLocalPagesStandalone`/`IsLocalPagesSwitchEnabled` 属性、两页面顶栏 CheckBox、Dodoco/Mistletoe VM 代理属性与转发分支——grep 旧名零残留，不留死代码。门控保留但改由总开关驱动（`IsStandaloneMode`）：Relay 访问器门控与五个成员墙/订阅决策点全部换判 `IsStandaloneMode`，`OnMainVmPropertyChanged` 改响应 `nameof(IsStandaloneMode)`。MHA 编译 0 error |

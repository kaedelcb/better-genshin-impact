# 联机锄地"上线"链路稳定性分析报告（服务端 × BGI 侧）

> 只读探查，分支 main-OldTeaBag-B151，未修改任何文件。行号基于探查当日工作区副本。

---

## ① 服务端上线判定状态机梳理

### 数据模型：按成员记，不按房间记

- 每个控制房间成员（`ControlRoomPlayer`）自带一组上线事件字段：`OnlineEventGeneration`（当前代序号）、`OnlineEventConsumed`（是否已消费，默认 true）、`OnlineEventTime`——`BgiCoordinatorServer/Models/ControlRoomPlayer.cs:55-60`。
- 成员条目按 **(PlayerUid, ClientInstanceId)** 复用：重连换 connectionId 时找回旧条目并更新 ConnectionId——`RoomManager.cs:1041-1078`（AddToControlRoom）。
- 房间级状态机 `RoomAllReadyState`（idle / ready / confirming / consumed / exhausted；注释里提到 waiting 但**没有任何代码把状态置为 waiting**）：`RoomManager.cs:1264-1279`，存于 `_roomAllReadyStates`（1281）。确认阶段的 `ConfirmedUids` 按 **uid** 键控（1274）。

### 状态流转

- **[成员事件槽]** `ReportOnlineEvent`（RoomManager.cs:1284-1308）：`generation <= 成员历史值` → 直接 return（1292），房间状态不重置；新事件 → 更新成员槽（1295-1299）+ 房间状态无条件打回 `idle`（1303-1306）。
- **[房间状态机]** `CheckAndTransition`（RoomManager.cs:1311-1343）：仅 idle/waiting 可进（1316）；ready = `Online && !Consumed && gen>0` 的成员（1324）；threshold = 所有在线成员 `ExpectedHoeingPlayers` 的最小值（`max(1,x)`，**不对在线人数封顶**）（1326）；`ready.Count < threshold` → 保持 idle 返回 false（1330-1334）；就绪 → `state="ready"`，`generation = 就绪成员最小 gen`（minGen，1337-1342）。
- **[Hub 分发]** `CoordinatorHub.ReportOnlineEvent`（CoordinatorHub.cs:2626-2672）：就绪成员 ≤1 → 单人直达：ConsumeOnlineReady + 广播 AllReady（2655-2660）；≥2 → BeginConfirming（RoomManager.cs:1414-1424）+ StartConfirmAsync（2663-2664）。
- **[确认阶段]** `StartConfirmAsync`（CoordinatorHub.cs:2675-2732），3 轮 × 30s：每轮向未确认成员单发 AllReadyConfirm（2688-2696）；`IsStateConfirming=false` → 中断并按 minGen 消费已确认者（2701-2709）；`IsAllConfirmed` → ConsumeOnlineReady（2711-2716，**不广播 AllReady**）；3 轮耗尽 → MarkExhausted（2730；RoomManager.cs:1435-1439），整轮放弃。
- **[消费]** `ConsumeOnlineReady`（RoomManager.cs:1346-1395）：**只消费 `OnlineEventGeneration == minGen` 的成员**（1357），state=`consumed`（1386）。
- **[确认回执]** `ConfirmAllReady`（CoordinatorHub.cs:2735-2749）：connectionId → uid → `RegisterConfirmAck`（RoomManager.cs:1426-1433，校验 state=confirming 且 gen 匹配）。
- **[清零路径]** 断线：`OnDisconnectedAsync`（CoordinatorHub.cs:1085）→ `RemoveFromControlRoom`（RoomManager.cs:1144-1167）：Online=false、ConnectionId=""、**OnlineEventGeneration=0、Consumed=true**（1160-1162）。30 分钟 OnlineReady 过期：`HeartbeatMonitor`（10s 扫描，14/141 行）`CheckControlRoomTimeout:279-290` → OnlineReady=false + gen=0 + Consumed=true。

### 客户端（助手 + BGI）对应点

- 路径① 本地定时：`StartOnlineScheduler` 每 30s 检查，按天去重（`_lastScheduledFireDate`），到点 `MarkOnlineAsync("scheduled")` → `_localOnlineGeneration++`（**内存计数，从 0 开始，不持久化**）→ 上报——`MainViewModel.cs:36, 634-663, 667-688`。
- 路径② BGI 任务：`NotifyOnlineTask`（`BetterGenshinImpact/GameTask/AutoOnline/NotifyOnlineTask.cs`），generation 持久化 `User/online_generation.txt`（P0-B，24/44/50-80 行）；IPC `task.status` 聚合 `onlineGeneration` + `onlineTriggeredAt`——`InstanceRequestHandler.cs:744-775`；助手轮询做边沿检测后上报——`MainViewModel.cs:512-560`。
- 收 AllReady / AllReadyConfirm：`SignalRClient.cs:75-83`；助手收到 **AllReadyConfirm 后立即 ack 并开始开锄**——`MainViewModel.cs:2649-2659` → `OnAllReadyConfirmedInternal`（4587-4601，幂等号 `_lastProcessedAllReadyGeneration`）。

---

## ② 六个问题的直接回答

**Q1 丢弃逻辑**：`RoomManager.cs:1292`，`generation <= player.OnlineEventGeneration` 即丢弃，**按成员记**（per-player 槽），A、B 互不把对方当旧事件——跨成员担心不成立。但**同一成员的两条路径是两个独立计数序列共用一个服务端槽**：本地路径内存计数（MainViewModel.cs:36,652）与 BGI 路径持久化计数（NotifyOnlineTask.cs:24）互不知道对方。若本地路径已把 gen 顶到 5 且被消费，BGI 路径首次触发报 gen=1 → `1 <= 5` 被丢弃 → 该次上线事件丢失、该成员被落下（直到 BGI gen 超过 5）。反向同理。

**Q2 触发窗口**：先到者**不会**被先消费，只要 threshold ≥ 2——先到者事件留在槽里（30 分钟保鲜），state 停在 idle，后到人上报时一起进确认。误判只在两种配置下发生：(a) 任一在线成员 `ExpectedHoeingPlayers=1` → threshold=1，任何单人上报立即单人直达开锄（CoordinatorHub.cs:2655-2660），后到者被落下、之后各开各的；(b) 默认 ExpectedHoeingPlayers=4（MainViewModel.cs:509）而房间实际人数 < 4 → ready 永远凑不齐 → **永远不上线**。threshold 不对 `onlinePlayers.Count` 封顶（RoomManager.cs:1326）是这两个极端的共同根因。

**Q3 确认期间快照**：`targetUids` 是确认开始时快照（CoordinatorHub.cs:2645-2663）。
- **掉线**：条目不删、只标离线（RoomManager.cs:1150-1164），uid 仍在 PendingConfirmUids → 算"未确认"。90s 内重连可救：重试每 30s 重查 connectionId（2690），AddToControlRoom 按 (uid, clientInstanceId) 复用条目，ack 按 uid 计入。90s 未归 → 整轮 MarkExhausted。
- **新上线/新上报**：任何新 generation 事件把房间状态打回 idle（RoomManager.cs:1303-1306）→ StartConfirmAsync 检测 IsStateConfirming=false 中断（CoordinatorHub.cs:2701-2709），只按 minGen 消费已确认者，随后新事件的 CheckAndTransition 可能**立刻用残留事件重开一轮**。
- **重连换 connectionId**：确认状态按 uid 保留，重连后 ConfirmAllReady 经新 connectionId 解析 uid 仍计入（CoordinatorHub.cs:2741-2743）。
- 小问题：断线成员 `ConnectionId=""`（RoomManager.cs:1154），`GetConnectionIdByUid` 返回 `""` 而非 null，`if (connId != null)`（2691）放行 → 向空连接发送，静默丢失、无日志。

**Q4 exhausted 恢复**：唯一出口是任一成员上报**通过 1292 检查的新 generation** → 状态打回 idle（1303-1306）。被丢弃的旧事件**不**重置状态（1292 在重置之前 return）。若全员只用本地定时路径（按天去重，当天不会第二次触发），耗尽后当天无人能产生新 gen → 房间卡 exhausted 到次日；无手动重置入口。次日由各路径 gen 递增自愈。

**Q5 BGI 侧触发**：理解正确。`NotifyOnlineTask` 在 `SoloTaskRegistry.cs:46` 按任务名注册，必须被用户加进调度器配置组/一条龙任务列表才会执行；不放则路径②不存在，只剩本地定时路径。另注意 `CurrentGeneration` 是进程内 static，任务没跑过时 IPC 恒返回 0（InstanceRequestHandler.cs:774）。

**Q6 重连**：条目按 (uid, clientInstanceId) 复用（RoomManager.cs:1046-1056），确认状态（uid 键控）保留；但 `OnlineEventGeneration/Consumed` 在断线瞬间已被 RemoveFromControlRoom 清零（1160-1162）——**不保留**。且助手重连后只重新 JoinControlRoom（SignalRClient.cs:94-109），**不补报**最近一次 generation。

---

## ③ 风险清单（按严重程度排序）

### 高

**R1【重复触发/误开锄】ConsumeOnlineReady 只消费 minGen 成员，高 gen 成员事件残留武装** — 未修
- 位置：`RoomManager.cs:1357`（`p.OnlineEventGeneration == generation` 才消费）+ `1337`（minGen）。
- 时序：A(gen5)、B(gen2) 同轮就绪 → minGen=2 → 只有 B 被消费，A 的 gen5 留在槽里 unconsumed。30 分钟内任何人上报新事件 → state 打回 idle（1305）→ CheckAndTransition 立即就绪 → A 收到 AllReadyConfirm 自动 ack 并开锄（MainViewModel.cs:2649-2659），尽管 A 本轮根本没触发。
- 说明：成员 generation 各自独立递增，一轮内几乎必然不等，所以**每次多人成功开锄都会留下残留事件**。唯一清道夫是 30 分钟 OnlineReady 过期（HeartbeatMonitor.cs:279-290）。

**R2【"缺人不开锄"形同虚设】AllReadyConfirm 即开锄，耗尽无法召回已开锄成员** — 未被 P2-G 真正覆盖
- 位置：`MainViewModel.cs:2649-2659`（收到 AllReadyConfirm → ack → 立即 `OnAllReadyConfirmedInternal` 开锄）+ `CoordinatorHub.cs:2724-2730`（耗尽仅 MarkExhausted + 广播列表，无召回命令）。
- 时序：确认阶段第 1 轮，A 收到确认消息立刻开锄；B 卡住 → 3×30s 后 MarkExhausted → 但 A 已经锄了 90 秒。"整轮放弃"只对卡住的人生效。
- 说明：今天 P2-G 定稿"耗尽即放弃"，但放弃动作没有下发 stop/召回，已确认成员的开锄不可逆。

**R3【卡死/整轮耗尽】客户端幂等号假设轮次 generation 单调，但轮次 id = minGen 跨轮不单调** — 未修
- 位置：`MainViewModel.cs:2651` 与 `4596`（`generation <= _lastProcessedAllReadyGeneration` 直接 return，**不 ack**）vs `RoomManager.cs:1337`（轮次 id 取就绪成员最小 gen）。
- 时序：昨天某轮 minGen=5，A 的 `_lastProcessedAllReadyGeneration=5`；今天 B 的助手重启过、本地 gen 从 1 重来（MainViewModel.cs:36 内存计数），minGen=1 → A 收到 AllReadyConfirm(1) → `1 <= 5` 静默忽略：不 ack、不开锄 → 服务端永远等不到 A → 3 轮后整轮 exhausted。只要任一成员的计数序列低于其他成员的历史 minGen 就会复现。

### 中

**R4【有人被落下】断线即抹掉上线事件，重连不补报** — 未修
- 位置：`RoomManager.cs:1160-1162` + `SignalRClient.cs:94-109`。
- 时序：成员上报后、凑齐人之前网络抖动断线 1 秒 → gen 清零、Consumed=true；重连后只 rejoin 不补报 → 该成员从 ready 集合消失。若因此达不到 threshold → 整轮空等（无任何超时/告警）；若发生在确认阶段 → 见 Q3，90s 内重连可救。

**R5【确认阶段被打断 + 与 R1 叠加】确认期间任何新上报把状态打回 idle** — 未修
- 位置：`RoomManager.cs:1303-1306`（重置不看当前状态）+ `CoordinatorHub.cs:2701-2709`。
- 时序：确认进行中，第三名成员到点上报 → state=idle → 确认循环中断（只按 minGen 部分消费）→ 同一次 ReportOnlineEvent 的 CheckAndTransition 用残留 + 新事件立即重开一轮 → 旧轮已开锄成员（R2）+ 新轮并发，助手侧靠 `_isAllReadyProcessing` 互斥锁（4590）挡第二次开锄，但服务端状态已混乱。

**R6【永远不上线 或 单人误判】threshold 不对在线人数封顶** — 未修
- 位置：`RoomManager.cs:1326`；默认值 `MainViewModel.cs:509`（4 人）。
- 时序：(a) 2 人房、没人改默认 4 → ready 永远 < 4 → 永远等待（无任何提示）；(b) 任何在线成员配 1 → 单人上报立即直达开锄，后到者被落下。

**R7【幻影上线】助手重启即触发上线：边沿基线归零 + 忽略 onlineTriggeredAt** — 未修（P0-B 的反面）
- 位置：`MainViewModel.cs:38`（`_lastOnlineGeneration` 内存基线，启动为 0）、`535-549`（gen>0 即上报）；`InstanceRequestHandler.cs:775` 提供了 `onlineTriggeredAt` 但助手端没有任何代码读取它。
- 时序：BGI 凌晨跑过任务（gen=5，已持久化），中午用户才启动助手 → 首轮轮询 gen=5 > 0 → 立即上报上线 → 非到点上线/凑人头。
- 说明：P0-B 修的是"BGI 重启后 gen 复位被服务端永久丢弃"；助手重启方向的幻影触发仍存在。

### 低

**R8【单成员被落下】同一成员双路径 generation 不同源，低序列事件被 1292 丢弃** — 部分覆盖
- 见 Q1。P0-B 让 BGI 路径跨重启递增，缩小了差距，但两条序列仍会交错；本地路径（`_localOnlineGeneration`）至今不持久化。触发条件较窄（同一天内双路径都触发，如手动重跑 BGI 任务），按天去重使其概率低。

**R9【当天卡死】exhausted 无手动重置，全员纯本地定时路径时当天无法恢复** — 设计取舍的延伸，未修
- 见 Q4。属于"缺人不开锄"的可接受后果，但建议至少提供房主手动重置入口或 exhausted 超时自愈。

**R10【观察项】state "waiting" 是死状态** — 文档与实现不符
- `RoomManager.cs:1267` 注释与 `1316` 的准入条件包含 "waiting"，但全仓库没有任何代码置 waiting。无实际危害，说明状态机文档化不准确。

---

## 覆盖对照：今天 8 项止血 vs 本报告

| 风险 | 今天是否已覆盖 |
|---|---|
| P0-B（BGI gen 持久化） | 已修 —— 解决"BGI 重启后事件被服务端永久丢弃"；但 R7 幻影上线、R8 双路径交错仍在 |
| P2-G（耗尽即放弃） | 状态机层面已落实（MarkExhausted）；但 R2 指出"放弃"无法召回已开锄成员，语义缺口仍在 |
| IPC/断线韧性 6 项 | 覆盖助手侧异常冒泡、重连 rejoin 等；但 R4（断线抹事件不补报）、R3（幂等号非单调）不在其列 |
| R1 / R3 / R5 / R6 | **完全未修**，其中 R1 在每次多人成功开锄后都会留下 30 分钟残留窗口 |
